__pycache__/
*.pyc
.venv/
.pytest_cache/
.superpowers/
_site/
.env

# 보도자료 hwpx 는 고용노동부 게시판에서 언제든 다시 받는다
# (domains/employment/pipeline/releases.py 의 fetch_list('ei') + fill_attachments).
# 회차마다 1.4MB 라 저장소에 쌓을 이유가 없다. 반면 sources/raw 의 기사와
# sources/verdicts 의 판정은 **다시 만들 수 없으므로** 반드시 담는다 —
# 네이버 검색 API 는 검색어당 1,000건까지만 거슬러 가서 약 50일이면 못 닿는다.
domains/press/sources/releases/

# wrangler 로컬 캐시·상태. 배포 산출물이 아니다.
.wrangler/
