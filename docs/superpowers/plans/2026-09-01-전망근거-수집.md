# 전망 근거 수집 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 전망 수치 옆에 그 기관이 밝힌 근거 문장을 원문 그대로 붙인다.

**Architecture:** 보고서 본문에서 세 조건(지표 언급·인과 표지·전망 표지)을 모두 만족하는 문장을 찾아 인용한다. 요약하지도 번역하지도 않는다. 근거는 (기관, 발표일, 지표)당 하나이며 `rationales.json` 에 한 번만 저장하고, 화면이 그 키로 찾아 붙인다.

**Tech Stack:** Python 3.12, pydantic v2, pytest, 브라우저 ES 모듈(테스트는 `node --test`)

**Spec:** `docs/superpowers/specs/2026-09-01-전망근거-수집-design.md`

**선행 작업:** `docs/superpowers/plans/2026-09-01-IMF-OECD-원문링크.md` 가 **먼저 끝나야 한다.** IMF·OECD 는 지금 보고서를 받아오지 않아 본문에 닿을 길이 없다.

## Global Constraints

- **원문 문장을 그대로 인용한다.** 요약·의역·번역 금지. 영문 보고서의 문장은 영문 그대로 싣는다.
- 근거의 단위는 **(org, published_at, indicator)** 하나다. 같은 지표의 연간·상반기·하반기가 같은 근거를 가리킨다.
- **못 찾으면 비운다.** 다른 지표의 문장을 가져오거나 회차 요약으로 대체하지 않는다.
- 태그는 **인용한 문장 안의 낱말에서만** 뽑는다. 문장에 없는 요인은 붙이지 않는다.
- 근거를 못 찾았다고 **수치 수집을 실패시키지 않는다.** 수치가 본체이고 근거는 맥락이다.
- 이미 있는 근거를 재수집이 **덮어쓰지 않는다** — 사람이 고친 문장을 지우면 안 된다.
- 기존 453건의 **수치는 한 건도 바뀌지 않는다.** 이것이 회귀 기준이다.
- 모든 주석·문서·메시지는 한국어, 기존 수집기 문체.
- 새 의존성 없음. `domains/employment/`, `hub/`, `core/`, `tools/` 를 건드리지 않는다.
- 커밋 메시지는 `feat(forecast):` 형태의 한국어.

## 파일 구조

| 파일 | 책임 |
|---|---|
| `domains/forecast/pipeline/rationale.py` (신규) | 문장 선정과 태그 부여. 기관을 모른다 |
| `domains/forecast/pipeline/rationale_store.py` (신규) | `rationales.json` 읽기·병합·저장 |
| `collectors/*.py` (수정) | 표를 읽는 자리에서 본문도 넘긴다 |
| `domains/forecast/pipeline/collect.py`·`backfill.py` (수정) | 근거를 모아 저장 |
| `domains/forecast/pipeline/models.py` (수정) | `rationale`·`rationale_tags` 제거 |
| `domains/forecast/app/js/data.js` · `screens/*.js` (수정) | 키로 찾아 표시 |

`rationale.py` 는 기관도 PDF 도 모른다 — 문장 목록과 지표를 받아 하나를 고른다.
그래서 기관별 규칙이 새 파일에 번지지 않고, 테스트가 문자열만으로 돈다.

---

## Task 1: 문장 선정기

**Files:**
- Create: `domains/forecast/pipeline/rationale.py`
- Test: `domains/forecast/tests/test_rationale.py`

**Interfaces:**
- Produces:
  - `rationale.sentences(text: str) -> list[str]`
  - `rationale.pick(text: str, indicator: str) -> str | None`
  - `rationale.INDICATOR_WORDS: dict[str, tuple[str, ...]]`

- [ ] **Step 1: 실패 테스트를 쓴다**

```python
import pytest

from domains.forecast.pipeline import rationale

# 아래 문장은 KEIS 2026년 제5호 20쪽에서 그대로 딴 것이다.
REASON = ("2026년 하반기 취업자 수 증가 배경에는 상반기 고용시장의 완만한 "
          "회복 흐름과 경기 개선 기대가 자리한 것으로 전망된다.")
RESTATEMENT = "하반기 취업자 수는 전년 동기 대비 약 18만 5천 명 증가할 것으로 예상되며"
OTHER_INDICATOR = ("생산가능인구는 하반기에도 전년 동기 대비 0.4% 증가세를 "
                   "이어갈 것으로 예상되나, 대부분은 60세 이상 고령층이다.")


def test_pick_takes_the_sentence_that_gives_a_reason():
    assert rationale.pick(REASON, "emp_change") == REASON.strip()


def test_pick_rejects_a_restatement_of_the_number():
    # 지표와 전망 표지는 있으나 인과 표지가 없다
    assert rationale.pick(RESTATEMENT, "emp_change") is None


def test_pick_rejects_a_sentence_about_another_indicator():
    assert rationale.pick(OTHER_INDICATOR, "emp_change") is None


def test_pick_needs_a_forecast_marker():
    # 인과 표지는 있으나 지난 일을 말한다
    past = "상반기 취업자 증가는 건설경기 부진 완화에 기인했다."
    assert rationale.pick(past, "emp_change") is None


def test_pick_takes_the_first_when_several_qualify():
    first = "취업자 증가는 내수 회복에 힘입어 이어질 것으로 전망된다."
    second = "취업자 증가는 수출 호조를 반영해 확대될 것으로 예상된다."
    assert rationale.pick(f"{first} {second}", "emp_change") == first


def test_pick_reads_english_reports():
    en = ("Employment growth is projected to remain solid, supported by "
          "resilient domestic demand.")
    assert rationale.pick(en, "emp_change") == en


def test_pick_returns_none_for_text_without_any_reason():
    assert rationale.pick("표는 다음과 같다.", "emp_change") is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_rationale.py -q`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 구현한다**

```python
"""전망 근거로 삼을 문장을 고른다.

기관도 PDF 도 모른다 — 본문과 지표코드를 받아 문장 하나를 고를 뿐이다. 기관별
규칙이 여기 번지지 않아야 테스트가 문자열만으로 돌고, 새 기관이 붙어도 이 파일이
자라지 않는다.

요약하지 않고 원문 문장을 그대로 준다. 요약은 기관이 하지 않은 말을 기관의
근거처럼 적을 위험이 있고, 그 오류는 수치와 달리 원문 대조로 드러나지 않는다.
"""
from __future__ import annotations

import re

# 지표 표제어. 한국어와 영어를 함께 둔다 — IMF·OECD 는 영문 보고서다.
INDICATOR_WORDS: dict[str, tuple[str, ...]] = {
    "emp_change": ("취업자", "고용", "employment"),
    "emp_rate": ("고용률", "employment rate"),
    "unemp_rate": ("실업률", "unemployment"),
    "gdp_growth": ("성장률", "국내총생산", "GDP", "growth"),
    "cpi": ("소비자물가", "물가", "inflation", "CPI"),
    "labor_force": ("경제활동참가율", "participation"),
}

# 인과 표지 — 이 문장이 '왜' 를 말한다는 신호
_CAUSE = ("배경", "때문", "기인", "반영", "영향", "힘입어", "따라",
          "due to", "driven by", "reflecting", "supported by",
          "owing to", "on the back of")

# 전망 표지 — 지난 일이 아니라 앞을 말한다는 신호
_FORECAST = ("전망", "예상", "것으로",
             "is projected", "are projected", "is expected", "are expected",
             "is forecast", "will")

_SENTENCE = re.compile(r"[^.。\n]+[.。]|[^.。\n]+$")


def sentences(text: str) -> list[str]:
    """본문을 문장 단위로 자른다. 빈 조각은 버린다."""
    return [s.strip() for s in _SENTENCE.findall(text) if s.strip()]


def pick(text: str, indicator: str) -> str | None:
    """그 지표의 근거 문장을 준다. 없으면 None.

    세 조건을 모두 만족해야 한다 — 지표 언급, 인과 표지, 전망 표지. 하나라도
    없으면 근거로 보지 않는다. 느슨하게 잡으면 엉뚱한 문장이 기관의 근거로
    남고, 그 잘못은 그럴듯해서 아무도 의심하지 않는다. 빡빡한 쪽으로 틀린다.
    """
    words = INDICATOR_WORDS.get(indicator, ())
    for sentence in sentences(text):
        lowered = sentence.lower()
        if not any(w.lower() in lowered for w in words):
            continue
        if not any(c.lower() in lowered for c in _CAUSE):
            continue
        if not any(f.lower() in lowered for f in _FORECAST):
            continue
        return sentence
    return None
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_rationale.py -q`
Expected: PASS (7 passed)

주의 — `test_pick_rejects_a_sentence_about_another_indicator` 가 실패하면
`emp_change` 의 표제어에 `고용`이 있어 "고용시장"·"고령층" 같은 낱말을 잘못 잡는
것이다. 그 경우 표제어를 좁히지 말고 **테스트의 문장이 정말 근거가 아닌지**부터
다시 본다.

- [ ] **Step 5: 커밋**

```bash
git add domains/forecast/pipeline/rationale.py domains/forecast/tests/test_rationale.py
git commit -m "feat(forecast): 전망 근거 문장을 고르는 규칙"
```

---

## Task 2: 태그 부여

**Files:**
- Modify: `domains/forecast/pipeline/rationale.py`
- Modify: `domains/forecast/tests/test_rationale.py`

**Interfaces:**
- Produces: `rationale.tags_for(sentence: str) -> list[str]`, `rationale.TAG_WORDS: dict[str, tuple[str, ...]]`

- [ ] **Step 1: 실패 테스트를 쓴다**

```python
def test_tags_come_only_from_words_in_the_sentence():
    s = "반도체 수출 호조와 설비투자 확대가 성장세를 뒷받침할 것으로 전망된다."
    assert rationale.tags_for(s) == ["수출", "설비투자"]


def test_tags_are_empty_when_nothing_matches():
    assert rationale.tags_for("경기 흐름이 이어질 것으로 전망된다.") == []


def test_tags_do_not_guess_beyond_the_sentence():
    # '반도체' 는 수출을 함의하지만 태그 낱말이 아니다 — 지어내지 않는다
    assert rationale.tags_for("반도체 경기가 회복될 것으로 예상된다.") == []


def test_tags_keep_the_declared_order():
    s = "유가 하락과 내수 회복이 물가를 낮출 것으로 전망된다."
    assert rationale.tags_for(s) == ["내수", "유가"]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_rationale.py -q -k tags`
Expected: FAIL — `AttributeError: ... has no attribute 'tags_for'`

- [ ] **Step 3: 구현한다**

기획서 3.3 의 16개 체계를 그대로 쓴다. 순서는 그 표의 순서다.

```python
# 기획서 3.3 의 태그 체계. 순서를 표와 같게 두어 화면에서 늘 같은 차례로 보인다.
TAG_WORDS: dict[str, tuple[str, ...]] = {
    "수출": ("수출", "export"),
    "글로벌경기": ("글로벌", "세계경제", "global"),
    "환율": ("환율", "exchange rate"),
    "통상정책": ("통상", "관세", "tariff"),
    "내수": ("내수", "민간소비", "domestic demand"),
    "건설투자": ("건설투자", "건설경기"),
    "설비투자": ("설비투자"),
    "재정정책": ("재정", "추경", "fiscal"),
    "통화정책": ("통화정책", "금리", "monetary"),
    "인구구조": ("인구구조", "생산가능인구", "고령"),
    "돌봄일자리": ("돌봄", "보건복지"),
    "제조업고용": ("제조업",),
    "건설업고용": ("건설업",),
    "유가": ("유가", "oil price"),
    "농산물": ("농산물",),
    "공공요금": ("공공요금",),
}


def tags_for(sentence: str) -> list[str]:
    """인용한 문장 안의 낱말에서만 태그를 뽑는다.

    태그는 그 문장에 붙이는 이름표이지 추론이 아니다. '반도체' 가 수출을
    함의하더라도 문장에 '수출' 이 없으면 붙이지 않는다 — 지어낸 태그로 거른
    결과는 사용자가 검증할 방법이 없다.
    """
    lowered = sentence.lower()
    return [tag for tag, words in TAG_WORDS.items()
            if any(w.lower() in lowered for w in words)]
```

주의 — `"설비투자": ("설비투자")` 는 괄호만으로는 튜플이 아니라 문자열이다.
`("설비투자",)` 로 쉼표를 넣는다. 위 코드에 일부러 남긴 함정이 아니라 실수하기
쉬운 곳이니 확인하고 고친다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_rationale.py -q`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add domains/forecast/pipeline/rationale.py domains/forecast/tests/test_rationale.py
git commit -m "feat(forecast): 인용 문장에서 근거 태그를 뽑는다"
```

---

## Task 3: `rationales.json` 저장소

**Files:**
- Create: `domains/forecast/pipeline/rationale_store.py`
- Test: `domains/forecast/tests/test_rationale_store.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `rationale_store.Rationale` — pydantic 모델(`org`, `published_at`, `indicator`, `text`, `tags`, `source_url`, `source_page`)
  - `rationale_store.load(path) -> list[Rationale]`
  - `rationale_store.save(path, items) -> None`
  - `rationale_store.merge(existing, new) -> list[Rationale]`

- [ ] **Step 1: 실패 테스트를 쓴다**

```python
from datetime import date

from domains.forecast.pipeline import rationale_store as rs


def _item(indicator="emp_change", text="가", **kw):
    base = dict(org="KDI", published_at=date(2026, 8, 19), indicator=indicator,
                text=text, tags=[], source_url="https://x/y.pdf", source_page=5)
    base.update(kw)
    return rs.Rationale(**base)


def test_merge_adds_a_new_item():
    got = rs.merge([], [_item()])
    assert len(got) == 1


def test_merge_does_not_overwrite_an_existing_item():
    # 사람이 고친 문장을 다음 수집이 지우면 안 된다
    kept = _item(text="사람이 고친 문장")
    got = rs.merge([kept], [_item(text="수집기가 새로 뽑은 문장")])
    assert len(got) == 1
    assert got[0].text == "사람이 고친 문장"


def test_merge_keys_on_org_date_and_indicator():
    got = rs.merge([_item(indicator="emp_change")], [_item(indicator="cpi")])
    assert {r.indicator for r in got} == {"emp_change", "cpi"}


def test_save_and_load_round_trip(tmp_path):
    path = tmp_path / "rationales.json"
    rs.save(path, [_item(tags=["수출"])])
    got = rs.load(path)
    assert got[0].tags == ["수출"]
    assert got[0].published_at == date(2026, 8, 19)


def test_load_returns_empty_when_the_file_is_absent(tmp_path):
    assert rs.load(tmp_path / "none.json") == []
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_rationale_store.py -q`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 구현한다**

`store.py` 의 모양을 따른다 — 같은 도메인의 두 저장소가 다르게 생기면 읽는 사람이
헷갈린다.

```python
"""근거를 한 번만 저장한다.

레코드마다 복사하지 않는다. 같은 문장이 연간·상반기·하반기에 흩어지면 문장을
손볼 때 몇 군데를 고쳐야 하는지 알 수 없다. 여기서는 (기관, 발표일, 지표) 하나에
한 줄이라 사람이 직접 열어 고칠 수 있다.
"""
from __future__ import annotations

import json
from datetime import date
from pathlib import Path

from pydantic import BaseModel, Field


class Rationale(BaseModel):
    org: str
    published_at: date
    indicator: str
    text: str
    tags: list[str] = Field(default_factory=list)
    source_url: str
    source_page: int | None = None

    @property
    def key(self) -> tuple[str, date, str]:
        return (self.org, self.published_at, self.indicator)


def load(path: Path | str) -> list[Rationale]:
    p = Path(path)
    if not p.exists():
        return []
    return [Rationale.model_validate(row)
            for row in json.loads(p.read_text(encoding="utf-8"))]


def save(path: Path | str, items: list[Rationale]) -> None:
    ordered = sorted(items, key=lambda r: (r.published_at.isoformat(), r.org, r.indicator))
    Path(path).write_text(
        json.dumps([r.model_dump(mode="json") for r in ordered],
                   ensure_ascii=False, indent=2) + "\n",
        encoding="utf-8")


def merge(existing: list[Rationale], new: list[Rationale]) -> list[Rationale]:
    """없는 것만 더한다.

    이미 있는 항목은 그대로 둔다 — 사람이 문장을 다듬었을 수 있고, 재수집이
    그것을 조용히 되돌리면 손댈 이유가 사라진다.
    """
    seen = {r.key for r in existing}
    return list(existing) + [r for r in new if r.key not in seen]
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_rationale_store.py -q`
Expected: PASS (5 passed)

- [ ] **Step 5: 커밋**

```bash
git add domains/forecast/pipeline/rationale_store.py domains/forecast/tests/test_rationale_store.py
git commit -m "feat(forecast): 근거를 한 번만 저장하는 rationales.json"
```

---

## Task 4: 수집기가 본문을 넘긴다

**Files:**
- Modify: `domains/forecast/pipeline/report.py`
- Modify: `domains/forecast/pipeline/collectors/keis.py`
- Test: `domains/forecast/tests/test_keis.py`

**Interfaces:**
- Consumes: `rationale.pick`, `rationale.tags_for`, `rationale_store.Rationale`
- Produces: `report.rationales_from_text(text, *, org, issue, indicators, source_url, source_page) -> list[Rationale]`

한 기관(KEIS)으로 먼저 붙인다. 통하면 Task 5 가 나머지에 같은 틀을 넓힌다.

- [ ] **Step 1: 실패 테스트를 쓴다**

`domains/forecast/tests/test_keis.py` 에 이어 붙인다. `keis_no_forecast.txt` 는
이미 있는 픽스처로, KEIS 2026년 제5호의 장 도입부 서술이다.

```python
def test_rationales_from_text_finds_one_per_indicator():
    from domains.forecast.pipeline import report

    text = ("2026년 하반기 취업자 수 증가 배경에는 경기 개선 기대가 자리한 것으로 전망된다. "
            "실업률은 내수 회복을 반영해 하락할 것으로 예상된다.")
    got = report.rationales_from_text(
        text, org="KEIS", issue=ISSUE_2026_08,
        indicators=("emp_change", "emp_rate", "unemp_rate"),
        source_url="https://x/y.pdf", source_page=20)

    by_ind = {r.indicator: r for r in got}
    assert set(by_ind) == {"emp_change", "unemp_rate"}   # emp_rate 는 근거가 없다
    assert by_ind["emp_change"].text.startswith("2026년 하반기 취업자 수 증가 배경")
    assert by_ind["emp_change"].published_at == ISSUE_2026_08.published_at
    assert by_ind["emp_change"].source_page == 20


def test_rationales_from_text_returns_empty_when_nothing_qualifies():
    from domains.forecast.pipeline import report

    got = report.rationales_from_text(
        "표는 다음과 같다.", org="KEIS", issue=ISSUE_2026_08,
        indicators=("emp_change",), source_url="https://x/y.pdf", source_page=1)
    assert got == []
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_keis.py -q -k rationales`
Expected: FAIL — `AttributeError: ... has no attribute 'rationales_from_text'`

- [ ] **Step 3: 구현한다**

`report.py` 에 이어 붙인다. 여기 두는 이유는 `records_from_values` 와 같은 자리 —
회차 정보를 레코드로 옮기는 곳이기 때문이다.

```python
def rationales_from_text(text: str, *, org: str, issue: Issue,
                         indicators: Iterable[str], source_url: str,
                         source_page: int | None) -> list["Rationale"]:
    """본문에서 지표별 근거 문장을 뽑는다. 없는 지표는 건너뛴다."""
    from . import rationale
    from .rationale_store import Rationale

    out = []
    for indicator in indicators:
        sentence = rationale.pick(text, indicator)
        if sentence is None:
            continue
        out.append(Rationale(
            org=org, published_at=issue.published_at, indicator=indicator,
            text=sentence, tags=rationale.tags_for(sentence),
            source_url=source_url, source_page=source_page,
        ))
    return out
```

- [ ] **Step 4: KEIS 수집기가 본문을 함께 준다**

`keis.collect_issue` 가 전망표 쪽 원문을 이미 갖고 있다. 그 쪽에는 표뿐 아니라
서술도 함께 있으므로 같은 텍스트를 넘긴다. 반환값을
`tuple[list[ForecastRecord], list[Rationale]]` 로 바꾸지 말고, **모듈 함수를 하나
더 노출한다** — 기존 호출부를 건드리지 않기 위해서다.

```python
def collect_issue_rationales(listed: ListedIssue, *, fetch=None,
                             read_pages=None) -> list["Rationale"]:
    """그 회차의 근거 문장을 준다. 전망표가 없으면 빈 리스트."""
```

본문은 표 쪽 하나로 충분하지 않을 수 있다 — KEIS 는 표 앞쪽에 근거 서술이 있다.
전망표 쪽과 그 **앞 쪽**을 합쳐 넘긴다.

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest domains/forecast -q`
Expected: PASS

- [ ] **Step 6: 실제 회차로 확인한다**

Run:
```bash
PATH="/c/Program Files/Tesseract-OCR:$PATH" python -c "
import sys; sys.path.insert(0,'.'); sys.stdout.reconfigure(encoding='utf-8')
from domains.forecast.pipeline.collectors import keis
for r in keis.collect_issue_rationales(keis.list_issues()[0]):
    print(r.indicator, '|', r.tags, '|', r.text[:70])
"
```

Expected: 지표 한둘에 실제 문장이 붙는다. 하나도 안 나오면 규칙이 지나치게
빡빡한 것이니 **문장을 눈으로 보고** 보고한다 — 규칙을 임의로 느슨하게 하지
않는다.

- [ ] **Step 7: 커밋**

```bash
git add domains/forecast/pipeline/report.py domains/forecast/pipeline/collectors/keis.py domains/forecast/tests/test_keis.py
git commit -m "feat(forecast): KEIS 회차에서 지표별 근거 문장을 뽑는다"
```

---

## Task 5: 나머지 기관으로 넓힌다

**Files:**
- Modify: `collectors/bok.py`, `kdi.py`, `kli.py`, `kiet.py`, `imf.py`, `oecd.py`, `oecd_interim.py`
- Test: 각 기관 테스트 파일

각 수집기가 이미 표 페이지 원문을 갖고 있다. Task 4 와 같은 모양으로
`collect_issue_rationales` 를 노출한다.

이 태스크는 **조사가 먼저**다. 기관마다 근거 서술이 어디 있는지 모른 채 코드를
쓰면 빈 결과만 나온다. 조사 결과를 보고한 뒤 구현한다.

- [ ] **Step 1: 기관마다 근거가 실린 곳을 찾는다**

각 기관의 기존 픽스처(`domains/forecast/tests/fixtures/`)를 열고, 그 안에서
`rationale.pick` 이 무엇을 고르는지 실제로 돌려 본다.

```bash
python -c "
import sys,io,glob; sys.path.insert(0,'.'); sys.stdout.reconfigure(encoding='utf-8')
from domains.forecast.pipeline import rationale
for f in sorted(glob.glob('domains/forecast/tests/fixtures/*.txt')):
    t=io.open(f,encoding='utf-8').read()
    hits={i: rationale.pick(t,i) for i in ('gdp_growth','cpi','emp_change','unemp_rate')}
    hits={k:v for k,v in hits.items() if v}
    print(f.split('/')[-1], '->', len(hits), '개')
    for k,v in hits.items(): print('    ', k, ':', v[:70])
"
```

이 결과로 표를 채워 **보고한다.** 픽스처가 표 쪽만 담고 있어 근거가 안 나오는
기관은, 그 기관의 보고서에서 서술이 실린 장·쪽을 확인해 적는다.

| 기관 | 근거가 실린 곳 | 픽스처가 그 텍스트를 담고 있나 |
|---|---|---|
| BOK | | |
| KDI | | |
| KLI | | |
| KIET | | |
| IMF | | |
| OECD | | |

픽스처가 담고 있지 않으면 그 기관의 실제 보고서에서 해당 텍스트를 따 **새
픽스처로 추가한다.** 네트워크를 타는 테스트를 만들지 않는다.

- [ ] **Step 2: 기관마다 테스트를 먼저 쓴다**

Step 1 에서 확인한 문장을 그대로 단언한다. 예시 형태는 다음과 같다 — 실제
문장은 픽스처에서 딴 것으로 바꾼다.

```python
def test_bok_rationale_comes_from_the_report():
    from domains.forecast.pipeline import report

    got = report.rationales_from_text(
        BOK_SUMMARY_TEXT, org="BOK", issue=BOK_ISSUE,
        indicators=("gdp_growth", "cpi", "emp_change", "unemp_rate"),
        source_url="https://x/y.pdf", source_page=3)
    by_ind = {r.indicator: r.text for r in got}
    # 아래 문장은 픽스처에서 그대로 딴 것이다 — 지어내지 않는다
    assert by_ind["gdp_growth"] == "<픽스처에서 딴 실제 문장>"
```

**문장을 지어내지 않는다.** 픽스처에 없는 문장을 기대값으로 쓰면 테스트가
현실과 무관해진다.

- [ ] **Step 3: 구현하고 통과를 확인한다**

Run: `python -m pytest domains/forecast -q`

- [ ] **Step 4: 추출률을 보고한다**

회차 32개 × 지표별로 근거가 몇 개나 붙었는지 센다. 설계 3.2 가 요구한 로그다.

```bash
python -c "
import sys; sys.path.insert(0,'.')
# 각 기관의 픽스처로 pick 을 돌려 지표별 성공/실패를 센다
"
```

추출률이 지나치게 낮으면(예: 절반 이상 비어 있으면) **규칙을 고치지 말고 보고**
한다. 규칙을 느슨하게 할지는 사람이 판단한다.

- [ ] **Step 5: 커밋**

```bash
git add domains/forecast/pipeline/collectors/ domains/forecast/tests/
git commit -m "feat(forecast): 나머지 기관에서도 근거 문장을 뽑는다"
```

---

## Task 6: 파이프라인이 근거를 저장한다

**Files:**
- Modify: `domains/forecast/pipeline/collect.py`
- Modify: `domains/forecast/pipeline/backfill.py`
- Test: `domains/forecast/tests/test_collect.py`, `test_backfill.py`

- [ ] **Step 1: 실패 테스트를 쓴다**

`collect.main` 은 이미 `collectors` 를 주입받는다. 근거 추출자도 같은 방식으로
`rationale_collectors` 를 주입받게 해 네트워크를 타지 않는다.

```python
from datetime import date, datetime

from domains.forecast.pipeline import rationale_store as rs


def _one_record():
    return ForecastRecord(
        id="kdi-2026-08-gdp_growth-2026", org="KDI", org_name_ko="KDI",
        report_title="KDI 경제전망, 2026년 8월", published_at=date(2026, 8, 19),
        target_year=2026, target_period="annual", indicator="gdp_growth",
        value=3.2, unit="%", source_url="https://x/y.pdf", landing_url="https://x/",
        confidence="extracted", collected_at=datetime(2026, 8, 20),
    )


def _one_rationale(text="수출 호조가 성장을 뒷받침할 것으로 전망된다."):
    return rs.Rationale(
        org="KDI", published_at=date(2026, 8, 19), indicator="gdp_growth",
        text=text, tags=["수출"], source_url="https://x/y.pdf", source_page=5)


def test_main_saves_rationales_next_to_records(tmp_path):
    collect.main(
        data_dir=tmp_path,
        collectors={"kdi": lambda today: [_one_record()]},
        rationale_collectors={"kdi": lambda today: [_one_rationale()]},
    )
    assert len(store.load_forecasts(tmp_path / "forecasts.json")) == 1
    saved = rs.load(tmp_path / "rationales.json")
    assert [r.indicator for r in saved] == ["gdp_growth"]


def test_main_keeps_an_existing_rationale(tmp_path):
    # 사람이 고친 문장을 재수집이 덮지 않는다
    rs.save(tmp_path / "rationales.json", [_one_rationale("사람이 고친 문장")])
    collect.main(
        data_dir=tmp_path,
        collectors={"kdi": lambda today: [_one_record()]},
        rationale_collectors={"kdi": lambda today: [_one_rationale("수집기가 새로 뽑은 문장")]},
    )
    assert rs.load(tmp_path / "rationales.json")[0].text == "사람이 고친 문장"


def test_a_failing_rationale_does_not_fail_the_collector(tmp_path):
    # 수치가 본체다. 근거 때문에 수치를 잃지 않는다.
    def boom(today):
        raise ValueError("본문을 못 받았다")

    collect.main(
        data_dir=tmp_path,
        collectors={"kdi": lambda today: [_one_record()]},
        rationale_collectors={"kdi": boom},
    )
    assert len(store.load_forecasts(tmp_path / "forecasts.json")) == 1
    summary = json.loads((tmp_path / "last_run.json").read_text(encoding="utf-8"))
    assert any("kdi" in e for e in summary["errors"])
    assert summary["collectors"]["kdi"]["ok"] is True   # 수치 수집은 성공이다
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_collect.py -q -k rationale`

- [ ] **Step 3: 구현한다**

`collect.main` 이 수집기마다 근거도 모아 `rationale_store.merge` 로 합치고
`rationales.json` 에 저장한다. **근거 추출 실패는 그 수집기의 수치 수집을
실패시키지 않는다** — `try/except` 로 가두고 `last_run.json` 에 한 줄 남긴다.

`backfill.run` 도 같게 한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast -q`

- [ ] **Step 5: 커밋**

```bash
git add domains/forecast/pipeline/collect.py domains/forecast/pipeline/backfill.py domains/forecast/tests/
git commit -m "feat(forecast): 수집이 근거를 함께 저장한다"
```

---

## Task 7: 모델에서 죽은 필드를 뺀다

**Files:**
- Modify: `domains/forecast/pipeline/models.py`
- Modify: `domains/forecast/tests/test_models.py`

- [ ] **Step 1: 실패 테스트를 쓴다**

```python
def test_record_no_longer_carries_a_rationale_field():
    # 근거는 rationales.json 이 정본이다. 두 곳에 두면 어느 쪽이 맞는지 모호해진다
    assert "rationale" not in ForecastRecord.model_fields
    assert "rationale_tags" not in ForecastRecord.model_fields
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_models.py -q -k rationale`
Expected: FAIL

- [ ] **Step 3: 두 필드를 지운다**

`models.py` 의 `rationale`·`rationale_tags` 를 지운다. 기존 데이터는 전부 빈
값이라 잃을 것이 없다. `store.save_forecasts` 가 `model_dump` 로 쓰므로 다음
저장에서 키가 사라진다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast -q`
Expected: PASS. 기존 데이터를 읽을 때 pydantic 이 모르는 키를 만나 실패하면
`model_config` 에 `extra="ignore"` 가 있는지 확인한다.

- [ ] **Step 5: 커밋**

```bash
git add domains/forecast/pipeline/models.py domains/forecast/tests/test_models.py
git commit -m "refactor(forecast): 레코드에서 빈 근거 필드를 뺀다"
```

---

## Task 8: 화면이 근거를 붙인다

**Files:**
- Modify: `domains/forecast/app/js/app.js`, `js/data.js`
- Modify: `js/screens/home.js`, `screens/org.js`
- Test: `domains/forecast/tests/web/data.test.mjs`

- [ ] **Step 1: 실패 테스트를 쓴다**

```javascript
test('근거는 기관·발표일·지표로 찾는다', () => {
  const items = [{ org: 'KDI', published_at: '2026-08-19', indicator: 'gdp_growth',
                   text: '수출 호조', tags: ['수출'] }];
  const rec = { org: 'KDI', published_at: '2026-08-19', indicator: 'gdp_growth' };
  assert.equal(rationaleFor(rec, items).text, '수출 호조');
});

test('같은 지표의 반기 레코드도 같은 근거를 가리킨다', () => {
  const items = [{ org: 'KDI', published_at: '2026-08-19', indicator: 'gdp_growth',
                   text: '수출 호조', tags: [] }];
  const h2 = { org: 'KDI', published_at: '2026-08-19', indicator: 'gdp_growth',
               target_period: 'h2' };
  assert.equal(rationaleFor(h2, items).text, '수출 호조');
});

test('없으면 null 을 준다', () => {
  assert.equal(rationaleFor({ org: 'BOK', published_at: '2026-01-01',
                              indicator: 'cpi' }, []), null);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test "domains/forecast/tests/web/*.mjs"`

- [ ] **Step 3: 구현한다**

`data.js` 에 `rationaleFor(rec, items)` 를 더하고 export 한다. `app.js` 가
`./data/rationales.json` 을 함께 받아 `ctx` 에 넣는다.

`home.js`·`org.js` 의 `rationaleText` 가 `rec.rationale` 대신 이 함수를 쓴다.
**못 찾으면 지금처럼 보고서 제목으로 떨어진다** — 근거 없는 회차가 정상이다.

`org.js` 의 태그 자리에 `tags` 를 그린다.

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test "core/tests/*.mjs" "hub/tests/*.mjs" "domains/*/tests/web/*.mjs"`
Expected: PASS

- [ ] **Step 5: 빌드가 도는지 본다**

Run: `python -m tools.build`
Expected: 성공. `_site/forecast/data/rationales.json` 이 생긴다(빌드가 `data/` 를
통째로 복사한다).

- [ ] **Step 6: 커밋**

```bash
git add domains/forecast/app/js domains/forecast/tests/web
git commit -m "feat(forecast): 화면이 근거와 태그를 붙인다"
```

---

## Task 9: 소급 실행과 마무리 검증

**Files:** 없음 (실행·검증만). 데이터 커밋은 컨트롤러가 한다.

- [ ] **Step 1: 백필로 근거를 채운다**

Run: `PATH="/c/Program Files/Tesseract-OCR:$PATH" python -m domains.forecast.pipeline.backfill`

몇 분 걸린다. 회차 32개를 다시 읽는다.

- [ ] **Step 2: 수치가 안 바뀌었는지 확인한다**

```bash
python -c "
import json,subprocess
old=json.loads(subprocess.run(['git','show','HEAD:domains/forecast/data/forecasts.json'],
                              capture_output=True,text=True,encoding='utf-8').stdout)
new=json.load(open('domains/forecast/data/forecasts.json',encoding='utf-8'))
a={(r['id'],r['value']) for r in old}; b={(r['id'],r['value']) for r in new}
print('레코드 수', len(old), '->', len(new), '| 수치 동일:', a==b)
"
```

Expected: `수치 동일: True`. 아니면 **멈추고 보고한다.**

- [ ] **Step 3: 근거 현황을 본다**

```bash
python -c "
import json,sys; sys.stdout.reconfigure(encoding='utf-8')
from collections import Counter
items=json.load(open('domains/forecast/data/rationales.json',encoding='utf-8'))
print('근거', len(items), '건')
print('기관별:', dict(Counter(r['org'] for r in items).most_common()))
print('지표별:', dict(Counter(r['indicator'] for r in items).most_common()))
print('태그 붙은 비율:', sum(1 for r in items if r['tags']), '/', len(items))
print()
for r in items[:5]: print(f\"  {r['org']} {r['published_at']} {r['indicator']}: {r['text'][:60]}\")
"
```

- [ ] **Step 4: 전체 테스트**

Run: `python -m pytest -q` 와 `node --test "core/tests/*.mjs" "hub/tests/*.mjs" "domains/*/tests/web/*.mjs"`

- [ ] **Step 5: 보고**

근거 건수·기관별 분포·예시 문장 몇 개를 보고한다. **데이터 파일은 커밋하지
않는다** — 컨트롤러가 문장을 눈으로 보고 커밋한다. 근거는 사람이 읽을 문장이라
기계 검증만으로는 부족하다.
