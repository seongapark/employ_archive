# 전망 근거 LLM 선별 전환 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 전망 근거를 규칙 대신 LLM 이 고르게 하되, 고른 문장이 원문에 실재하는지 프로그램으로 대조해 지어낸 문장이 저장되지 않게 한다.

**Architecture:** LLM 은 **선별자**다 — 요약하지 않고 원문 문장을 지목한다. 검증은 호출과 분리된 순수 함수로, 공백을 지운 후보가 공백을 지운 원문의 부분열인지만 본다. 전체는 CI 가 아니라 `tools/` 아래 사람이 돌리는 도구다.

**Tech Stack:** Python 3.12, pydantic v2, requests(신규 의존성 없음 — Anthropic API 를 HTTP 로 직접 부른다), pytest.

**Spec:** `docs/superpowers/specs/2026-09-01-전망근거-LLM선별-design.md`

## Global Constraints

- **새 의존성을 추가하지 않는다.** `requirements.txt` 를 건드리지 않는다. Anthropic API 는 이미 있는 `requests` 로 호출한다. CI 가 이 도구를 부르지 않으므로 SDK 를 CI 에 설치시킬 이유가 없다.
- **`.github/workflows/` 를 한 줄도 건드리지 않는다.** 이것이 "사이트 운영에 지장 없음" 의 구조적 보장이다.
- **테스트가 네트워크를 타지 않는다.** LLM 호출부는 주입 가능한 인자로 받고, 테스트는 녹화된 응답을 재생한다.
- **`domains/forecast/data/` 를 구현자가 커밋하지 않는다.** 컨트롤러가 도구를 실행하고 데이터를 커밋한다.
- 모델 ID: `claude-sonnet-5`. API 주소: `https://api.anthropic.com/v1/messages`. 키: 환경변수 `ANTHROPIC_API_KEY`.
- 주석·docstring 은 한국어. 기존 `domains/forecast/pipeline/` 의 문체를 따른다(규칙마다 실측 근거를 옆에 적는다).
- `git add -A` · `git add .` · `git commit -a` 금지. 경로를 명시하고 커밋 전 `git diff --cached --name-only` 로 확인한다.

## File Structure

| 파일 | 책임 |
|---|---|
| `domains/forecast/pipeline/llm_verify.py` (신규) | 후보 문장이 원문에 실재하는지 대조 + 기존 방어 재사용. **네트워크를 모른다.** |
| `domains/forecast/pipeline/llm_select.py` (신규) | 프롬프트 구성 · HTTP 호출 · 응답 파싱. 검증을 모른다. |
| `domains/forecast/pipeline/documents.py` (신규) | 여섯 출처의 회차 목록과 본문 획득을 한 모양으로 묶는다 |
| `tools/rationales.py` (신규) | CLI. 위 셋을 엮고 `rationale_store` 에 저장 |
| `domains/forecast/pipeline/rationale.py` (수정) | 선별·분리 장치 삭제, 검증용 방어만 남김 |
| `domains/forecast/pipeline/collect.py` · `backfill.py` (수정) | 근거 수집 배선 제거 |
| 수집기 6개 (수정) | `collect_issue_rationales` 제거 |

`llm_verify` 와 `llm_select` 를 나누는 이유는 **검증이 호출을 몰라야** 하기 때문이다. 검증은 두 문자열만 받는 순수 함수라 네트워크 없이 전부 시험할 수 있고, 모델을 바꾸거나 사람이 손으로 넣은 문장에도 같은 검사가 걸린다.

---

### Task 1: 검증 — 원문 대조

**Files:**
- Create: `domains/forecast/pipeline/llm_verify.py`
- Test: `domains/forecast/tests/test_llm_verify.py`

**Interfaces:**
- Consumes: `rationale.tags_for`, `rationale._MAX_RATIONALE_LENGTH`, `rationale._DUPLICATED_HANGUL` (모두 기존)
- Produces:
  - `llm_verify.normalize(s: str) -> str`
  - `llm_verify.Rejected(Exception)` — `reason: str` 속성을 갖는다
  - `llm_verify.verify(candidate: str, source_page_text: str) -> str` — 통과하면 candidate 를 그대로 돌려주고, 아니면 `Rejected` 를 던진다

- [x] **Step 1: 실패 테스트를 쓴다**

```python
import pytest

from domains.forecast.pipeline import llm_verify as v

PAGE = ("하반기에는 건설경기 부진 완화와 내수 회복이 점진적으로 반영되고,\n"
        "상반기 고용 증가를 주도한 보건복지업 및 대면 서비스업의 노동 수요\n"
        "역시 지속될 것으로 예상")


def test_accepts_a_sentence_that_is_in_the_page():
    got = v.verify("하반기에는 건설경기 부진 완화와 내수 회복이 점진적으로 반영되고,", PAGE)
    assert got == "하반기에는 건설경기 부진 완화와 내수 회복이 점진적으로 반영되고,"


def test_accepts_across_a_line_wrap():
    # 원문은 세 줄이지만 LLM 은 한 문장으로 이어 돌려준다. 공백을 지우면 같다.
    joined = ("하반기에는 건설경기 부진 완화와 내수 회복이 점진적으로 반영되고, 상반기 고용 "
              "증가를 주도한 보건복지업 및 대면 서비스업의 노동 수요 역시 지속될 것으로 예상")
    assert v.verify(joined, PAGE) == joined


def test_accepts_closing_a_wrap_artifact_space():
    # pdfplumber 가 낱말 중간에 넣은 공백을 LLM 이 붙여 써도 통과한다
    page = "에너지 가격 상승이 물가 부담으로 작용하 고, 회복세를 제한한다"
    assert v.verify("물가 부담으로 작용하고, 회복세를 제한한다", page)


def test_rejects_a_word_that_is_not_in_the_page():
    with pytest.raises(v.Rejected) as e:
        v.verify("하반기에는 건설경기 호황과 내수 회복이 반영되고,", PAGE)
    assert "원문에 없다" in e.value.reason


def test_rejects_an_invented_sentence():
    with pytest.raises(v.Rejected):
        v.verify("정부의 확장적 재정 기조가 고용을 뒷받침할 것으로 보인다.", PAGE)


def test_rejects_two_sentences_stitched_from_different_places():
    page = "가나다 라마바. 사아자 차카타."
    with pytest.raises(v.Rejected):
        v.verify("가나다 차카타.", page)


def test_rejects_a_candidate_over_the_length_limit():
    long_source = "가" * 400
    with pytest.raises(v.Rejected) as e:
        v.verify("가" * 350, long_source)
    assert "길다" in e.value.reason


def test_rejects_bold_overprint_debris():
    page = "낙낙낙관관관시시시나나나리리리오오오 중국과 진행 중인 협상이 타결될 것으로 예상"
    with pytest.raises(v.Rejected) as e:
        v.verify("낙낙낙관관관시시시나나나리리리오오오 중국과 진행 중인 협상이", page)
    assert "렌더링" in e.value.reason
```

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_llm_verify.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'domains.forecast.pipeline.llm_verify'`

- [x] **Step 3: 구현한다**

```python
"""LLM 이 고른 문장이 원문에 실재하는지 대조한다.

이 모듈은 네트워크를 모른다 — (후보, 원문) 두 문자열만 받는 순수 함수라
모델을 바꾸거나 사람이 손으로 넣은 문장에도 같은 검사가 걸린다.

핵심은 공백을 지우고 대조하는 것이다. 화면에는 "이 기관은 이렇게 전망했다"
로 뜨므로, LLM 이 매끄럽게 다듬은 문장은 그럴듯해서 아무도 의심하지 않는
최악의 실패가 된다. 낱말을 하나라도 바꾸면 이 대조에 걸린다.

공백을 무시하는 이유는 두 가지다. 원문 한 문장이 여러 줄에 걸쳐 있어도
이어 붙인 답이 통과해야 하고, pdfplumber 가 낱말 중간에 넣은 공백
("작용하 고,")을 LLM 이 붙여 쓴 것은 오히려 개선이라 막을 이유가 없다.
낱말 자체를 바꾸는 것과는 다른 일이다.
"""
from __future__ import annotations

import re

from .rationale import (_DUPLICATED_HANGUL, _MAX_RATIONALE_LENGTH)

_WHITESPACE = re.compile(r"\s+")


class Rejected(Exception):
    """후보를 저장하지 않는다. reason 은 로그에 그대로 남긴다."""

    def __init__(self, reason: str) -> None:
        super().__init__(reason)
        self.reason = reason


def normalize(s: str) -> str:
    """공백을 전부 지운다. 줄바꿈도 공백이라 함께 사라진다."""
    return _WHITESPACE.sub("", s)


def verify(candidate: str, source_page_text: str) -> str:
    """통과하면 candidate 를 그대로 돌려준다. 아니면 Rejected."""
    if not candidate.strip():
        raise Rejected("빈 문장이다")
    if len(candidate) > _MAX_RATIONALE_LENGTH:
        raise Rejected(f"{_MAX_RATIONALE_LENGTH}자보다 길다({len(candidate)}자)")
    if _DUPLICATED_HANGUL.search(candidate):
        raise Rejected("굵은 글씨 반복 렌더링 흔적이 들어 있다")
    if normalize(candidate) not in normalize(source_page_text):
        raise Rejected("원문에 없다")
    return candidate
```

- [x] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_llm_verify.py -q`
Expected: PASS (8 passed)

- [x] **Step 5: 보장을 일부러 망가뜨린다**

세 가지를 각각 망가뜨리고 실패를 본 뒤 되돌린다. 관찰한 단언문을 보고한다.

1. `normalize` 를 항등함수(`return s`)로 → 줄 감김 테스트가 실패해야 한다
2. 부분열 검사(`not in`)를 제거 → 지어낸 문장 테스트가 실패해야 한다
3. 길이 검사를 제거 → 길이 테스트가 실패해야 한다

어느 하나라도 실패가 안 나면 그 테스트가 아무것도 안 지키는 것이니 **코드가 아니라 테스트를 고친다.**

- [x] **Step 6: 커밋**

```bash
git add domains/forecast/pipeline/llm_verify.py domains/forecast/tests/test_llm_verify.py
git commit -m "feat(forecast): LLM 이 고른 문장을 원문과 대조한다"
```

---

### Task 2: 선별 — 프롬프트와 응답 파싱

**Files:**
- Create: `domains/forecast/pipeline/llm_select.py`
- Test: `domains/forecast/tests/test_llm_select.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `llm_select.Picked(NamedTuple)` — `indicator: str`, `text: str`, `source_page: int`
  - `llm_select.build_prompt(org: str, title: str, indicators: Sequence[str], pages: Sequence[str]) -> str`
  - `llm_select.parse_response(body: str) -> list[Picked]`
  - `llm_select.select(org, title, indicators, pages, *, call=None) -> list[Picked]` — `call` 은 `(prompt: str) -> str`. 기본값은 실제 API 호출.
  - `llm_select.MODEL = "claude-sonnet-5"`

- [x] **Step 1: 실패 테스트를 쓴다**

```python
import json

import pytest

from domains.forecast.pipeline import llm_select as s

PAGES = ["표지", "2026년 취업자는 내수 회복으로 늘어날 전망", "각주"]


def test_prompt_carries_page_numbers_so_the_model_can_cite():
    p = s.build_prompt("KDI", "KDI 경제전망 2026", ["emp_change"], PAGES)
    assert "1쪽" in p and "2쪽" in p and "3쪽" in p
    assert "2026년 취업자는 내수 회복으로 늘어날 전망" in p


def test_prompt_names_only_the_indicators_this_org_forecasts():
    p = s.build_prompt("KLI", "노동리뷰", ["emp_change"], PAGES)
    assert "emp_change" in p
    assert "gdp_growth" not in p


def test_prompt_forbids_rewriting():
    p = s.build_prompt("KDI", "t", ["cpi"], PAGES)
    assert "한 글자도" in p


def test_parse_reads_the_json_array():
    body = json.dumps([{"indicator": "emp_change", "text": "가나다", "source_page": 2}],
                      ensure_ascii=False)
    assert s.parse_response(body) == [s.Picked("emp_change", "가나다", 2)]


def test_parse_tolerates_a_fenced_code_block():
    body = "```json\n[{\"indicator\":\"cpi\",\"text\":\"가\",\"source_page\":1}]\n```"
    assert s.parse_response(body) == [s.Picked("cpi", "가", 1)]


def test_parse_drops_an_entry_with_an_empty_text():
    body = json.dumps([{"indicator": "cpi", "text": "", "source_page": 1}])
    assert s.parse_response(body) == []


def test_parse_raises_on_unparsable_output():
    with pytest.raises(ValueError):
        s.parse_response("죄송합니다, 찾지 못했습니다")


def test_select_uses_the_injected_call_and_does_not_touch_the_network():
    seen = {}

    def fake(prompt):
        seen["prompt"] = prompt
        return json.dumps([{"indicator": "emp_change", "text": "가", "source_page": 2}],
                          ensure_ascii=False)

    got = s.select("KDI", "t", ["emp_change"], PAGES, call=fake)
    assert got == [s.Picked("emp_change", "가", 2)]
    assert "KDI" in seen["prompt"]
```

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_llm_select.py -q`
Expected: FAIL — `ModuleNotFoundError`

- [x] **Step 3: 구현한다**

```python
"""보고서 전문을 주고 지표별 근거 문장을 지목하게 한다.

요약시키지 않는다. 원문 문장을 그대로 옮기게 하고, 옮긴 것이 맞는지는
llm_verify 가 따로 대조한다 — 이 모듈은 검증을 모른다.

쪽을 좁히지 않고 전문을 주는 것이 규칙 방식과의 가장 큰 차이다. 규칙 방식은
표 앞뒤 한 쪽씩만 봤는데, 창을 넓히자 한국은행 집필진 명단이 근거로 뽑혔다
(낱말만 보면 조건을 다 만족한다). LLM 은 그것을 근거로 착각하지 않으므로
BOK 의 진짜 서술이 8·10·11·39·43쪽에 흩어져 있어도 닿는다.
"""
from __future__ import annotations

import json
import os
import re
from typing import Callable, NamedTuple, Sequence

import requests

MODEL = "claude-sonnet-5"
API_URL = "https://api.anthropic.com/v1/messages"
MAX_TOKENS = 2000
TIMEOUT = 120

_FENCE = re.compile(r"```(?:json)?\s*(.*?)\s*```", re.S)


class Picked(NamedTuple):
    indicator: str
    text: str
    source_page: int


def build_prompt(org: str, title: str, indicators: Sequence[str],
                 pages: Sequence[str]) -> str:
    body = "\n\n".join(f"[{n}쪽]\n{t}" for n, t in enumerate(pages, start=1))
    wanted = ", ".join(indicators)
    return f"""다음은 {org} 의 「{title}」 전문이다.

지표마다 **그 전망을 그렇게 한 이유를 말하는 문장 하나**를 원문에서 찾아라.

대상 지표: {wanted}

규칙:
- 원문을 **한 글자도** 바꾸지 마라. 다듬거나 요약하면 버려진다.
- 이유를 말하는 문장이 없으면 그 지표는 **비워라.** 빈 칸은 정상이고
  지어낸 근거보다 낫다.
- 수치를 다시 말하는 문장은 근거가 아니다("3.2% 성장할 전망" 은 전망이지
  이유가 아니다).
- 표·목차·집필진 명단·각주·참고문헌은 근거가 아니다.
- 그 문장이 실린 쪽번호를 함께 적어라.

JSON 배열로만 답하라. 다른 말을 덧붙이지 마라:
[{{"indicator": "...", "text": "...", "source_page": 1}}]

{body}
"""


def parse_response(body: str) -> list[Picked]:
    """JSON 배열을 읽는다. 못 읽으면 ValueError — 조용히 넘기지 않는다."""
    text = body.strip()
    fenced = _FENCE.search(text)
    if fenced:
        text = fenced.group(1)
    try:
        rows = json.loads(text)
    except json.JSONDecodeError as exc:
        raise ValueError(f"응답을 JSON 으로 읽지 못했다: {body[:200]}") from exc
    if not isinstance(rows, list):
        raise ValueError(f"배열이 아니다: {body[:200]}")
    out = []
    for row in rows:
        text_ = str(row.get("text", "")).strip()
        if not text_:
            continue  # 근거 없음은 정상이다
        out.append(Picked(str(row["indicator"]), text_, int(row["source_page"])))
    return out


def _call_api(prompt: str) -> str:
    key = os.environ.get("ANTHROPIC_API_KEY", "").strip()
    if not key:
        raise RuntimeError("ANTHROPIC_API_KEY 가 없다")
    resp = requests.post(
        API_URL,
        headers={"x-api-key": key, "anthropic-version": "2023-06-01",
                 "content-type": "application/json"},
        json={"model": MODEL, "max_tokens": MAX_TOKENS,
              "messages": [{"role": "user", "content": prompt}]},
        timeout=TIMEOUT,
    )
    resp.raise_for_status()
    return "".join(b.get("text", "") for b in resp.json()["content"])


def select(org: str, title: str, indicators: Sequence[str], pages: Sequence[str],
           *, call: Callable[[str], str] | None = None) -> list[Picked]:
    call = call or _call_api
    return parse_response(call(build_prompt(org, title, indicators, pages)))
```

- [x] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_llm_select.py -q`
Expected: PASS (8 passed)

- [x] **Step 5: 네트워크를 안 타는지 확인한다**

`requests.post` 를 예외를 던지도록 monkeypatch 한 뒤 `python -m pytest domains/forecast/tests/test_llm_select.py -q` 를 돌린다. 전부 통과해야 한다. 통과하지 않으면 어딘가 실제 호출이 남아 있는 것이다.

- [x] **Step 6: 커밋**

```bash
git add domains/forecast/pipeline/llm_select.py domains/forecast/tests/test_llm_select.py
git commit -m "feat(forecast): 보고서 전문에서 근거 문장을 지목하게 한다"
```

---

### Task 3: 여섯 출처의 본문을 한 모양으로

**Files:**
- Create: `domains/forecast/pipeline/documents.py`
- Test: `domains/forecast/tests/test_documents.py`

**Interfaces:**
- Consumes: 각 수집기의 `list_issues()` / `EDITIONS`, `pdf.page_texts`, `ocr.page_texts`
- Produces:
  - `documents.Listed(NamedTuple)` — `org: str`, `title: str`, `published_at: date`, `indicators: tuple[str, ...]`, `fetch_pages: Callable[[], tuple[str, list[str]]]`

**`fetch_pages` 가 주소와 쪽을 함께 돌려주는 이유.** BOK·KIET·KDI 는 PDF 주소를
상세 페이지에서 읽어야 안다(`parse_pdf_link(http.get(issue.url).text)`). 주소를 즉시
필드로 두면 목록을 만드는 것만으로 회차마다 상세 페이지를 받게 되고, 회차가 30여 개라
그만큼 네트워크를 탄다. 주소도 본문과 함께 지연시켜야 한다. KLI·OECD Interim·KEIS 는
주소를 계산으로 얻으므로 이 제약이 없지만, 여섯이 한 모양이어야 도구가 출처를 모른다.
  - `documents.SOURCES: dict[str, Callable[[], list[Listed]]]` — 키는 `bok` `kdi` `kli` `kiet` `keis` `oecd_interim`

**기관별 지표 집합** (기존 상수를 그대로 읽는다. 새로 적지 않는다):

| 기관 | 지표 |
|---|---|
| bok | `bok.REQUIRED_INDICATORS` |
| kdi | `kdi.REQUIRED_INDICATORS` |
| keis | `keis.REQUIRED_INDICATORS` |
| kli | `kli.INDICATORS` |
| kiet | `set(kiet.LABEL_TO_INDICATOR.values())` |
| oecd_interim | `{"gdp_growth", "cpi"}` |

`imf` 와 `oecd` 본편은 넣지 않는다 — SDMX·CSV API 로 수치만 받아 원문 텍스트가 없다.

- [x] **Step 1: 실패 테스트를 쓴다**

```python
from datetime import date

from domains.forecast.pipeline import documents as d


def test_sources_covers_the_six_text_bearing_orgs():
    assert set(d.SOURCES) == {"bok", "kdi", "kli", "kiet", "keis", "oecd_interim"}


def test_imf_and_oecd_are_absent_because_they_have_no_document_text():
    assert "imf" not in d.SOURCES
    assert "oecd" not in d.SOURCES


def test_listed_carries_only_the_indicators_that_org_forecasts(monkeypatch):
    from domains.forecast.pipeline.collectors import kiet
    from domains.forecast.pipeline.report import Issue

    monkeypatch.setattr(kiet, "list_issues",
                        lambda: [Issue("2026년 전망", date(2026, 5, 26), "https://x/y")])
    listed = d.SOURCES["kiet"]()
    assert listed[0].indicators == ("gdp_growth",)
    assert listed[0].org == "KIET"


def test_fetch_pages_is_lazy_even_when_the_url_needs_a_detail_page(monkeypatch):
    # KIET 는 PDF 주소를 상세 페이지에서 읽는다. 목록 단계에서 그것을 받으면
    # 회차 30여 개마다 네트워크를 탄다 — 주소도 본문과 함께 지연돼야 한다.
    from domains.forecast.pipeline import http
    from domains.forecast.pipeline.collectors import kiet
    from domains.forecast.pipeline.report import Issue

    monkeypatch.setattr(kiet, "list_issues",
                        lambda: [Issue("t", date(2026, 5, 26), "https://x/y")])
    monkeypatch.setattr(http, "get", _boom)
    d.SOURCES["kiet"]()  # 여기서 터지면 안 된다


def test_fetch_pages_returns_the_source_url_with_the_pages(monkeypatch):
    from domains.forecast.pipeline import http, pdf
    from domains.forecast.pipeline.collectors import kli
    from domains.forecast.pipeline.report import Issue

    monkeypatch.setattr(kli, "list_issues",
                        lambda: [Issue("t", date(2026, 1, 2), "https://x/list?no=7")])
    monkeypatch.setattr(http, "get", lambda url, **k: _Resp(b"pdf"))
    monkeypatch.setattr(pdf, "page_texts", lambda data: ["1쪽", "2쪽"])
    url, pages = d.SOURCES["kli"]()[0].fetch_pages()
    assert url.startswith("http")
    assert pages == ["1쪽", "2쪽"]


class _Resp:
    def __init__(self, content):
        self.content = content


def _boom(*a, **k):
    raise AssertionError("목록 단계에서 네트워크를 탔다")
```

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_documents.py -q`
Expected: FAIL — `ModuleNotFoundError`

- [x] **Step 3: 구현한다**

각 수집기가 회차 목록과 PDF 주소를 얻는 방식이 제각각이라, 그것을 한 모양으로 덮는 얇은 층만 만든다. **수집기 안을 고치지 않는다.**

```python
"""여섯 출처의 회차 목록과 본문 획득을 한 모양으로 묶는다.

수집기마다 목록을 얻는 방법이 다르다(게시판 HTML, 상설 목록, 상수 표).
그 차이를 여기서 한 번 흡수하면 도구는 출처를 몰라도 된다.

fetch_pages 를 지연 호출로 두는 이유는 목록만 보려 할 때 회차마다 PDF 를
끌어오지 않기 위해서다. KEIS 는 텍스트 레이어가 없어 전문 OCR 에 회차당
1분 반이 걸리는데, 목록 단계에서 그것을 치르면 도구를 쓸 수 없다.
"""
from __future__ import annotations

from datetime import date
from typing import Callable, NamedTuple

from . import http, ocr, pdf
from .collectors import bok, kdi, keis, kiet, kli, oecd_interim


class Listed(NamedTuple):
    org: str
    title: str
    published_at: date
    indicators: tuple[str, ...]
    fetch_pages: Callable[[], tuple[str, list[str]]]


def _via_detail(detail_url: str, find_pdf) -> Callable[[], tuple[str, list[str]]]:
    """주소를 상세 페이지에서 읽는 출처(BOK·KIET·KDI)용."""
    def fetch() -> tuple[str, list[str]]:
        url = find_pdf(http.get(detail_url).text)
        return url, pdf.page_texts(http.get(url).content)
    return fetch


def _direct(url: str) -> Callable[[], tuple[str, list[str]]]:
    """주소를 계산으로 아는 출처(KLI·OECD Interim)용."""
    return lambda: (url, pdf.page_texts(http.get(url).content))


def _bok() -> list[Listed]:
    return [Listed("BOK", i.title, i.published_at,
                   tuple(sorted(bok.REQUIRED_INDICATORS)),
                   _via_detail(i.url, bok.parse_pdf_link))
            for i in bok.list_issues()]
```

나머지 다섯도 같은 요령이다. 수집기 안을 **고치지 않고** 이미 있는 함수를 부른다:

- `kiet`: `_via_detail(issue.url, kiet.parse_pdf_link)`, 지표는 `set(kiet.LABEL_TO_INDICATOR.values())`.
- `kli`: `_direct(kli.DOWNLOAD_URL.format(no=kli._list_no(issue.url)))`, 지표는 `kli.INDICATORS`.
- `oecd_interim`: `EDITIONS[label]` 의 `(published_at, url)` 로 `_direct(url)`, 지표는 `{"gdp_growth", "cpi"}`.
- `kdi`: 상세 페이지에서 첨부 주소를 얻는 기존 경로를 쓰되, 본문은 반드시
  `[kdi._unfold_february_header(t) for t in pdf.page_texts(...)]` — 2월 회차 헤더 접힘
  처리를 빠뜨리면 그 회차 본문이 어긋난다.
- `keis`: `keis.list_issues()` 의 `ListedIssue.pdf_url` 로 **전문** OCR —
  `lambda: (li.pdf_url, ocr.page_texts(http.get(li.pdf_url).content, None, dpi=400, preprocess=True))`.

수집기에 그 이름의 함수가 없으면(`parse_pdf_link`·`_list_no` 등) `collect_issue` 안의
해당 줄을 **그대로** 함수로 뽑아 쓴다. 동작을 바꾸지 말고 추출만 한다.

- [x] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_documents.py -q`
Expected: PASS (4 passed)

- [x] **Step 5: 지연 호출을 일부러 망가뜨린다**

`_from_pdf` 를 즉시 호출(`pages = pdf.page_texts(...); return lambda: pages`)로 바꾸고 `test_fetch_pages_is_lazy` 가 실패하는지 본다. 실패를 확인한 뒤 되돌리고, 관찰한 것을 보고한다.

- [x] **Step 6: 커밋**

```bash
git add domains/forecast/pipeline/documents.py domains/forecast/tests/test_documents.py
git commit -m "feat(forecast): 여섯 출처의 본문 획득을 한 모양으로 묶는다"
```

---

### Task 4: 도구

**Files:**
- Create: `tools/rationales.py`
- Test: `tools/tests/test_rationales.py`

**Interfaces:**
- Consumes: `documents.SOURCES`, `llm_select.select`, `llm_verify.verify`, `rationale.tags_for`, `rationale_store.{load_or_empty, merge, save}`
- Produces: `rationales.run(...) -> Report`, `rationales.main(argv) -> int`

- [x] **Step 1: 실패 테스트를 쓴다**

```python
from datetime import date

from domains.forecast.pipeline import rationale_store as rs
from tools import rationales


PAGES = ["", "취업자는 내수 회복이 반영되어 늘어날 것으로 전망된다."]


def _listed(**kw):
    base = dict(org="KDI", title="t", published_at=date(2026, 8, 19),
                indicators=("emp_change",),
                fetch_pages=lambda: ("https://x/y.pdf", PAGES))
    base.update(kw)
    from domains.forecast.pipeline.documents import Listed
    return Listed(**base)


def _pick(text="취업자는 내수 회복이 반영되어 늘어날 것으로 전망된다.", page=2):
    from domains.forecast.pipeline.llm_select import Picked
    return [Picked("emp_change", text, page)]


def test_saves_a_verified_rationale(tmp_path):
    rep = rationales.run(tmp_path, sources={"kdi": lambda: [_listed()]},
                         select=lambda *a, **k: _pick())
    assert rep.saved == 1
    saved = rs.load(tmp_path / "rationales.json")
    assert saved[0].indicator == "emp_change"
    assert saved[0].source_page == 2
    assert saved[0].tags == ["내수"]


def test_drops_a_candidate_that_is_not_in_the_source(tmp_path):
    rep = rationales.run(tmp_path, sources={"kdi": lambda: [_listed()]},
                         select=lambda *a, **k: _pick(text="지어낸 문장이다."))
    assert rep.saved == 0
    assert any("원문에 없다" in line for line in rep.rejected)
    assert rs.load(tmp_path / "rationales.json") == []


def test_does_not_overwrite_a_human_edited_sentence(tmp_path):
    rs.save(tmp_path / "rationales.json", [rs.Rationale(
        org="KDI", published_at=date(2026, 8, 19), indicator="emp_change",
        text="사람이 고친 문장", tags=[], source_url="https://x/y.pdf", source_page=2)])
    rationales.run(tmp_path, sources={"kdi": lambda: [_listed()]},
                   select=lambda *a, **k: _pick())
    assert rs.load(tmp_path / "rationales.json")[0].text == "사람이 고친 문장"


def test_one_failing_source_does_not_stop_the_others(tmp_path):
    def boom():
        raise ValueError("본문을 못 받았다")

    rep = rationales.run(
        tmp_path,
        sources={"bad": lambda: [_listed(fetch_pages=boom)], "kdi": lambda: [_listed()]},
        select=lambda *a, **k: _pick())
    assert rep.saved == 1
    assert len(rep.failures) == 1


def test_never_writes_forecasts_json(tmp_path):
    (tmp_path / "forecasts.json").write_text("[]", encoding="utf-8")
    before = (tmp_path / "forecasts.json").read_bytes()
    rationales.run(tmp_path, sources={"kdi": lambda: [_listed()]},
                   select=lambda *a, **k: _pick())
    assert (tmp_path / "forecasts.json").read_bytes() == before
```

- [x] **Step 2: 실패를 확인한다**

Run: `python -m pytest tools/tests/test_rationales.py -q`
Expected: FAIL — `ImportError: cannot import name 'rationales'`

- [x] **Step 3: 구현한다**

```python
"""전망 근거를 LLM 으로 골라 rationales.json 에 넣는다.

CI 가 이 도구를 부르지 않는다. 근거는 회차당 한 번 정해지면 되는 것이라
매일 돌 이유가 없고, 도구로 두면 LLM 이 죽든 키가 만료되든 매일 도는
수치 수집과 배포는 그대로 돈다.

이미 있는 항목은 덮지 않는다 — 사람이 문장을 다듬었을 수 있다. 일부러
다시 만들려면 --refresh 로 대상을 명시해야 한다.
"""
from __future__ import annotations

import sys
from dataclasses import dataclass, field
from pathlib import Path

from domains.forecast.pipeline import llm_select, llm_verify, rationale
from domains.forecast.pipeline import rationale_store as rs
from domains.forecast.pipeline.documents import SOURCES

DATA_DIR = Path(__file__).resolve().parent.parent / "domains" / "forecast" / "data"


@dataclass
class Report:
    saved: int = 0
    skipped: int = 0
    rejected: list[str] = field(default_factory=list)
    failures: list[str] = field(default_factory=list)
    lines: list[str] = field(default_factory=list)


def run(data_dir, *, sources=None, select=None, only=None, refresh=()) -> Report:
    sources = SOURCES if sources is None else sources
    select = select or llm_select.select
    path = Path(data_dir) / "rationales.json"
    existing, load_error = rs.load_or_empty(path)
    rep = Report()
    if load_error:
        rep.failures.append(f"rationales.json 을 읽지 못했다: {load_error}")
        return rep  # 사람이 고치던 파일을 기계 파일로 덮지 않는다

    if refresh:
        existing = [r for r in existing if r.key not in refresh]

    fresh: list[rs.Rationale] = []
    for name, listing in sources.items():
        if only and name not in only:
            continue
        try:
            listed_all = listing()
        except Exception as exc:
            rep.failures.append(f"{name} 목록: {type(exc).__name__}: {exc}")
            continue
        for listed in listed_all:
            try:
                source_url, pages = listed.fetch_pages()
                picked = select(listed.org, listed.title, listed.indicators, pages)
            except Exception as exc:
                rep.failures.append(
                    f"{name} {listed.title}: {type(exc).__name__}: {exc}")
                continue
            for p in picked:
                if p.indicator not in listed.indicators:
                    rep.rejected.append(
                        f"{listed.org} {listed.title} {p.indicator}: "
                        "이 기관이 전망하지 않는 지표다")
                    continue
                page_text = pages[p.source_page - 1] if 0 < p.source_page <= len(pages) else ""
                try:
                    text = llm_verify.verify(p.text, page_text)
                except llm_verify.Rejected as exc:
                    rep.rejected.append(
                        f"{listed.org} {listed.title} {p.indicator}: {exc.reason}")
                    continue
                fresh.append(rs.Rationale(
                    org=listed.org, published_at=listed.published_at,
                    indicator=p.indicator, text=text, tags=rationale.tags_for(text),
                    source_url=source_url, source_page=p.source_page))

    merged = rs.merge(existing, fresh)
    rep.saved = len(merged) - len(existing)
    rep.skipped = len(fresh) - rep.saved
    if merged != existing:
        rs.save(path, merged)
    return rep
```

`main(argv)` 은 `--only <기관>` 과 `--refresh <기관>:<발표일>:<지표>` 를 받고, 요약과 거절·실패 목록을 출력한 뒤 실패가 있으면 1 을 돌려준다. 인자 없이 전체를 덮는 경로를 만들지 않는다.

- [x] **Step 4: 통과를 확인한다**

Run: `python -m pytest tools/tests/test_rationales.py -q`
Expected: PASS (5 passed)

- [x] **Step 5: 보장을 일부러 망가뜨린다**

각각 망가뜨리고 실패를 본 뒤 되돌린다. 관찰한 단언문을 보고한다.

1. `llm_verify.verify` 호출을 빼고 후보를 그대로 저장 → 원문 대조 테스트가 실패해야 한다
2. `rs.merge` 를 덮어쓰기로 → 사람 수정 보호 테스트가 실패해야 한다
3. 회차 처리의 `try/except` 를 제거 → 한 출처 실패 테스트가 실패해야 한다
4. `p.indicator not in listed.indicators` 검사를 제거 → 이 검사만 자극하는 테스트를 추가한다(기관이 전망하지 않는 지표를 LLM 이 돌려주는 경우)

- [x] **Step 6: 커밋**

```bash
git add tools/rationales.py tools/tests/test_rationales.py
git commit -m "feat(forecast): 근거를 LLM 으로 골라 넣는 도구"
```

---

### Task 5: 규칙 방식 은퇴 — 파이프라인 배선 제거

**Files:**
- Modify: `domains/forecast/pipeline/collect.py`, `domains/forecast/pipeline/backfill.py`
- Modify: `domains/forecast/pipeline/collectors/{bok,kdi,kli,kiet,keis,oecd_interim}.py`
- Modify: `domains/forecast/pipeline/check_run.py` (근거 경고 채널이 쓰이지 않게 되면)
- Test: 해당 테스트 파일들

**Interfaces:**
- Consumes: 없음
- Produces: 없음(삭제)

매일 수집에서 근거를 뽑지 않는다. Task 4 의 도구가 그 일을 대신한다.

- [x] **Step 1: 무엇이 사라지는지 먼저 확인한다**

```bash
grep -rn "collect_issue_rationales\|collect_edition_rationales\|rationale_collectors\|rationale_sources\|rationale_errors" domains/ tools/ .github/
```

목록을 보고서에 적는다. `.github/` 에 한 건이라도 걸리면 **멈추고 보고한다** — 워크플로를 건드리지 않는 것이 이 계획의 제약이다.

- [x] **Step 2: 테스트를 먼저 지운다**

근거 수집을 단언하는 테스트를 지우고 `python -m pytest -q` 가 여전히 통과하는지 본다. 여기서 실패하는 것이 있으면 그 테스트가 다른 것도 함께 지키고 있었다는 뜻이니 **지우지 말고 보고한다.**

- [x] **Step 3: 배선을 지운다**

`collect.main` 과 `backfill.run` 에서 `rationale_collectors` / `rationale_sources` 인자와 그 처리, `rationales.json` 저장을 지운다. 수치 수집 경로는 **한 줄도 바뀌면 안 된다.**

- [x] **Step 4: 수집기의 진입점을 지운다**

여섯 수집기의 `collect_issue_rationales`(oecd_interim 은 `collect_edition_rationales`)를 지운다. 그 함수만 쓰던 헬퍼도 함께 지운다 — 단, `keis._locate_forecast_page` 는 `collect_issue` 도 쓰므로 **남긴다.**

- [x] **Step 5: 수치가 그대로인지 확인한다**

```bash
python -m pytest -q
python -c "import sys; sys.path.insert(0,'.'); from domains.forecast.pipeline import store; print(len(store.load_forecasts('domains/forecast/data/forecasts.json')))"
```

Expected: 테스트 전부 통과, 453 출력.

- [x] **Step 6: 커밋**

```bash
git add domains/forecast/pipeline/collect.py domains/forecast/pipeline/backfill.py domains/forecast/pipeline/collectors domains/forecast/tests
git commit -m "refactor(forecast): 매일 수집에서 근거 수집을 뺀다"
```

---

### Task 6: `rationale.py` 정리

**Files:**
- Modify: `domains/forecast/pipeline/rationale.py`
- Modify: `domains/forecast/tests/test_rationale.py`

**Interfaces:**
- Consumes: 없음
- Produces: `rationale.tags_for`, `TAG_WORDS`, `_FALSE_CONTAINMENT`, `_MAX_RATIONALE_LENGTH`, `_DUPLICATED_HANGUL` 만 남는다

- [x] **Step 1: 호출부가 정말 없는지 프로그램으로 확인한다**

```bash
for n in pick sentences _unwrap _bullet_sentences _is_page_furniture _FOOTNOTE_MARKER \
         _SECTION_HEADING _NEGATIVE_NUMBER _SENTENCE _DECIMAL_POINT INDICATOR_WORDS \
         _CAUSE _FORECAST _FORECAST_SUFFIX _MORE_SPECIFIC_FORM _BULLET_MARKERS \
         _WRAP_BOUNDARY_MARKERS; do
  echo "--- $n"; grep -rn "\b$n\b" domains/ tools/ --include=*.py | grep -v "pipeline/rationale.py" | grep -v tests/
done
```

**하나라도 남아 있으면 그 이름은 지우지 말고 보고한다.** 설계 10장이 명시한 확인이다.

- [x] **Step 2: 선별·분리 장치를 지운다**

지울 것: `pick`, `INDICATOR_WORDS`, `_CAUSE`, `_FORECAST`, `_FORECAST_SUFFIX`, `_MORE_SPECIFIC_FORM`, `_word_present`, `_mentions_indicator`, `sentences`, `_unwrap`, `_bullet_sentences`, `_BULLET_MARKERS`, `_WRAP_BOUNDARY_MARKERS`, `_is_page_furniture`, `_HAS_WORD_CHAR`, `_FOOTNOTE_MARKER`, `_SECTION_HEADING`, `_NEGATIVE_NUMBER`, `_SENTENCE`, `_DECIMAL_POINT`, `_DECIMAL_PLACEHOLDER`.

남길 것: `TAG_WORDS`, `_FALSE_CONTAINMENT`, `_word_present_as_tag`, `tags_for`, `_MAX_RATIONALE_LENGTH`, `_DUPLICATED_HANGUL`.

- [x] **Step 3: 모듈 docstring 을 고쳐 쓴다**

지금 docstring 은 문장을 고르는 모듈이라고 설명한다. 이제 **고른 문장에 태그를 붙이고 검사하는** 모듈이다. 왜 선별이 여기 없는지(LLM 이 한다), 어디로 갔는지(`llm_select`), 검증은 어디인지(`llm_verify`) 를 적는다.

- [x] **Step 4: 테스트를 정리한다**

`test_rationale.py` 에서 지운 이름을 쓰는 테스트를 지운다. `tags_for` 테스트와 `_FALSE_CONTAINMENT` 테스트는 **전부 남긴다** — 태그는 그대로 쓰이고, `이유가` 오탐은 실제로 데이터에 실렸던 결함이다.

- [x] **Step 5: 통과를 확인한다**

Run: `python -m pytest -q` · `node --test`
Expected: 전부 통과. 줄어든 테스트 수를 보고한다.

- [x] **Step 6: 커밋**

```bash
git add domains/forecast/pipeline/rationale.py domains/forecast/tests/test_rationale.py
git commit -m "refactor(forecast): 문장을 고르는 장치를 걷어내고 검사만 남긴다"
```

---

### Task 7: 문서 갱신

**Files:**
- Modify: `docs/superpowers/specs/2026-09-01-전망근거-수집-design.md`
- Modify: `README.md` (근거 관련 서술이 있으면)

- [x] **Step 1: 선행 설계 문서에 은퇴를 적는다**

`2026-09-01-전망근거-수집-design.md` 머리에 **이 설계의 선별 방식은 은퇴했고 `2026-09-01-전망근거-LLM선별-design.md` 로 대체됐다**고 적는다. 문서를 지우지 않는다 — 규칙 방식이 어디서 막혔는지가 새 설계의 근거이고, 그 실측이 문서에만 남아 있다.

저장 형식·태그·화면은 그대로 유효하다는 것도 적는다. 어느 부분이 살아 있고 어느 부분이 죽었는지 구분되지 않으면 다음 사람이 문서 전체를 못 믿는다.

- [x] **Step 2: 도구 사용법을 적는다**

README 에 `ANTHROPIC_API_KEY` 를 넣고 `python -m tools.rationales` 를 돌리는 법, `--only` 와 `--refresh` 의 뜻, **CI 가 이 도구를 부르지 않는다**는 사실을 적는다.

- [x] **Step 3: 커밋**

```bash
git add docs/superpowers/specs/2026-09-01-전망근거-수집-design.md README.md
git commit -m "docs(forecast): 규칙 선별의 은퇴와 새 도구 사용법"
```

---

### Task 8: 실행과 데이터 커밋 — 컨트롤러가 한다

구현자가 하지 않는다. 컨트롤러가 `ANTHROPIC_API_KEY` 를 넣고 도구를 돌린 뒤,
나온 근거를 **전부 눈으로 읽고** 커밋한다.

- [ ] `rationales.json` 을 지우고 `python -m tools.rationales` 실행
- [ ] 나온 문장을 전부 읽는다. 원문 대조를 통과했어도 **근거가 아닌 문장**(수치 재진술, 다른 지표 이야기)이 섞였는지는 사람이 본다
- [ ] `forecasts.json` 이 453건 그대로인지 대조
- [ ] 거절·실패 목록을 읽고 프롬프트를 고칠 필요가 있는지 판단
- [ ] 데이터 커밋

**실행 규모(2026-09-02 실측).** 목록 조회와 소스별 대표 회차 1건 다운로드로 재 본
숫자다 — 다음 세션이 다시 재지 않도록 남긴다.

| 소스 | 회차 | 대표 회차 본문 |
|---|---|---|
| bok | 51 | 69쪽 / 69,603자 |
| kdi | 11 | 5쪽 / 3,364자 (요약 장만 받으므로 작다) |
| kli | 10 | 27쪽 / 29,213자 |
| kiet | 5 | 103쪽 / 93,543자 |
| keis | 10 | OCR — 회차당 약 96초 |
| oecd_interim | 3 | 27쪽 / 61,346자 |

합계 90회차. 전량 실행은 LLM 입력 300만 토큰 안팎(Sonnet 기준 10달러 내외)에
벽시계 30~60분(KEIS OCR 16분 + BOK PDF 51개 내려받기)이 든다.

**순서는 시범 먼저로 정했다(사용자 판단, 2026-09-02).** `--only kdi --only kli
--only oecd_interim` 24회차를 먼저 돌려(1달러 미만·5분) 뽑힌 문장과 거절 사유를
사람이 읽고, 프롬프트를 고칠 일이 있으면 **BOK 51회차에 돈을 쓰기 전에** 고친다.

## Self-Review

**1. 설계 각 장의 구현 위치**

| 설계 | 태스크 |
|---|---|
| §2 LLM 선별, CI 밖 | 2, 4 |
| §3 경계(안 바뀌는 것) | 4 가 `rationale_store`·`tags_for` 를 그대로 씀 |
| §4 구조(3모듈) | 1, 2, 3 |
| §5 검증 | 1 |
| §6 프롬프트 계약 | 2 |
| §7 실패 처리 | 4 |
| §8 테스트 | 1~4 각 태스크 |
| §9 운영(키·모델·KEIS 비용) | 2(키·모델), 3(KEIS 전문 OCR), 7(사용법) |
| §10 은퇴 범위 | 5, 6 |
| §11 안 하는 것 | 3 이 imf·oecd 를 뺌 |
| §12 성공 기준 | 8 |

빠진 장 없음.

**2. 플레이스홀더** — 없음. Task 3 의 `bok.pdf_url_for` 만 "없으면 기존 줄을 함수로 뽑아라" 로 조건부인데, 수집기마다 주소 획득 방식이 달라 코드를 미리 못 적는다. 무엇을 하라는지와 금지사항(동작을 바꾸지 말 것)은 명시했다.

**3. 이름 일관성** — `Listed.indicators`(Task 3) → `select(..., indicators, ...)`(Task 2) → `p.indicator not in listed.indicators`(Task 4) 일치. `Picked(indicator, text, source_page)` → `rs.Rationale(indicator=, text=, source_page=)` 일치. `llm_verify.verify(candidate, source_page_text)` → Task 4 가 `pages[p.source_page - 1]` 을 넘김 — 쪽번호가 1부터라는 것이 Task 2 프롬프트(`enumerate(..., start=1)`)와 일치.
