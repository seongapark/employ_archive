# 연구보고서 전량 아카이브 전환과 추천 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 연구보고서 도메인을 "고용 키워드로 거르는 아카이브"에서 "외부기관 보고서를 전량 취합하고 LLM 이 읽을 만한 것을 추천하는 아카이브"로 바꾸고, 한국은행을 다섯 번째 기관으로 넣는다.

**Architecture:** 수집(`collect`)은 판정하지 않고 raw 에 넣는다. 빌드(`build`)는 전량 수록하며 결정적·오프라인이다. 추천(`recommend`)은 빌드 밖 별도 단계로 LLM 을 부르고 id 캐시를 쌓는다. 추천이 실패해도 아카이브는 돈다.

**Tech Stack:** Python 3.12 · pydantic · curl_cffi · pytest · 바닐라 ES 모듈 프런트(빌드 없음) · GitHub Actions · GitHub Pages

**Spec:** `docs/superpowers/specs/2026-09-11-연구보고서-전량아카이브-추천-design.md`

## Global Constraints

- 작업 폴더는 워크트리 `~/Desktop/연구보고서-전량아카이브`, 브랜치 `reports/full-archive`. **이 폴더 밖을 건드리지 않는다.**
- **`git add -A` · `git commit -a` 금지.** 항상 `git commit -m "..." -- <경로>` 로 경로를 지정한다. (`-m` 이 `--` 보다 앞에 와야 한다.)
- 커밋 메시지 끝에 두 줄을 붙인다:
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh`
- **테스트는 네트워크를 쓰지 않는다.** 파서 테스트는 `domains/reports/tests/fixtures/` 의 고정본으로, 추천 테스트는 `call` 주입으로 돈다.
- 수집 하한 `CUTOFF = '2021-01-01'` (`models.py`). 바꾸지 않는다.
- 모델 ID 는 정확히 `claude-opus-5`. 날짜 접미사를 붙이지 않는다.
- `employ_archive` 는 **PUBLIC 저장소**다. second-brain 원문·센터명·프로젝트명을 커밋하지 않는다.
- 새 파이썬 진입점은 `sys.stdout.reconfigure(encoding='utf-8', errors='replace')` 로 시작한다. Windows 콘솔(cp949)에서 로그 한 줄 때문에 전체가 죽는다.
- 파이썬 테스트: `python -m pytest -q domains/reports`
- **화면 테스트는 pytest 가 아니다.** `node:test` 로 도는 `.mjs` 다:
  `node --test "domains/reports/tests/web/*.test.mjs"`
  (`.github/workflows/pages.yml` 이 이 경로를 이미 돌린다 — 새 파일은 `*.test.mjs` 이름을
  지켜야 자동으로 잡힌다.)

---

## 파일 구조

**새로 만드는 것**

| 경로 | 책임 |
|---|---|
| `domains/reports/pipeline/sites/bok.py` | 한국은행 목록·상세 파서. 다른 넷과 같은 `list_url`/`parse_list`/`parse_detail` 인터페이스 |
| `domains/reports/pipeline/recommend.py` | LLM 추천 판정과 캐시. 네트워크를 아는 유일한 새 모듈 |
| `domains/reports/pipeline/interests.py` | `interests.md` 를 읽고 축 이름과 프로파일 버전을 준다 |
| `domains/reports/data/interests.md` | 관심축 프로파일. 추천 기준의 유일한 사본 |
| `domains/reports/data/recommendations.json` | 판정 캐시 (빌드 산출물이자 앱이 읽는 파일) |
| `domains/reports/app/js/screens/picks.js` | 추천 탭 화면 |
| `domains/reports/tests/fixtures/bok_list.html` | `listCont.do` 응답 고정본 |
| `domains/reports/tests/fixtures/bok_detail_note.html` | BOK 이슈노트 상세(초록형) |
| `domains/reports/tests/fixtures/bok_detail_outlook.html` | 경제전망보고서 상세(목차형) |
| `domains/reports/tests/fixtures/bok_detail_industry.html` | 주력산업 모니터링 상세(안내문형) |
| `domains/reports/tests/test_bok.py` | BOK 파서 테스트 |
| `domains/reports/tests/test_recommend.py` | 추천 판정·캐시 테스트 |
| `domains/reports/tests/test_interests.py` | 프로파일 로더 테스트 |
| `domains/reports/tests/web/picks.test.mjs` | 추천 화면 테스트 (`node:test`) |

**고치는 것**

| 경로 | 무엇을 |
|---|---|
| `domains/reports/pipeline/boards.py` | `Board` 에 `menu_no`·`body` 추가, `filter` 제거 |
| `domains/reports/pipeline/models.py` | `Report` 에서 `employment` 제거 |
| `domains/reports/pipeline/build.py` | 전량 수록, 게시판 내 제목 중복 제거, gzip 크기 기록 |
| `domains/reports/pipeline/collect.py` | `SITES` 에 `'bok'` |
| `domains/reports/data/boards.json` | BOK 8줄 추가, 기존 11줄에서 `filter` 제거 |
| `domains/reports/data/orgs.json` | BOK 한 줄 |
| `domains/reports/app/css/app.css` | `--org-bok` 색 |
| `domains/reports/app/js/route.js` | `picks` 라우트 |
| `domains/reports/app/js/app.js` | `picks` 화면 등록, `recommendations.json` 로드 |
| `domains/reports/app/index.html` | 탭 넷 |
| `domains/reports/app/js/screens/home.js` | `ORGS` 에 BOK, 검색창 아래 추천 섹션, 타임라인 배지 |
| `domains/reports/app/js/screens/report.js` | 추천 축·이유 표시 |
| `domains/reports/tests/test_build.py` | 전량 수록으로 전제가 바뀐 테스트 갱신 |
| `.github/workflows/collect-reports.yml` | `recommend` 단계와 시크릿 둘 |

---

## Task 1: 한국은행 목록 파서

**Files:**
- Create: `domains/reports/tests/fixtures/bok_list.html`
- Create: `domains/reports/pipeline/sites/bok.py`
- Modify: `domains/reports/pipeline/boards.py` (`Board` 에 `menu_no`·`body` 필드 추가)
- Test: `domains/reports/tests/test_bok.py`

**Interfaces:**
- Consumes: `boards.Board`, `sites.common.text/absolute/page_url`, `models.RawReport/normalize_published/report_id`
- Produces: `bok.list_url(board, page) -> str`, `bok.parse_list(html, board) -> list[dict]` — dict 키는 다른 파서와 같다: `native_id`, `detail_url`, `published_raw`, `title`, `dept`

- [ ] **Step 1: 고정본을 뜬다**

한 번만 네트워크를 쓴다. 테스트는 이 파일만 읽는다.

```bash
python -X utf8 - <<'PY'
import urllib.request, urllib.parse, pathlib
q = dict(pageIndex='1', targetDepth='3', menuNo='200433', syncMenuChekKey='1',
         searchCnd='1', searchKwd='', depth2='201157', depth3='200433',
         date='', sdate='', edate='', sort='1', pageUnit='100')
u = 'https://www.bok.or.kr/portal/singl/newsData/listCont.do?' + urllib.parse.urlencode(q)
req = urllib.request.Request(u, headers={'User-Agent': 'Mozilla/5.0',
                                         'X-Requested-With': 'XMLHttpRequest'})
html = urllib.request.urlopen(req, timeout=40).read().decode('utf-8', 'replace')
p = pathlib.Path('domains/reports/tests/fixtures/bok_list.html')
p.write_text(html, encoding='utf-8')
print(p, len(html))
PY
```

Expected: 약 100건이 든 HTML, 15만자 안팎.

- [ ] **Step 2: `Board` 에 필드를 더한다**

`domains/reports/pipeline/boards.py` 의 `Board` 클래스에 두 줄을 더한다(`filter` 는 Task 5 에서 없앤다).

```python
    # 한국은행은 게시판을 menuNo 로 구분한다. 목록 주소가 전 게시판 공통이라
    # list_url 만으로는 어느 게시판인지 알 수 없다.
    menu_no: Optional[str] = None
    # 상세 본문 블록(div.dbdata)이 게시판마다 다른 것을 담는다 — 초록이기도,
    # 목차이기도, "첨부파일 참조" 안내문이기도 하다. 실측표는 스펙 §3-3.
    # 이 값을 바꾸면 그 게시판만 재수집해야 한다(분류가 수집 시점에 일어난다).
    body: Literal['abstract', 'toc', 'none'] = 'abstract'
```

파일 맨 위 import 에 `Literal` 을 더한다: `from typing import Literal, Optional`

- [ ] **Step 3: 실패하는 테스트를 쓴다**

`domains/reports/tests/test_bok.py`:

```python
"""한국은행 파서. 고정본만 읽는다 — 네트워크를 쓰지 않는다."""
from pathlib import Path

import pytest

from domains.reports.pipeline.boards import Board
from domains.reports.pipeline.sites import bok

FIX = Path(__file__).parent / 'fixtures'


def board(**over):
    base = dict(id='bok-note', org='bok', name='BOK 이슈노트', series='BOK 이슈노트',
                list_url='https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=200433',
                menu_no='200433', item_re=r'nttId=(\d+)', body='abstract')
    base.update(over)
    return Board.model_validate(base)


@pytest.fixture(scope='module')
def list_html():
    return (FIX / 'bok_list.html').read_text(encoding='utf-8')


def test_list_url_carries_the_menu_number_and_page(list_html):
    url = bok.list_url(board(), 3)
    assert 'menuNo=200433' in url
    assert 'pageIndex=3' in url
    assert 'pageUnit=100' in url
    assert 'listCont.do' in url


def test_parse_list_reads_every_row(list_html):
    items = bok.parse_list(list_html, board())
    assert len(items) >= 50
    first = items[0]
    assert first['native_id'].isdigit()
    assert first['detail_url'].startswith('https://www.bok.or.kr/portal/bbs/')
    assert 'nttId=' in first['detail_url']
    assert first['title']
    assert first['published_raw'].startswith('20')


def test_parse_list_keeps_the_department(list_html):
    items = bok.parse_list(list_html, board())
    assert any(i.get('dept') for i in items)


def test_parse_list_does_not_duplicate_ids(list_html):
    items = bok.parse_list(list_html, board())
    ids = [i['native_id'] for i in items]
    assert len(ids) == len(set(ids))
```

- [ ] **Step 4: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_bok.py`
Expected: FAIL — `ModuleNotFoundError: ... sites.bok`

- [ ] **Step 5: 목록 파서를 쓴다**

`domains/reports/pipeline/sites/bok.py`:

```python
"""한국은행(BOK) 목록·상세 파서.

목록 페이지(list.do)는 자바스크립트가 그린다. 그리는 주체인 AJAX 원본
listCont.do 가 완성된 HTML 을 그대로 주므로 그쪽을 읽는다 — 쪽당 100건에
pageIndex 페이징이라 2021년까지 백필된다.

(고용전망 도메인의 BOK 수집기는 RSS 를 쓴다. 그건 최신 회차만 필요해서다.
여기는 백필이 필요해 경로가 다르다 — 코드를 공유하지 않는다.)

게시판 여덟이 전부 이 파서 하나로 처리된다. 목록 엔드포인트가 하나고,
상세는 전부 /portal/bbs/<bbsId>/view.do?nttId= 이며 bbsId 는 목록 앵커에
박혀 있어 boards.json 에 적을 필요가 없다. 구분자는 menuNo 뿐이다.
"""
from __future__ import annotations

import re
from urllib.parse import urlencode

from ..boards import Board
from ..models import RawReport, normalize_published, report_id
from . import common

BASE = 'https://www.bok.or.kr'
LIST_ENDPOINT = f'{BASE}/portal/singl/newsData/listCont.do'
PAGE_UNIT = 100

_ROW = re.compile(r'<li class="bbsRowCls">(.*?)</li>', re.S)
_DATE = re.compile(r'class="date"><span class="sr-only">[^<]*</span>\s*([\d.]+)')
_DEPT = re.compile(r'class="depart"><span class="sr-only">[^<]*</span>\s*([^<]*)')
_LINK = re.compile(
    r'<a href="(/portal/bbs/[A-Z]\d+/view\.do\?nttId=(\d+)[^"]*)"[^>]*class="title">(.*?)</a>',
    re.S)


def list_url(board: Board, page: int) -> str:
    query = {
        'pageIndex': str(page), 'targetDepth': '3', 'menuNo': board.menu_no or '',
        'syncMenuChekKey': '1', 'searchCnd': '1', 'searchKwd': '',
        'depth2': '201157', 'depth3': board.menu_no or '',
        'date': '', 'sdate': '', 'edate': '', 'sort': '1',
        'pageUnit': str(PAGE_UNIT),
    }
    return f'{LIST_ENDPOINT}?{urlencode(query)}'


def parse_list(html: str, board: Board) -> list[dict]:
    items: list[dict] = []
    seen: set[str] = set()
    for block in _ROW.findall(html or ''):
        link = _LINK.search(block)
        date = _DATE.search(block)
        if not (link and date):
            continue
        native_id = link.group(2)
        if native_id in seen:
            continue
        title = common.text(link.group(3))
        if not title:
            continue
        dept = _DEPT.search(block)
        seen.add(native_id)
        items.append({
            'native_id': native_id,
            'detail_url': common.absolute(BASE, link.group(1).replace('&amp;', '&')),
            'published_raw': date.group(1),
            'title': title,
            'dept': common.text(dept.group(1)) if dept else '',
        })
    return items
```

- [ ] **Step 6: 통과를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_bok.py`
Expected: PASS (4건)

- [ ] **Step 7: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 한국은행 목록 파서 — AJAX 원본을 읽는다

list.do 는 JS 가 그리지만 listCont.do 가 완성된 HTML 을 준다.
쪽당 100건·pageIndex 라 2021년까지 백필된다. 게시판 여덟이 menuNo
하나로 갈리고 bbsId 는 목록 앵커에 박혀 있어 등록할 필요가 없다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/pipeline/sites/bok.py domains/reports/pipeline/boards.py domains/reports/tests/test_bok.py domains/reports/tests/fixtures/bok_list.html
```

---

## Task 2: 한국은행 상세 파서 — 본문 세 갈래

**Files:**
- Create: `domains/reports/tests/fixtures/bok_detail_note.html`, `bok_detail_outlook.html`, `bok_detail_industry.html`
- Modify: `domains/reports/pipeline/sites/bok.py`
- Test: `domains/reports/tests/test_bok.py`

**Interfaces:**
- Consumes: Task 1 의 `parse_list` 가 낸 item dict, `Board.body`
- Produces: `bok.parse_detail(html, item, board) -> RawReport`

**왜 갈래가 셋인가:** 상세 본문 블록 `div.dbdata` 가 게시판마다 다른 것을 담는다. 이슈노트는 진짜 초록(1,500~2,000자), 경제전망보고서는 목차, 주력산업 모니터링은 "자세한 내용은 첨부파일을 참조하시기 바랍니다"(25자)다. 한 갈래로 처리하면 목차가 초록 자리에 들어간다. 실측표는 스펙 §3-3.

- [ ] **Step 1: 고정본 셋을 뜬다**

```bash
python -X utf8 - <<'PY'
import urllib.request, pathlib
PAGES = {
    'bok_detail_note.html':
        'https://www.bok.or.kr/portal/bbs/P0002353/view.do?nttId=11064490&menuNo=200433',
    'bok_detail_outlook.html':
        'https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=200066',
    'bok_detail_industry.html':
        'https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=201127',
}
for name, url in PAGES.items():
    req = urllib.request.Request(url, headers={'User-Agent': 'Mozilla/5.0'})
    html = urllib.request.urlopen(req, timeout=40).read().decode('utf-8', 'replace')
    p = pathlib.Path('domains/reports/tests/fixtures') / name
    p.write_text(html, encoding='utf-8')
    print(p, len(html))
PY
```

**주의:** 위 URL 중 `note` 만 상세 주소다. `outlook`·`industry` 는 목록 주소를 적어 뒀으므로 **그대로 쓰면 안 된다.** 먼저 Task 1 의 `parse_list` 로 그 게시판 목록에서 첫 항목의 `detail_url` 을 얻어 그 주소를 받는다:

```bash
python -X utf8 - <<'PY'
import pathlib, urllib.request, sys
sys.path.insert(0, '.')
from domains.reports.boards_helper_stub import *  # noqa  (없으면 아래처럼 직접 만든다)
PY
```

간단히 이렇게 한다:

```bash
python -X utf8 - <<'PY'
import sys, pathlib, urllib.request
sys.path.insert(0, '.')
from domains.reports.pipeline.boards import Board
from domains.reports.pipeline.sites import bok

def grab(menu_no, board_id, name):
    b = Board.model_validate(dict(
        id=board_id, org='bok', name=name, series=name,
        list_url=f'https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo={menu_no}',
        menu_no=menu_no, item_re=r'nttId=(\d+)'))
    req = urllib.request.Request(bok.list_url(b, 1),
                                 headers={'User-Agent': 'Mozilla/5.0',
                                          'X-Requested-With': 'XMLHttpRequest'})
    html = urllib.request.urlopen(req, timeout=40).read().decode('utf-8', 'replace')
    item = bok.parse_list(html, b)[0]
    req = urllib.request.Request(item['detail_url'], headers={'User-Agent': 'Mozilla/5.0'})
    detail = urllib.request.urlopen(req, timeout=40).read().decode('utf-8', 'replace')
    return item, detail

for menu_no, bid, name, fname in [
        ('200066', 'bok-outlook', '경제전망보고서', 'bok_detail_outlook.html'),
        ('201127', 'bok-industry', '주력산업 모니터링 보고서', 'bok_detail_industry.html')]:
    item, detail = grab(menu_no, bid, name)
    pathlib.Path('domains/reports/tests/fixtures', fname).write_text(detail, encoding='utf-8')
    print(fname, item['title'][:40], len(detail))
PY
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`domains/reports/tests/test_bok.py` 끝에 붙인다:

```python
def _detail(name):
    return (FIX / name).read_text(encoding='utf-8')


def test_abstract_board_puts_the_body_in_the_abstract():
    item = {'native_id': '11064490', 'title': '가구DB를 이용한 가계부채 리스크 평가',
            'detail_url': 'https://www.bok.or.kr/portal/bbs/P0002353/view.do?nttId=11064490',
            'published_raw': '2026.09.07', 'dept': '금융통화연구실'}
    rec = bok.parse_detail(_detail('bok_detail_note.html'), item, board())
    assert rec.id == 'bok-11064490'
    assert rec.org == 'bok'
    assert rec.abstract and len(rec.abstract) > 500
    assert rec.toc == []
    assert rec.abstract_note is None


def test_toc_board_puts_the_body_in_the_toc():
    item = {'native_id': '999001', 'title': '경제전망보고서(2026년 8월)',
            'detail_url': 'https://www.bok.or.kr/portal/bbs/P0002359/view.do?nttId=999001',
            'published_raw': '2026.08.27', 'dept': '조사국'}
    b = board(id='bok-outlook', name='경제전망보고서', series='경제전망보고서',
              menu_no='200066', body='toc')
    rec = bok.parse_detail(_detail('bok_detail_outlook.html'), item, b)
    assert rec.abstract is None
    assert len(rec.toc) >= 3
    assert rec.abstract_note == '요약 없음'


def test_none_board_drops_the_attachment_notice():
    item = {'native_id': '999002', 'title': '2024년 4/4분기 주력산업 모니터링 보고서',
            'detail_url': 'https://www.bok.or.kr/portal/bbs/B0000357/view.do?nttId=999002',
            'published_raw': '2025.02.17', 'dept': '조사국'}
    b = board(id='bok-industry', name='주력산업 모니터링 보고서',
              series='주력산업 모니터링 보고서', menu_no='201127', body='none')
    rec = bok.parse_detail(_detail('bok_detail_industry.html'), item, b)
    assert rec.abstract is None
    assert rec.toc == []
    assert rec.abstract_note == '요약 없음'


def test_the_attachment_url_is_the_stable_one_not_the_expiring_cdn():
    item = {'native_id': '11064490', 'title': 'ㄱ',
            'detail_url': 'https://www.bok.or.kr/portal/bbs/P0002353/view.do?nttId=11064490',
            'published_raw': '2026.09.07', 'dept': ''}
    rec = bok.parse_detail(_detail('bok_detail_note.html'), item, board())
    assert rec.file_url and rec.file_url.startswith('https://www.bok.or.kr/fileSrc/')
    # 뷰어가 쓰는 file-cdn 주소에는 만료 토큰이 붙는다. 저장하면 안 된다.
    assert 'file-cdn' not in rec.file_url
    assert 'token=' not in rec.file_url


def test_the_source_note_prefix_is_stripped_from_an_issue_abstract():
    body = ('* 본 내용은 2026년 5월 경제전망보고서 에 수록된 <중장기 심층연구> 입니다.\n'
            '경 제 전 망 보 고 서 전문 은 아래의 경로에서 확인 가능합니다.\n'
            '실제 초록 문장이 여기서 시작한다. ' + '가' * 300)
    assert bok._strip_source_note(body).startswith('실제 초록 문장이')
```

- [ ] **Step 3: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_bok.py`
Expected: FAIL — `AttributeError: module ... has no attribute 'parse_detail'`

- [ ] **Step 4: 상세 파서를 쓴다**

`bok.py` 에 붙인다:

```python
_BODY = re.compile(r'<div class="dbdata\s*"[^>]*>(.*?)</div>\s*(?:<div|</div>)', re.S)
_FILE = re.compile(r'<a href="(/fileSrc/[^"]+)"')
# 이슈분석·심층연구는 초록 앞에 출처 안내문이 붙는다. 안내문만 있는 회차도 있다.
_SOURCE_NOTE = re.compile(
    r'^\s*\*?\s*본 내용은.*?확인 가능합니다\s*\.?', re.S)
_ATTACH_ONLY = re.compile(r'첨부파일')


def _strip_source_note(text: str) -> str:
    return _SOURCE_NOTE.sub('', text or '').strip()


def _body_text(html: str) -> str:
    m = _BODY.search(html or '')
    return common.text(m.group(1)) if m else ''


def _body_lines(html: str) -> list[str]:
    m = _BODY.search(html or '')
    return common.lines(m.group(1)) if m else []


def parse_detail(html: str, item: dict, board: Board) -> RawReport:
    published, precision = normalize_published(item['published_raw'])
    abstract, toc, note = None, [], None

    if board.body == 'toc':
        toc = [line for line in _body_lines(html) if line]
        note = '요약 없음'
    elif board.body == 'none':
        note = '요약 없음'
    else:
        text = _strip_source_note(_body_text(html))
        # 안내문만 든 회차를 초록으로 세면 결측률이 거짓으로 내려간다.
        if len(text) < 80 and _ATTACH_ONLY.search(text):
            note = '요약 없음'
        elif text:
            abstract = text
        else:
            note = '요약 없음'

    file_m = _FILE.search(html or '')
    return RawReport(
        id=report_id('bok', board.id_prefix + item['native_id']),
        org='bok',
        board=board.id,
        series=board.series,
        title=item['title'],
        authors=[item['dept']] if item.get('dept') else [],
        published=published,
        date_precision=precision,
        landing_url=item['detail_url'],
        file_url=common.absolute(BASE, file_m.group(1)) if file_m else None,
        abstract=abstract,
        toc=toc,
        abstract_note=note,
    )
```

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_bok.py`
Expected: PASS (9건)

고정본에 따라 `_BODY` 가 안 맞을 수 있다. 실패하면 고정본에서 `dbdata` 주변을 직접 보고 정규식을 맞춘다:

```bash
python -X utf8 -c "
import re
h = open('domains/reports/tests/fixtures/bok_detail_note.html', encoding='utf-8').read()
i = h.find('dbdata')
print(repr(h[i-200:i+600]))
"
```

- [ ] **Step 6: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 한국은행 상세 파서 — 본문이 게시판마다 다른 것을 담는다

div.dbdata 가 이슈노트에서는 초록(1,500~2,000자), 경제전망보고서에서는
목차, 주력산업 모니터링에서는 "첨부파일 참조" 25자다. 한 갈래로 처리하면
목차가 초록 자리에 들어간다. boards.json 의 body 로 갈라 담는다.

첨부는 <a href> 의 /fileSrc/ 주소를 쓴다. 뷰어가 쓰는 file-cdn 주소에는
만료 토큰이 붙어 저장하면 나중에 못 받는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/pipeline/sites/bok.py domains/reports/tests/test_bok.py domains/reports/tests/fixtures/
```

---

## Task 3: 게시판 여덟을 등록하고 수집기에 잇는다

**Files:**
- Modify: `domains/reports/data/boards.json`, `domains/reports/data/orgs.json`
- Modify: `domains/reports/pipeline/collect.py:33`
- Modify: `domains/reports/app/css/app.css`, `domains/reports/app/js/screens/home.js`
- Test: `domains/reports/tests/test_boards.py`

**Interfaces:**
- Consumes: Task 1·2 의 `bok` 모듈
- Produces: `SITES['bok']`, 게시판 id 여덟 — `bok-outlook`·`bok-assess`·`bok-region`·`bok-industry`·`bok-note`·`bok-issue`·`bok-intl`·`bok-research`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`domains/reports/tests/test_boards.py` 끝에 붙인다:

```python
def test_every_bok_board_is_registered_with_a_menu_number_and_body_kind():
    boards = boards_mod.load_boards()
    bok_boards = {b.id: b for b in boards if b.org == 'bok'}
    assert set(bok_boards) == {
        'bok-outlook', 'bok-assess', 'bok-region', 'bok-industry',
        'bok-note', 'bok-issue', 'bok-intl', 'bok-research'}
    for b in bok_boards.values():
        assert b.menu_no and b.menu_no.isdigit()
        assert b.body in ('abstract', 'toc', 'none')


def test_bok_board_ids_do_not_collide_with_other_orgs():
    ids = [b.id for b in boards_mod.load_boards()]
    assert len(ids) == len(set(ids))
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_boards.py`
Expected: FAIL — `assert set() == {...}`

- [ ] **Step 3: `boards.json` 에 여덟 줄을 더한다**

기존 배열 끝(`kdi-etc` 뒤)에 붙인다. `list_url` 은 사람이 열어보는 주소이고, 수집은 `menu_no` 로 한다.

```json
  {"id": "bok-outlook", "org": "bok", "name": "경제전망보고서", "series": "경제전망보고서",
   "list_url": "https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=200066",
   "menu_no": "200066", "item_re": "nttId=(\\d+)", "body": "toc"},
  {"id": "bok-assess", "org": "bok", "name": "경제상황 평가", "series": "경제상황 평가",
   "list_url": "https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=200439",
   "menu_no": "200439", "item_re": "nttId=(\\d+)", "body": "abstract"},
  {"id": "bok-region", "org": "bok", "name": "지역경제보고서", "series": "지역경제보고서",
   "list_url": "https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=200069",
   "menu_no": "200069", "item_re": "nttId=(\\d+)", "body": "toc"},
  {"id": "bok-industry", "org": "bok", "name": "주력산업 모니터링 보고서", "series": "주력산업 모니터링 보고서",
   "list_url": "https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=201127",
   "menu_no": "201127", "item_re": "nttId=(\\d+)", "body": "none"},
  {"id": "bok-note", "org": "bok", "name": "BOK 이슈노트", "series": "BOK 이슈노트",
   "list_url": "https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=200433",
   "menu_no": "200433", "item_re": "nttId=(\\d+)", "body": "abstract"},
  {"id": "bok-issue", "org": "bok", "name": "이슈분석·심층연구", "series": "이슈분석·심층연구",
   "list_url": "https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=201140",
   "menu_no": "201140", "item_re": "nttId=(\\d+)", "body": "abstract"},
  {"id": "bok-intl", "org": "bok", "name": "국제경제이슈", "series": "국제경제이슈",
   "list_url": "https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=201757",
   "menu_no": "201757", "item_re": "nttId=(\\d+)", "body": "toc"},
  {"id": "bok-research", "org": "bok", "name": "조사연구자료", "series": "조사연구자료",
   "list_url": "https://www.bok.or.kr/portal/singl/newsData/list.do?menuNo=200560",
   "menu_no": "200560", "item_re": "nttId=(\\d+)", "body": "none"}
```

- [ ] **Step 4: `orgs.json` 에 한 줄을 더한다**

```json
  {"code": "bok", "name": "한국은행", "short": "BOK", "color": "--org-bok"}
```

- [ ] **Step 5: 수집기에 잇는다**

`domains/reports/pipeline/collect.py` 29·33행:

```python
from .sites import bok, kdi, keis, kiet, kli

...

SITES = {'kli': kli, 'keis': keis, 'kdi': kdi, 'kiet': kiet, 'bok': bok}
```

- [ ] **Step 6: 화면 쪽 두 줄**

`domains/reports/app/css/app.css` — 기존 `--org-kli` 들이 정의된 자리에 같은 형식으로 더한다:

```css
  --org-bok: #2f6f4f;
```

`domains/reports/app/js/screens/home.js:15` 의 `ORGS`:

```javascript
const ORGS = [
  ['kli', 'KLI'], ['keis', 'KEIS'], ['kdi', 'KDI'], ['kiet', 'KIET'], ['bok', 'BOK'],
];
```

- [ ] **Step 7: 통과를 확인한다**

Run: `python -m pytest -q domains/reports`
Expected: PASS 전부

- [ ] **Step 8: 한 게시판만 실제로 받아 본다**

```bash
python -m domains.reports.pipeline.collect --board bok-intl
```

국제경제이슈는 1건뿐이라 빨리 끝난다. `domains/reports/sources/raw/bok/` 에 파일이 생기고 레코드에 제목·발간일·상세주소가 들어 있는지 본다:

```bash
python -X utf8 -c "
import json, glob
for p in glob.glob('domains/reports/sources/raw/bok/*.json'):
    rows = json.load(open(p, encoding='utf-8'))
    print(p, len(rows))
    for r in rows[:2]:
        print('  ', r['published'], r['title'][:50], '| 초록', len(r.get('abstract') or ''), '| 목차', len(r.get('toc') or []))
"
```

- [ ] **Step 9: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 한국은행 게시판 여덟을 담는다

경제전망보고서·경제상황 평가·지역경제보고서·주력산업 모니터링·BOK 이슈노트·
이슈분석심층연구·국제경제이슈·조사연구자료. 2021년 이후 710건.

사이트의 게시판 22개 중 사람이 고른 여덟이다. 집계 게시판(간행물·조사연구·
지역연구자료 등 상위 메뉴)은 고르지 않았다 — 하위를 합쳐 보여주므로 담으면
중복된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/data/boards.json domains/reports/data/orgs.json domains/reports/pipeline/collect.py domains/reports/app/css/app.css domains/reports/app/js/screens/home.js domains/reports/tests/test_boards.py
```

---

## Task 4: 전량 수록으로 전환

**Files:**
- Modify: `domains/reports/pipeline/build.py:29-35`, `domains/reports/pipeline/models.py`, `domains/reports/pipeline/boards.py`
- Modify: `domains/reports/data/boards.json` (기존 11줄에서 `"filter": true` 제거)
- Test: `domains/reports/tests/test_build.py`

**Interfaces:**
- Consumes: `keywords.match`
- Produces: `build.judge(raw, board, kw) -> Report` — `employment` 없이 `matched` 만 붙인다

**왜:** 이 도메인은 취지가 바뀌었다. 거르는 일은 수록 단계가 아니라 추천 단계가 한다. 이미 긁어둔 583건(KDI 186·KIET 397)이 버려지고 있다.

- [ ] **Step 1: 기존 테스트를 새 전제로 고친다**

먼저 `domains/reports/tests/test_build.py:8-12` 의 두 게시판에서 `filter=` 인자를 지운다.
(pydantic 이 모르는 인자를 조용히 무시하므로 남겨 두면 테스트가 거짓으로 통과한다.)

```python
KLI_BOARD = Board(id='kli-research', org='kli', name='연구보고서',
                  series='연구보고서', list_url='https://x/1',
                  item_re=r'(\d+)')
KDI_BOARD = Board(id='kdi-report', org='kdi', name='연구보고서',
                  series='연구보고서', list_url='https://x/2',
                  item_re=r'(\d+)')
```

그리고 세 테스트를 지운다:
`test_filtered_board_drops_a_report_with_no_keyword`,
`test_filtered_board_keeps_a_report_that_matches`,
`test_changing_keywords_flips_the_verdict_without_recollecting`.

그 자리에 넣는다:

```python
def test_every_raw_record_is_kept_regardless_of_keywords():
    # 취지가 바뀌었다 — 거르는 일은 수록이 아니라 추천이 한다.
    rows = [raw('kdi-4', KDI_BOARD, '반도체 경기와 거시경제'),
            raw('kdi-5', KDI_BOARD, '청년고용 부진의 원인')]
    kept, _, _ = build.build(rows, [KDI_BOARD], KW)
    assert sorted(r.id for r in kept) == ['kdi-4', 'kdi-5']


def test_changing_keywords_flips_the_topic_tag_without_recollecting():
    # 재수집 없이 주제 태깅이 바뀌는 성질은 그대로 지킨다. raw 는 그대로고
    # keywords 만 바뀐다.
    rows = [raw('kdi-6', KDI_BOARD, '플랫폼 종사자 실태')]
    assert build.build(rows, [KDI_BOARD], KW)[0][0].matched == []
    wider = dict(KW, 고용형태=['플랫폼 종사자'])
    assert build.build(rows, [KDI_BOARD], wider)[0][0].matched == ['고용형태']


def test_the_employment_flag_is_gone_from_the_app_record():
    kept, _, _ = build.build([raw('kli-9', KLI_BOARD, '제목')], [KLI_BOARD], KW)
    assert 'employment' not in build._light(kept[0])
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_build.py`
Expected: FAIL — `kdi-4` 가 수록되지 않는다 / `employment` 가 아직 있다

- [ ] **Step 3: `Report` 에서 `employment` 를 없앤다**

`domains/reports/pipeline/models.py` 끝:

```python
class Report(RawReport):
    """build 가 주제 태그를 붙인 모양.

    수록 여부를 가르는 필드는 없다 — 전량 수록이다. 읽을 만한 것을 고르는 일은
    recommend 가 별도 파일에서 한다.
    """

    matched: list[str] = Field(default_factory=list)
```

- [ ] **Step 4: `build.judge` 를 고친다**

`domains/reports/pipeline/build.py`:

```python
def judge(raw: RawReport, board, kw: dict) -> Report:
    """주제 태그만 붙인다. 수록 여부는 가르지 않는다 — 전량 수록이다."""
    text = ' '.join([raw.title, raw.abstract or '', ' '.join(raw.toc)])
    return Report(**raw.model_dump(), matched=kw_mod.match(text, kw))
```

`build()` 안의 수록 조건을 없앤다:

```python
        rec = judge(raw, board, kw)
        kept.append(rec)
```

(`if rec.employment:` 줄을 지우고 무조건 담는다.)

모듈 docstring 첫 문단도 고친다 — "고용 여부를 가르는 것은 여기다"는 더 이상 사실이 아니다:

```python
"""raw + keywords.json → data/reports.json · abstracts.json · 결측률.

**전량 수록한다.** 이 사이트는 외부기관 보고서를 취합하는 아카이브이고, 읽을
만한 것을 고르는 일은 recommend 가 별도 파일에서 한다.

keywords 는 거르는 도구가 아니라 **분류하는 도구**다 — matched 가 주제 화면의
입구다. 키워드를 고치면 재수집 없이 여기서 수십 초에 다시 매겨진다.
"""
```

- [ ] **Step 5: `Board.filter` 를 없앤다**

`domains/reports/pipeline/boards.py` 의 `filter: bool = False` 줄을 지운다.
`domains/reports/data/boards.json` 에서 `"filter": true,` 를 전부 지운다 (11곳: kiet 6·kdi 5):

```bash
python -X utf8 - <<'PY'
import json, pathlib
p = pathlib.Path('domains/reports/data/boards.json')
rows = json.loads(p.read_text(encoding='utf-8'))
n = sum(1 for r in rows if r.pop('filter', None) is not None)
p.write_text(json.dumps(rows, ensure_ascii=False, indent=1) + '\n', encoding='utf-8')
print('filter 제거', n, '곳')
PY
```

**주의:** 위 스크립트는 `boards.json` 을 다시 써서 손으로 맞춘 들여쓰기가 바뀐다. 커밋 전에 `git diff` 로 내용만 바뀌었는지 확인한다.

- [ ] **Step 6: 통과를 확인한다**

Run: `python -m pytest -q domains/reports`
Expected: PASS 전부

- [ ] **Step 7: 실제로 빌드해 본다**

```bash
python -m domains.reports.pipeline.build
```

Expected: `built: 1533 reports` 안팎 (BOK 수집이 아직 덜 찼으면 그만큼 적다).
전에는 950건이었다.

- [ ] **Step 8: 커밋**

```bash
git commit -m "$(cat <<'EOF'
refactor(reports): 전량 수록으로 바꾼다 — 583건이 버려지고 있었다

이 사이트는 외부기관 보고서를 취합하는 아카이브다. 거르는 일은 수록
단계가 아니라 추천 단계가 한다. boards.json 의 filter 와 레코드의
employment 를 없앤다.

matched 는 남긴다. 키워드는 거르는 도구에서 분류하는 도구가 됐고,
주제 화면이 그걸로 돈다. 재수집 없이 다시 매겨지는 성질도 그대로다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/pipeline/build.py domains/reports/pipeline/models.py domains/reports/pipeline/boards.py domains/reports/data/boards.json domains/reports/tests/test_build.py
```

---

## Task 5: 같은 게시판 안 제목 중복을 없앤다

**Files:**
- Modify: `domains/reports/pipeline/build.py`
- Test: `domains/reports/tests/test_build.py`

**Interfaces:**
- Consumes: Task 4 의 `build.build`
- Produces: `build.dedupe(reports) -> list[Report]`

**왜:** 조사연구자료에 같은 연구를 두 지역본부가 각각 올린 7쌍이 있다(강원/강릉 4쌍, 광주전남/목포, 경기/강원). `nttId` 가 달라 id 로는 안 잡힌다. 게시판 간 중복은 0건이므로 **같은 게시판 안에서만** 본다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_the_same_study_posted_by_two_branches_is_kept_once():
    # 조사연구자료에서 실제로 7쌍 나온다 — 강원본부와 강릉본부가 같은 연구를
    # 각각 올린다. nttId 가 달라 id 로는 안 잡힌다.
    rows = [raw('bok-1', KDI_BOARD, '강원지역 주택가격이 실물경제에 미치는 영향 점검'),
            raw('bok-2', KDI_BOARD, '강원지역 주택가격이 실물경제에 미치는 영향 점검')]
    rows[0].published = '2023-07-18'
    rows[1].published = '2023-07-20'
    kept, _, _ = build.build(rows, [KDI_BOARD], KW)
    assert [r.id for r in kept] == ['bok-1']      # 이른 날짜가 원발행


def test_the_same_title_on_different_boards_is_kept_twice():
    # 게시판 간 중복은 실측 0건이었다. 우연히 제목이 같을 때 지우면 안 된다.
    other = KDI_BOARD.model_copy(update={'id': 'kdi-other'})
    rows = [raw('x-1', KDI_BOARD, '같은 제목'), raw('x-2', other, '같은 제목')]
    kept, _, _ = build.build(rows, [KDI_BOARD, other], KW)
    assert sorted(r.id for r in kept) == ['x-1', 'x-2']
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_build.py -k branches`
Expected: FAIL — 두 건이 다 남는다

- [ ] **Step 3: 중복 제거를 넣는다**

`build.py` 에 함수를 더하고 `build()` 안에서 부른다:

```python
def dedupe(reports: list[Report]) -> list[Report]:
    """같은 게시판에 제목이 똑같은 게 둘이면 이른 날짜 하나만 남긴다.

    조사연구자료에서 같은 연구를 두 지역본부가 각각 올린다(실측 7쌍). nttId 가
    달라 id 로는 안 잡힌다. **게시판 간에는 보지 않는다** — 고른 게시판 사이
    중복은 실측 0건이고, 우연한 동명이 지워지면 안 된다.
    """
    best: dict[tuple[str, str], Report] = {}
    for r in reports:
        key = (r.board, r.title.strip())
        prev = best.get(key)
        if prev is None or (r.published, r.id) < (prev.published, prev.id):
            best[key] = r
    return list(best.values())
```

`build()` 의 정렬 직전에 끼운다:

```python
    kept = dedupe(kept)
    kept.sort(key=lambda r: (r.published, r.id), reverse=True)
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest -q domains/reports`
Expected: PASS 전부

- [ ] **Step 5: 커밋**

```bash
git commit -m "$(cat <<'EOF'
fix(reports): 두 지역본부가 각각 올린 같은 연구를 한 번만 담는다

조사연구자료에 7쌍 있다(강원/강릉 4쌍, 광주전남/목포, 경기/강원).
nttId 가 달라 id 로는 안 잡힌다. 이른 날짜를 원발행으로 본다.

게시판 간에는 보지 않는다 — 고른 여덟 사이 중복은 실측 0건이고,
우연한 동명을 지우면 안 된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/pipeline/build.py domains/reports/tests/test_build.py
```

---

## Task 6: 데이터 전송 크기를 기록한다

**Files:**
- Modify: `domains/reports/pipeline/build.py`
- Test: `domains/reports/tests/test_build.py`

**Interfaces:**
- Produces: `last_run.json` 의 `sizes` — `{"reports_gzip": 79012, "abstracts_gzip": 1172480}`

**왜:** 수록이 2.4배로 늘면 `abstracts.json` 이 gzip 2.7MB 가 된다(실측 추정). 초록은 지연 로딩이라 첫 화면은 안 느려지지만 검색창을 누르는 순간 받는다. **미리 쪼개지 않고 숫자를 보고 정한다** — gzip 3MB 가 넘으면 그때 기관별로 쪼갠다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```python
def test_the_gzipped_size_of_each_data_file_is_recorded(tmp_path, monkeypatch):
    monkeypatch.setattr(build, 'DATA', tmp_path)
    monkeypatch.setattr(build.store, 'load_all_raw',
                        lambda: [raw('kli-20', KLI_BOARD, '제목', abstract='초록')])
    monkeypatch.setattr(build.boards_mod, 'load_boards', lambda: [KLI_BOARD])
    monkeypatch.setattr(build.kw_mod, 'load_keywords', lambda: KW)
    build.main([])
    last = json.loads((tmp_path / 'last_run.json').read_text(encoding='utf-8'))
    assert last['sizes']['reports_gzip'] > 0
    assert last['sizes']['abstracts_gzip'] > 0
```

`test_build.py` 맨 위에 `import json` 이 없으면 더한다.

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_build.py -k gzipped`
Expected: FAIL — `KeyError: 'sizes'`

- [ ] **Step 3: 크기를 잰다**

`build.py` 맨 위에 `import gzip` 을 더하고, `main()` 의 `last['reports'] = len(kept)` 옆에 붙인다:

```python
    def _gz(name: str) -> int:
        return len(gzip.compress((DATA / name).read_bytes()))

    # 초록은 검색창을 누를 때 통째로 받는다. 3MB 를 넘으면 기관별로 쪼갠다 —
    # 미리 쪼개면 검색 품질과 복잡도를 숫자가 나오기 전에 내주는 셈이다.
    last['sizes'] = {'reports_gzip': _gz('reports.json'),
                     'abstracts_gzip': _gz('abstracts.json')}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest -q domains/reports && python -m domains.reports.pipeline.build && python -X utf8 -c "import json; print(json.load(open('domains/reports/data/last_run.json', encoding='utf-8'))['sizes'])"`
Expected: PASS, 그리고 실제 크기 두 개가 찍힌다

- [ ] **Step 5: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 데이터 파일의 gzip 크기를 회차마다 기록한다

수록이 2.4배로 늘면 abstracts.json 이 gzip 2.7MB 가 된다. 검색창을
누르는 순간 받는 파일이다. 미리 쪼개지 않고 숫자를 보고 정한다 —
3MB 를 넘으면 기관별로 쪼갠다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/pipeline/build.py domains/reports/tests/test_build.py
```

---

## Task 7: 관심축 프로파일

**Files:**
- Create: `domains/reports/data/interests.md`
- Create: `domains/reports/pipeline/interests.py`
- Test: `domains/reports/tests/test_interests.py`

**Interfaces:**
- Produces: `interests.load(path=None) -> Profile`, `Profile.axes: list[str]`, `Profile.text: str`, `Profile.version: str` (내용 sha256 앞 8자)

**왜 버전이 해시인가:** 사람이 숫자를 올리는 걸 잊으면 프로파일이 바뀌었는데 옛 판정이 그대로 남는다. 내용에서 뽑으면 잊을 수가 없다.

- [ ] **Step 1: 프로파일을 쓴다**

`domains/reports/data/interests.md` — **이 파일이 추천 기준의 유일한 사본이다.**

```markdown
# 관심축

이 파일이 추천 기준의 유일한 사본이다. 고치면 프로파일 버전이 바뀌고 전체가
재판정된다(재수집은 필요 없다).

축 이름은 `## ` 로 시작하는 줄이다. 이름을 바꾸면 옛 판정의 axis 값이 유효하지
않게 되므로, 이름은 신중히 정하고 자주 바꾸지 않는다.

## AI·기술변화의 직종별 고용영향
산업이 아니라 직종(과업) 기준의 영향. AI 노출도, 자동화, 사무직 위축,
대체·보완 직종, 기술변화에 따른 숙련 수요 변화.

## 청년 고용
사무직 구직, 첫 일자리, 니트, 지역 이탈·정착, 청년 대상 타겟팅과 프로그램.

## 지역 노동시장
지역별 고용구조, 산업 특화·집중도, 미스매치, 지역 간 이동, 지역 고용위기.

## 산업·직종 구조
특정 산업의 고용구조, 산업분류(KSIC)와 직업분류(KECO·KSCO·ISCO) 연계,
직종 체계 자체에 대한 분석.

## 직업훈련과 취업성과
훈련수료자 취업률, 관련직종 판정, 훈련의 효과 추정, 창업·창직.

## 채용수요·구인구직
민간 채용공고와 공식 통계의 차이, 속보성 지표, 구인난, 채용 경로.

## 임금·근로조건
임금 개념과 포함범위, 임금격차, 시간제·근로시간, 고용형태별 격차.

## 고용행정데이터와 방법론
고용보험 DB·행정통계의 범위와 한계, 통계 간 비교 가능성, 인구효과/고용효과
분해·군집·분산분해 같은 분석기법, 지수 설계, 표본과 모집단 대표성.

# 닿지 않는 것

통화·금리·물가·환율, 재정, 부동산·가계부채, 무역·통상, 지급결제,
금융안정, 고용과 연결되지 않는 산업 기술동향·경기 전망.
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`domains/reports/tests/test_interests.py`:

```python
from domains.reports.pipeline import interests


def test_the_axis_names_are_read_from_the_headings():
    p = interests.load()
    assert 'AI·기술변화의 직종별 고용영향' in p.axes
    assert '고용행정데이터와 방법론' in p.axes
    assert len(p.axes) == 8


def test_the_excluded_section_is_not_an_axis():
    assert '닿지 않는 것' not in interests.load().axes


def test_the_version_comes_from_the_content(tmp_path):
    a = tmp_path / 'a.md'
    a.write_text('## 하나\n내용\n', encoding='utf-8')
    v1 = interests.load(a).version
    a.write_text('## 하나\n내용이 바뀌었다\n', encoding='utf-8')
    v2 = interests.load(a).version
    assert v1 != v2
    assert len(v1) == 8
```

- [ ] **Step 3: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_interests.py`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 4: 로더를 쓴다**

`domains/reports/pipeline/interests.py`:

```python
"""관심축 프로파일을 읽는다.

추천 기준은 코드가 아니라 data/interests.md 에 있다. 사람이 읽고 고치는
파일이고, 고치면 버전이 바뀌어 전체가 재판정된다.

버전은 내용에서 뽑는다 — 사람이 숫자를 올리는 걸 잊으면 프로파일이 바뀌었는데
옛 판정이 그대로 남는다.
"""
from __future__ import annotations

import hashlib
import re
from pathlib import Path

from pydantic import BaseModel

INTERESTS_PATH = Path(__file__).resolve().parent.parent / 'data' / 'interests.md'

_AXIS = re.compile(r'^##\s+(.+?)\s*$', re.M)


class Profile(BaseModel):
    text: str
    axes: list[str]
    version: str


def load(path: Path | str | None = None) -> Profile:
    p = Path(path or INTERESTS_PATH)
    text = p.read_text(encoding='utf-8')
    # '# 닿지 않는 것' 아래의 ## 은 축이 아니다. 1단계 제목에서 끊는다.
    head = re.split(r'^#\s+닿지 않는 것\s*$', text, flags=re.M)[0]
    axes = [m.group(1).strip() for m in _AXIS.finditer(head)]
    version = hashlib.sha256(text.encode('utf-8')).hexdigest()[:8]
    return Profile(text=text, axes=axes, version=version)
```

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_interests.py`
Expected: PASS (3건)

- [ ] **Step 6: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 관심축 프로파일 — 추천 기준의 유일한 사본

data/interests.md 에 축 여덟과 닿지 않는 것을 적는다. 사람이 읽고
고치는 파일이다. 버전은 내용 해시에서 뽑는다 — 숫자를 손으로 올리면
잊었을 때 프로파일이 바뀌었는데 옛 판정이 남는다.

공개 저장소이므로 센터명·프로젝트명은 담지 않는다. 주제 축만 남겼다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/data/interests.md domains/reports/pipeline/interests.py domains/reports/tests/test_interests.py
```

---

## Task 8: 추천 판정

**Files:**
- Create: `domains/reports/pipeline/recommend.py`
- Test: `domains/reports/tests/test_recommend.py`

**Interfaces:**
- Consumes: `interests.load()`, `models.Report`
- Produces:
  - `recommend.build_prompt(profile, rows) -> str`
  - `recommend.parse_response(body, n_expected, axes) -> list[Verdict]`
  - `recommend.Verdict = NamedTuple(n: int, pick: bool, axis: str, why: str)`
  - `recommend.judge(profile, rows, *, call=None, log=print) -> list[Verdict]`

**왜 이 모양인가:** 행통 `domains/press/pipeline/llm_cite.py` 와 같은 계약이다. 판정과 근거를 함께 받고, 빠진 항목은 에러로 올린다 — 조용히 `pick: false` 로 두면 누락이 안 보인다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`domains/reports/tests/test_recommend.py`:

```python
"""추천 판정. call 을 주입하므로 네트워크를 쓰지 않는다."""
import json
import re

import pytest

from domains.reports.pipeline import recommend
from domains.reports.pipeline.interests import Profile

PROFILE = Profile(text='## 청년 고용\n청년 일자리.\n## 임금·근로조건\n임금 격차.\n',
                  axes=['청년 고용', '임금·근로조건'], version='abcd1234')

ROWS = [
    {'id': 'bok-1', 'org': 'BOK', 'series': 'BOK 이슈노트',
     'title': '청년고용 위축, AI탓인가?', 'abstract': '경력 사다리 변화를 분석한다.'},
    {'id': 'kdi-2', 'org': 'KDI', 'series': '연구보고서',
     'title': '가계부채 리스크 평가', 'abstract': '가구 단위 신용위험.'},
]


def test_the_prompt_carries_the_profile_and_numbers_every_row():
    p = recommend.build_prompt(PROFILE, ROWS)
    assert '청년 고용' in p
    assert '1. ' in p and '2. ' in p
    assert '청년고용 위축' in p
    # id 를 모델에 주지 않는다 — 지어내면 엉뚱한 레코드에 붙는다.
    assert 'bok-1' not in p


def test_a_well_formed_response_is_read():
    body = json.dumps([
        {'n': 1, 'pick': True, 'axis': '청년 고용', 'why': '청년 경력 사다리를 분해'},
        {'n': 2, 'pick': False, 'axis': '', 'why': '가계부채 — 고용과 무관'},
    ], ensure_ascii=False)
    got = recommend.parse_response(body, 2, PROFILE.axes)
    assert got[0].pick is True and got[0].axis == '청년 고용'
    assert got[1].pick is False


def test_a_fenced_response_is_read():
    body = '```json\n[{"n":1,"pick":true,"axis":"청년 고용","why":"ㄱ"},' \
           '{"n":2,"pick":false,"axis":"","why":"ㄴ"}]\n```'
    assert len(recommend.parse_response(body, 2, PROFILE.axes)) == 2


def test_a_missing_row_is_an_error_not_a_silent_false():
    body = json.dumps([{'n': 1, 'pick': True, 'axis': '청년 고용', 'why': 'ㄱ'}])
    with pytest.raises(ValueError, match='빠진'):
        recommend.parse_response(body, 2, PROFILE.axes)


def test_an_unknown_axis_is_an_error():
    body = json.dumps([{'n': 1, 'pick': True, 'axis': '없는축', 'why': 'ㄱ'}],
                      ensure_ascii=False)
    with pytest.raises(ValueError, match='축'):
        recommend.parse_response(body, 1, PROFILE.axes)


def test_a_pick_without_a_reason_is_an_error():
    body = json.dumps([{'n': 1, 'pick': True, 'axis': '청년 고용', 'why': ''}],
                      ensure_ascii=False)
    with pytest.raises(ValueError, match='이유'):
        recommend.parse_response(body, 1, PROFILE.axes)


def test_judge_splits_into_batches_and_renumbers_globally():
    # 25건이면 20 + 5 두 묶음이다. 묶음마다 n 은 1부터 다시 세지만,
    # 돌려줄 때는 전체 기준 번호여야 호출자가 id 에 되맞출 수 있다.
    sizes = []

    def fake_call(prompt):
        n = sum(1 for line in prompt.splitlines()
                if re.match(r'^\d+\. \[', line))
        sizes.append(n)
        return json.dumps([{'n': i + 1, 'pick': False, 'axis': '', 'why': 'ㄱ'}
                           for i in range(n)], ensure_ascii=False)

    rows = [dict(ROWS[0], id=f'r-{i}') for i in range(25)]
    got = recommend.judge(PROFILE, rows, call=fake_call, log=lambda *_: None)
    assert sizes == [20, 5]
    assert [v.n for v in got] == list(range(1, 26))
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_recommend.py`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 판정기를 쓴다**

`domains/reports/pipeline/recommend.py` (윗부분만 — 캐시는 Task 9):

```python
# -*- coding: utf-8 -*-
"""보고서가 읽을 만한지 LLM 이 판정한다.

이 도메인은 전량 수록한다. 거르는 일은 수록이 아니라 여기가 한다.

키워드 단독으로는 안 된다 — 실측에서 '고용현황'·'청년층 고용' 같은 말투를
통째로 놓쳤다. 제목만 임베딩한 유사도도 기각했다: 양성 평균(0.612)이 음성
상위 10%(0.635)보다 낮아 임계값으로 자를 수 없었다(스펙 §3-6).

판정과 근거를 함께 받는다. 근거가 없으면 왜 틀렸는지 아무도 못 본다.
"""
from __future__ import annotations

import sys

if hasattr(sys.stdout, 'reconfigure'):
    sys.stdout.reconfigure(encoding='utf-8', errors='replace')

import json
import os
import re
from typing import Callable, NamedTuple, Sequence

import requests

from .interests import Profile

ANTHROPIC_URL = 'https://api.anthropic.com/v1/messages'
OPENROUTER_URL = 'https://openrouter.ai/api/v1/chat/completions'
MODEL_ANTHROPIC = 'claude-opus-5'
MODEL_OPENROUTER = 'anthropic/claude-opus-5'
MAX_TOKENS = 4000
TIMEOUT = 180
BATCH = 20                  # 한 번에 판정할 보고서 수
ABSTRACT_CAP = 800          # 초록을 이보다 길게 보내지 않는다

_FENCE = re.compile(r'```(?:json)?\s*(.*?)\s*```', re.S)
_WS = re.compile(r'\s+')


class Verdict(NamedTuple):
    n: int          # 묶음 안의 번호 (1부터). 호출자가 순서로 id 에 되맞춘다
    pick: bool
    axis: str
    why: str


def build_prompt(profile: Profile, rows: Sequence[dict]) -> str:
    lines = []
    for i, r in enumerate(rows, start=1):
        abstract = _WS.sub(' ', (r.get('abstract') or ''))[:ABSTRACT_CAP]
        head = f'{i}. [{r.get("org", "")} · {r.get("series", "")}] {r["title"]}'
        lines.append(f'{head}\n   {abstract}' if abstract else head)
    listing = '\n'.join(lines)
    axes = '\n'.join(f'- {a}' for a in profile.axes)
    return f"""고용노동부에서 노동시장을 분석하는 사람이 읽을 보고서를 고른다.

그 사람의 관심축은 이렇다:

{profile.text}

아래는 외부 연구기관이 낸 보고서 목록이다. 각 보고서가 **위 관심축에 닿고
실제로 읽을 가치가 있는지** 판정하라.

판정 기준:
- 관심축에 닿으면서 새 데이터·새 분석·정책 함의가 있으면 고른다.
- 같은 낱말을 써도 **다른 것을 말하면 고르지 않는다** — 금융안정 맥락의
  '가계', 물가 맥락의 '임금' 처럼.
- 정기 통계 공표·동향 요약처럼 **읽을 새 내용이 없으면** 고르지 않는다.
- 초록이 없어 제목만 있는 것은 제목이 분명히 관심축에 닿을 때만 고른다.
- 애매하면 고르지 않는 쪽으로 판정하라. 다만 그 이유를 적어라.

축 이름은 아래 중 하나를 **그대로** 쓴다. 고르지 않으면 빈 문자열로 둔다:
{axes}

보고서 목록:
{listing}

각 보고서마다 한 줄씩, JSON 배열로만 답하라. 다른 말을 덧붙이지 마라:
[{{"n": 1, "pick": true, "axis": "청년 고용", "why": "청년 경력 사다리를 직종 기준으로 분해"}}]

`why` 는 25자 안팎으로 짧게. 고르지 않으면 무엇을 다룬 보고서인지 적어라.
"""


def parse_response(body: str, n_expected: int, axes: Sequence[str]) -> list[Verdict]:
    """JSON 배열을 읽는다. 못 읽으면 ValueError — 조용히 넘기지 않는다."""
    text = (body or '').strip()
    fenced = _FENCE.search(text)
    if fenced:
        text = fenced.group(1)
    try:
        rows = json.loads(text)
    except json.JSONDecodeError as exc:
        raise ValueError(f'응답을 JSON 으로 읽지 못했다: {body[:200]}') from exc
    if not isinstance(rows, list):
        raise ValueError(f'배열이 아니다: {body[:200]}')

    known = set(axes)
    out, seen = [], set()
    for row in rows:
        if not isinstance(row, dict):
            raise ValueError(f'항목이 객체가 아니다: {row!r}')
        try:
            n = int(row['n'])
        except (KeyError, TypeError, ValueError) as exc:
            raise ValueError(f'n 을 읽지 못했다: {row!r}') from exc
        if not (1 <= n <= n_expected):
            raise ValueError(f'n 이 범위 밖이다({n}, 1~{n_expected}): {row!r}')
        if n in seen:
            raise ValueError(f'n 이 중복이다: {n}')
        seen.add(n)
        if 'pick' not in row:
            raise ValueError(f'pick 이 없다: {row!r}')
        pick = bool(row['pick'])
        axis = str(row.get('axis', '')).strip()
        why = str(row.get('why', '')).strip()
        if pick and axis not in known:
            raise ValueError(f'모르는 축이다({axis!r}): {row!r}')
        if not why:
            raise ValueError(f'이유가 비었다: {row!r}')
        out.append(Verdict(n, pick, axis if pick else '', why))
    missing = sorted(set(range(1, n_expected + 1)) - seen)
    if missing:
        # 빠진 보고서를 조용히 '고르지 않음' 으로 두면 누락이 안 보인다.
        raise ValueError(f'판정이 빠진 보고서가 있다: {missing}')
    return sorted(out)


def provider() -> tuple[str, str, dict] | None:
    """(url, model, headers) — 있는 키를 쓴다. 없으면 None."""
    key = os.environ.get('ANTHROPIC_API_KEY', '').strip()
    if key:
        return (ANTHROPIC_URL, MODEL_ANTHROPIC,
                {'x-api-key': key, 'anthropic-version': '2023-06-01',
                 'content-type': 'application/json'})
    key = os.environ.get('OPENROUTER_API_KEY', '').strip()
    if key:
        return (OPENROUTER_URL, MODEL_OPENROUTER,
                {'Authorization': f'Bearer {key}', 'content-type': 'application/json'})
    return None


def _call_api(prompt: str) -> str:
    got = provider()
    if got is None:
        raise RuntimeError('ANTHROPIC_API_KEY 도 OPENROUTER_API_KEY 도 없다')
    url, model, headers = got
    payload = {'model': model, 'max_tokens': MAX_TOKENS,
               'messages': [{'role': 'user', 'content': prompt}]}
    resp = requests.post(url, headers=headers, json=payload, timeout=TIMEOUT)
    if resp.status_code != 200:
        raise ValueError(f'API 가 {resp.status_code} 를 돌려줬다: {resp.text[:300]}')
    data = resp.json()
    try:
        if 'content' in data:                        # Anthropic Messages
            return ''.join(b.get('text', '') for b in data['content'])
        return data['choices'][0]['message']['content']
    except (KeyError, TypeError, IndexError, AttributeError) as exc:
        raise ValueError(f'200 응답인데 형식이 예상과 다르다: {resp.text[:300]}') from exc


def judge(profile: Profile, rows: Sequence[dict], *,
          call: Callable[[str], str] | None = None,
          log: Callable[[str], None] = print) -> list[Verdict]:
    """보고서 전체를 BATCH 씩 나눠 판정한다. n 은 전체 기준으로 돌려준다."""
    call = call or _call_api
    out: list[Verdict] = []
    for start in range(0, len(rows), BATCH):
        chunk = list(rows[start:start + BATCH])
        log(f'  판정 {start + 1}~{start + len(chunk)} / {len(rows)}')
        got = parse_response(call(build_prompt(profile, chunk)),
                             len(chunk), profile.axes)
        out.extend(Verdict(v.n + start, v.pick, v.axis, v.why) for v in got)
    return out
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_recommend.py`
Expected: PASS (7건)

- [ ] **Step 5: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): LLM 추천 판정 — 판정과 근거를 함께 받는다

관심축 프로파일을 프롬프트에 싣고 20건씩 묶어 판정한다. 모델에 보고서
id 를 주지 않는다 — 지어내면 조용히 엉뚱한 레코드에 붙는다. 묶음 안
번호로 받아 호출자가 되맞춘다.

빠진 항목·모르는 축·빈 이유는 전부 에러다. 조용히 '고르지 않음' 으로
두면 누락이 안 보인다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/pipeline/recommend.py domains/reports/tests/test_recommend.py
```

---

## Task 9: 판정 캐시와 재판정

**Files:**
- Modify: `domains/reports/pipeline/recommend.py`
- Test: `domains/reports/tests/test_recommend.py`

**Interfaces:**
- Consumes: Task 8 의 `judge`, `interests.load()`
- Produces:
  - `recommend.pending(reports, cache, profile) -> list[dict]`
  - `recommend.merge(cache, rows, verdicts, profile) -> dict`
  - `recommend.main(argv=None, *, call=None) -> int`

**캐시 레코드:**

```json
{"pick": true, "axis": "청년 고용", "why": "청년 경력 사다리를 직종 기준으로 분해",
 "basis": "abstract", "model": "claude-opus-5",
 "profile_version": "abcd1234", "judged_at": "2026-09-11T11:40:00+09:00"}
```

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test_recommend.py` 끝에 붙인다:

```python
def test_an_already_judged_report_is_not_asked_again():
    cache = {'bok-1': {'pick': True, 'axis': '청년 고용', 'why': 'ㄱ',
                       'basis': 'abstract', 'model': 'claude-opus-5',
                       'profile_version': 'abcd1234', 'judged_at': 'x'}}
    todo = recommend.pending(ROWS, cache, PROFILE)
    assert [r['id'] for r in todo] == ['kdi-2']


def test_a_changed_profile_forces_a_rejudge():
    cache = {'bok-1': {'pick': True, 'axis': '청년 고용', 'why': 'ㄱ',
                       'basis': 'abstract', 'model': 'claude-opus-5',
                       'profile_version': '옛버전', 'judged_at': 'x'}}
    todo = recommend.pending(ROWS, cache, PROFILE)
    assert sorted(r['id'] for r in todo) == ['bok-1', 'kdi-2']


def test_the_basis_records_whether_an_abstract_was_available():
    rows = [dict(ROWS[0]), dict(ROWS[1], abstract='')]
    verdicts = [recommend.Verdict(1, True, '청년 고용', 'ㄱ'),
                recommend.Verdict(2, False, '', 'ㄴ')]
    cache = recommend.merge({}, rows, verdicts, PROFILE)
    assert cache['bok-1']['basis'] == 'abstract'
    assert cache['kdi-2']['basis'] == 'title'


def test_merge_keeps_earlier_entries_for_reports_not_rejudged():
    old = {'old-1': {'pick': False, 'axis': '', 'why': 'ㄱ', 'basis': 'title',
                     'model': 'claude-opus-5', 'profile_version': 'abcd1234',
                     'judged_at': 'x'}}
    cache = recommend.merge(old, [ROWS[0]], [recommend.Verdict(1, True, '청년 고용', 'ㄴ')],
                            PROFILE)
    assert 'old-1' in cache and 'bok-1' in cache
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest -q domains/reports/tests/test_recommend.py -k "cache or judged or basis or merge"`
Expected: FAIL — `AttributeError: ... 'pending'`

- [ ] **Step 3: 캐시를 붙인다**

`recommend.py` 끝에:

```python
from datetime import datetime, timedelta, timezone
from pathlib import Path

KST = timezone(timedelta(hours=9))
DATA = Path(__file__).resolve().parent.parent / 'data'


def load_cache(path: Path | str | None = None) -> dict:
    p = Path(path or (DATA / 'recommendations.json'))
    if not p.exists():
        return {}
    return json.loads(p.read_text(encoding='utf-8'))


def pending(reports: Sequence[dict], cache: dict, profile: Profile) -> list[dict]:
    """아직 안 물어본 것과, 프로파일이 바뀐 뒤로 안 물어본 것."""
    todo = []
    for r in reports:
        got = cache.get(r['id'])
        if got is None or got.get('profile_version') != profile.version:
            todo.append(r)
    return todo


def merge(cache: dict, rows: Sequence[dict], verdicts: Sequence[Verdict],
          profile: Profile) -> dict:
    """판정을 캐시에 얹는다. 이번에 안 물어본 것은 그대로 둔다."""
    now = datetime.now(KST).isoformat(timespec='seconds')
    out = dict(cache)
    for v in verdicts:
        row = rows[v.n - 1]
        out[row['id']] = {
            'pick': v.pick, 'axis': v.axis, 'why': v.why,
            # 초록이 없어 제목만으로 판정한 것을 표시한다. 나중에 원문에서
            # 초록을 채우면 그 레코드만 다시 물어보면 된다.
            'basis': 'abstract' if (row.get('abstract') or '').strip() else 'title',
            'model': MODEL_ANTHROPIC,
            'profile_version': profile.version,
            'judged_at': now,
        }
    return out


def main(argv=None, *, call=None) -> int:
    import argparse

    from . import interests as interests_mod

    ap = argparse.ArgumentParser()
    ap.add_argument('--limit', type=int, default=0, help='이번에 판정할 최대 건수')
    ap.add_argument('--dry-run', action='store_true')
    args = ap.parse_args(argv)

    profile = interests_mod.load()
    reports = json.loads((DATA / 'reports.json').read_text(encoding='utf-8'))
    abstracts = json.loads((DATA / 'abstracts.json').read_text(encoding='utf-8'))
    rows = [{'id': r['id'], 'org': r['org'], 'series': r['series'], 'title': r['title'],
             'abstract': (abstracts.get(r['id']) or {}).get('abstract', '')}
            for r in reports]

    cache = load_cache()
    todo = pending(rows, cache, profile)
    if args.limit:
        todo = todo[:args.limit]
    print(f'판정 대상 {len(todo)} / 전체 {len(rows)} (프로파일 {profile.version})')
    if args.dry_run or not todo:
        return 0

    verdicts = judge(profile, todo, call=call)
    cache = merge(cache, todo, verdicts, profile)
    (DATA / 'recommendations.json').write_text(
        json.dumps(cache, ensure_ascii=False, indent=1) + '\n', encoding='utf-8')
    picked = sum(1 for v in cache.values() if v.get('pick'))
    print(f'추천 {picked} / 판정 {len(cache)}')

    last_path = DATA / 'last_run.json'
    last = json.loads(last_path.read_text(encoding='utf-8')) if last_path.exists() else {}
    last['recommend'] = {'judged': len(cache), 'picked': picked,
                         'profile_version': profile.version,
                         'title_only': sum(1 for v in cache.values()
                                           if v.get('basis') == 'title')}
    last_path.write_text(json.dumps(last, ensure_ascii=False, indent=2) + '\n',
                         encoding='utf-8')
    return 0


if __name__ == '__main__':
    raise SystemExit(main())
```

`from typing import Callable, NamedTuple, Sequence` 는 이미 있다. 파일 맨 위 import 블록에 `datetime`·`Path` 를 올려도 되고 위치는 자유다 — 다만 모듈 맨 위에 모으는 편이 읽기 좋다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest -q domains/reports`
Expected: PASS 전부

- [ ] **Step 5: 대상 수를 눈으로 본다 (네트워크 없음)**

```bash
python -m domains.reports.pipeline.recommend --dry-run
```

Expected: `판정 대상 1533 / 전체 1533 (프로파일 xxxxxxxx)` 안팎

- [ ] **Step 6: 20건만 실제로 판정해 본다**

API 키가 필요하다. 본체 폴더의 `.env` 를 끌어다 쓴다(워크트리에는 없다):

```bash
set -a && . ~/Desktop/고용데이터아카이브/.env && set +a
python -m domains.reports.pipeline.recommend --limit 20
```

**결과를 눈으로 읽는다.** 이 단계가 프로파일 품질의 첫 검증이다:

```bash
python -X utf8 -c "
import json
c = json.load(open('domains/reports/data/recommendations.json', encoding='utf-8'))
r = {x['id']: x for x in json.load(open('domains/reports/data/reports.json', encoding='utf-8'))}
for rid, v in c.items():
    mark = 'O' if v['pick'] else '.'
    print(f\"{mark} [{v['axis'] or '-'}] {r[rid]['title'][:46]} — {v['why']}\")
"
```

고른 것이 엉뚱하거나 놓친 게 많으면 `interests.md` 를 고치고 다시 돌린다.
버전이 바뀌므로 전체가 재판정 대상이 된다 — `--limit` 으로 다시 20건만 본다.

- [ ] **Step 7: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 추천 판정 캐시 — 프로파일이 바뀌면 다시 묻는다

id 로 캐시하고 profile_version 이 다르면 재판정한다. 초록 없이 제목만으로
판정한 것은 basis 로 표시한다 — 나중에 원문에서 초록을 채우면 그 레코드만
다시 물어보면 된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/pipeline/recommend.py domains/reports/tests/test_recommend.py domains/reports/data/recommendations.json domains/reports/data/last_run.json
```

---

## Task 10: 워크플로에 추천 단계를 잇는다

**Files:**
- Modify: `.github/workflows/collect-reports.yml`

**Interfaces:**
- Consumes: Task 9 의 `python -m domains.reports.pipeline.recommend`

- [ ] **Step 1: 단계를 끼운다**

`.github/workflows/collect-reports.yml` 의 `초록 채우기` 단계와 `Commit data` 사이에 넣는다:

```yaml
      - name: 빌드
        env:
          PYTHONIOENCODING: utf-8
        run: python -m domains.reports.pipeline.build
      - name: 추천 판정
        env:
          PYTHONIOENCODING: utf-8
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        # 새로 들어온 것만 판정한다(id 캐시). 실패해도 그날 수집을 버리지
        # 않는다 — 아카이브 본체는 키 없이도 돌고, 추천은 다음 회차에 붙는다.
        continue-on-error: true
        run: python -m domains.reports.pipeline.recommend
```

**`빌드` 단계가 없었다면 같이 넣는다.** 추천은 `reports.json` 을 읽으므로 빌드가 먼저 돌아야 한다. 기존 워크플로에 빌드 단계가 이미 있으면 중복해 넣지 말고 순서만 확인한다:

```bash
grep -n "pipeline.build\|pipeline.enrich\|pipeline.recommend" .github/workflows/collect-reports.yml
```

- [ ] **Step 2: 커밋할 파일 목록에 새 데이터가 포함되는지 확인한다**

`Commit data` 단계의 `git add domains/reports/data/ domains/reports/sources/` 가 `recommendations.json` 과 `interests.md` 를 이미 덮는다. 고칠 것 없다.

- [ ] **Step 3: YAML 이 유효한지 확인한다**

```bash
python -X utf8 -c "
import yaml
d = yaml.safe_load(open('.github/workflows/collect-reports.yml', encoding='utf-8'))
names = [s.get('name') or s.get('run', '')[:30] for s in d['jobs']['collect']['steps']]
print(*names, sep='\n')
"
```

Expected: 단계 이름이 순서대로 찍히고 `추천 판정` 이 `Commit data` 앞에 있다.

- [ ] **Step 4: 시크릿이 저장소에 있는지 확인한다**

```bash
gh secret list | grep -E "ANTHROPIC|OPENROUTER" || echo "없음 — 사용자에게 알린다"
```

없으면 **여기서 멈추고 사용자에게 알린다.** 값을 지어내지 않는다. 행통 도메인이 같은 키를 쓰므로 이미 있을 수 있다.

- [ ] **Step 5: 커밋**

```bash
git commit -m "$(cat <<'EOF'
ci(reports): 수집 뒤에 빌드와 추천 판정을 잇는다

추천은 continue-on-error 다. 아카이브 본체는 API 키 없이도 돌고,
추천이 실패하면 다음 회차에 붙는다 — 그날 수집을 버리지 않는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- .github/workflows/collect-reports.yml
```

---

## Task 11: 추천 탭

**Files:**
- Create: `domains/reports/app/js/screens/picks.js`
- Modify: `domains/reports/app/js/route.js`, `domains/reports/app/js/app.js`, `domains/reports/app/index.html`
- Test: `domains/reports/tests/web/picks.test.mjs`

**Interfaces:**
- Consumes: `ctx.reports`, `ctx.picks`(= `recommendations.json` 내용), `ui.reportRow`
- Produces: `picks.render(el, ctx)`, `picks.groupByAxis(reports, picks) -> [{axis, rows}]`

- [ ] **Step 1: 기존 화면 테스트의 형식을 본다**

```bash
sed -n 1,40p domains/reports/tests/web/screens.test.mjs
```

`node:test` + `node:assert/strict` 이고, 화면 모듈에서 **순수 함수를 이름으로 가져와** DOM 없이 잰다(`homeRows`·`topicChips`·`detailModel`). 같은 결로 쓴다.

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`domains/reports/tests/web/picks.test.mjs`:

```javascript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { groupByAxis } from '../../app/js/screens/picks.js';

const REPORTS = [
  { id: 'a', org: 'bok', title: 'ㄱ', published: '2026-01-01' },
  { id: 'b', org: 'bok', title: 'ㄴ', published: '2026-03-01' },
  { id: 'c', org: 'kli', title: 'ㄷ', published: '2026-02-01' },
];

test('축은 건수 순, 축 안은 최신 순', () => {
  const picks = {
    a: { pick: true, axis: '청년 고용', why: 'ㄱ' },
    b: { pick: true, axis: '청년 고용', why: 'ㄴ' },
    c: { pick: true, axis: '임금·근로조건', why: 'ㄷ' },
  };
  const got = groupByAxis(REPORTS, picks);
  assert.deepEqual(got.map((g) => g.axis), ['청년 고용', '임금·근로조건']);
  assert.deepEqual(got[0].rows.map((r) => r.id), ['b', 'a']);
});

test('고르지 않은 보고서는 없다', () => {
  const picks = { a: { pick: false, axis: '', why: '무관' } };
  assert.deepEqual(groupByAxis(REPORTS, picks), []);
});

test('이유가 행에 실려 온다', () => {
  const picks = { a: { pick: true, axis: '청년 고용', why: '경력 사다리 분해' } };
  assert.equal(groupByAxis(REPORTS, picks)[0].rows[0].why, '경력 사다리 분해');
});

test('추천이 아직 없어도 죽지 않는다', () => {
  assert.deepEqual(groupByAxis(REPORTS, undefined), []);
  assert.deepEqual(groupByAxis(undefined, {}), []);
});
```

- [ ] **Step 3: 실패를 확인한다**

Run: `node --test "domains/reports/tests/web/picks.test.mjs"`
Expected: FAIL — `Cannot find module ... picks.js`

- [ ] **Step 4: 화면을 쓴다**

`domains/reports/app/js/screens/picks.js`:

```javascript
// 추천 — 전량 아카이브의 입구다.
// 2천 건이 넘는 타임라인에서 읽을 것을 찾는 건 사람 일이 아니다. 여기서 관심축
// 별로 묶어 내놓고, 왜 골랐는지를 같이 적는다. 이유가 없으면 추천을 믿을 근거가
// 없다.
import { esc } from '../data.js';
import { reportRow, empty } from '../ui.js';

export function groupByAxis(reports, picks) {
  const byAxis = new Map();
  for (const r of reports || []) {
    const p = (picks || {})[r.id];
    if (!p || !p.pick) continue;
    const axis = p.axis || '그 밖';
    if (!byAxis.has(axis)) byAxis.set(axis, []);
    byAxis.get(axis).push({ ...r, why: p.why });
  }
  for (const rows of byAxis.values()) {
    rows.sort((a, b) => (a.published < b.published ? 1 : -1));
  }
  return [...byAxis.entries()]
    .map(([axis, rows]) => ({ axis, rows }))
    .sort((a, b) => b.rows.length - a.rows.length || (a.axis < b.axis ? -1 : 1));
}

export function render(el, ctx) {
  const groups = groupByAxis(ctx.reports, ctx.picks);
  const total = groups.reduce((n, g) => n + g.rows.length, 0);
  if (!total) {
    el.innerHTML = empty('아직 추천할 보고서가 없습니다.');
    return;
  }
  el.innerHTML = `
    <div class="section-title">추천 · ${total}건</div>
    ${groups.map((g) => `
      <div class="axis">
        <div class="axis__name">${esc(g.axis)} <span class="axis__count">${g.rows.length}</span></div>
        <div class="list">
          ${g.rows.map((r) => `
            ${reportRow(r)}
            <div class="why">${esc(r.why)}</div>`).join('')}
        </div>
      </div>`).join('')}`;
}
```

- [ ] **Step 5: 라우트와 탭을 잇는다**

`domains/reports/app/js/route.js` — 기존 `topics`·`orgs` 가 등록된 자리와 같은 형식으로 `picks` 를 더한다. 파일을 열어 기존 형식을 그대로 따른다:

```bash
cat domains/reports/app/js/route.js
```

`domains/reports/app/js/app.js`:

```javascript
import * as picks from './screens/picks.js';
```

`screens` 객체에 `picks` 를 더하고, 초기 로드에 한 줄을 더한다:

```javascript
  const [reports, boards, orgList, lastRun, picksData] = await Promise.all([
    loadJson('./data/reports.json'),
    loadJson('./data/boards.json'),
    loadJson('./data/orgs.json'),
    loadJson('./data/last_run.json'),
    loadJson('./data/recommendations.json'),
  ]);
```

`ctx` 에 `picks: picksData || {}` 를 더한다.

**추천 파일이 아직 없을 수 있다.** `loadJson` 이 404 에서 어떻게 도는지 확인하고, 없으면 `{}` 가 되게 한다:

```bash
grep -n "loadJson" core/shell.js domains/reports/app/js/*.js | head
sed -n "$(grep -n 'function loadJson' core/shell.js | cut -d: -f1),+12p" core/shell.js
```

`domains/reports/app/index.html` — 탭바에 `추천` 을 `홈` 다음에 넣는다. 기존 탭 마크업을 그대로 복사해 `href="#/picks"` 로 바꾼다.

`app.css` 에 새 클래스 셋을 더한다(기존 `.section-title`·`.list` 와 같은 결로):

```css
.axis { margin-bottom: 22px; }
.axis__name { font-weight: 600; margin: 0 0 6px; }
.axis__count { color: var(--muted); font-weight: 400; font-size: 0.88em; }
.why { color: var(--muted); font-size: 0.86em; margin: -4px 0 10px 2px; }
```

- [ ] **Step 6: 통과를 확인한다**

Run: `node --test "domains/reports/tests/web/*.test.mjs" && python -m pytest -q domains/reports`
Expected: PASS 전부

- [ ] **Step 7: 화면을 실제로 연다**

```bash
python -m http.server 8765 --directory domains/reports/app
```

브라우저에서 `http://localhost:8765/#/picks` 를 열어 축별로 묶여 나오는지, 이유가 붙는지, 좁은 폭(400px)에서 안 깨지는지 본다. 확인 후 서버를 끈다.

- [ ] **Step 8: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 추천 탭 — 관심축별로 묶고 왜 골랐는지 적는다

2천 건이 넘는 타임라인에서 읽을 것을 찾는 건 사람 일이 아니다.
이유를 같이 내놓지 않으면 추천을 믿을 근거가 없다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/app/ domains/reports/tests/web/
```

---

## Task 12: 홈 검색창 아래 추천 섹션과 타임라인 배지

**Files:**
- Modify: `domains/reports/app/js/screens/home.js`
- Test: `domains/reports/tests/web/screens.test.mjs` (기존 파일에 붙인다 — 홈 테스트가 이미 거기 있다)

**Interfaces:**
- Consumes: Task 11 의 `ctx.picks`
- Produces: `home.topPicks(reports, picks) -> rows` — 발행일 내림차순 6건, 한 기관 최대 2건

**왜 기관 상한이 있나:** BOK 이슈노트가 가장 많이 추천될 것이다. 상한이 없으면 홈의 추천 여섯 줄이 한 기관으로 찬다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`domains/reports/tests/web/screens.test.mjs` 의 import 줄에 `topPicks` 를 더하고, 홈 구역 끝에 붙인다:

```javascript
import { homeRows, headHtml, bodyHtml, topPicks } from '../../app/js/screens/home.js';
```

```javascript
test('홈 추천은 여섯 줄까지다', () => {
  const reports = Array.from({ length: 10 }, (_, i) => ({
    id: `r${i}`, org: 'bok', title: `ㄱ${i}`, published: `2026-01-${String(i + 1).padStart(2, '0')}`,
  }));
  const picks = Object.fromEntries(
    reports.map((r) => [r.id, { pick: true, axis: '청년 고용', why: 'ㄱ' }]));
  assert.ok(topPicks(reports, picks).length <= 6);
});

test('한 기관이 홈 추천을 다 채우지 못한다', () => {
  const reports = [
    ...Array.from({ length: 5 }, (_, i) => ({
      id: `b${i}`, org: 'bok', title: `ㄱ${i}`, published: `2026-02-${String(i + 1).padStart(2, '0')}`,
    })),
    { id: 'k1', org: 'kli', title: 'ㄴ', published: '2026-01-01' },
  ];
  const picks = Object.fromEntries(
    reports.map((r) => [r.id, { pick: true, axis: '청년 고용', why: 'ㄱ' }]));
  const rows = topPicks(reports, picks);
  assert.ok(rows.filter((r) => r.org === 'bok').length <= 2);
  assert.ok(rows.some((r) => r.org === 'kli'));
});

test('홈 추천은 최신 순이다', () => {
  const reports = [
    { id: 'a', org: 'bok', title: 'ㄱ', published: '2026-01-01' },
    { id: 'b', org: 'kli', title: 'ㄴ', published: '2026-03-01' },
  ];
  const picks = {
    a: { pick: true, axis: '청년 고용', why: 'ㄱ' },
    b: { pick: true, axis: '청년 고용', why: 'ㄴ' },
  };
  assert.deepEqual(topPicks(reports, picks).map((r) => r.id), ['b', 'a']);
});

test('추천 파일이 없어도 홈이 죽지 않는다', () => {
  assert.deepEqual(topPicks(CTX.reports, undefined), []);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test "domains/reports/tests/web/screens.test.mjs"`
Expected: FAIL — `topPicks is not a function`

- [ ] **Step 3: `home.js` 에 더한다**

```javascript
const HOME_PICKS = 6;
const PER_ORG = 2;   // BOK 이슈노트가 여섯 줄을 통째로 채우는 걸 막는다

export function topPicks(reports, picks) {
  const picked = (reports || [])
    .filter((r) => (picks || {})[r.id] && picks[r.id].pick)
    .sort((a, b) => (a.published < b.published ? 1 : -1));
  const out = [];
  const seen = new Map();
  for (const r of picked) {
    const n = seen.get(r.org) || 0;
    if (n >= PER_ORG) continue;
    seen.set(r.org, n + 1);
    out.push({ ...r, why: picks[r.id].why, axis: picks[r.id].axis });
    if (out.length >= HOME_PICKS) break;
  }
  return out;
}
```

`bodyHtml` 의 타임라인 앞에 섹션을 그린다(검색 중일 때는 그리지 않는다 — 검색 결과가 주인공이다):

```javascript
  const picks = topPicks(ctx.reports, ctx.picks);
  const picksHtml = picks.length ? `
    <div class="section-title">
      읽을 만한 보고서
      <a class="more" href="#/picks">더보기 ›</a>
    </div>
    <div class="list">
      ${picks.map((r) => `${reportRow(r)}<div class="why">${esc(r.why)}</div>`).join('')}
    </div>` : '';
```

타임라인 행에 배지를 붙인다 — `ui.js` 의 `reportRow` 가 `r.picked` 를 보고 표시를 그리게 하고, `homeRows` 가 타임라인 레코드에 `picked` 를 얹는다. `ui.js` 를 열어 기존 마크업에 맞춰 넣는다:

```bash
sed -n 1,60p domains/reports/app/js/ui.js
```

`esc` 가 `home.js` 에 아직 import 돼 있지 않으면 `../data.js` 에서 가져온다.

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test "domains/reports/tests/web/*.test.mjs" && python -m pytest -q domains/reports`
Expected: PASS 전부

- [ ] **Step 5: 화면을 연다**

```bash
python -m http.server 8765 --directory domains/reports/app
```

`http://localhost:8765/` 에서 — 검색창 아래 추천 여섯 줄이 있고, 검색어를 치면 사라지고, 지우면 돌아오고, 타임라인 행에 배지가 붙는지 본다. **한글을 한 글자씩 쳐서 조합이 안 끊기는지 반드시 확인한다**(`home.js` 주석이 경고하는 버그다).

- [ ] **Step 6: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 홈 검색창 아래 추천 여섯 줄과 타임라인 배지

한 기관 최대 2건으로 끊는다 — BOK 이슈노트가 가장 많이 추천될 텐데
상한이 없으면 여섯 줄이 한 기관으로 찬다.

검색 중에는 그리지 않는다. 머리는 그대로 두고 본문만 갈아 끼우는
기존 경계를 지킨다 — 화면을 통째로 갈면 한글 조합이 끊긴다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/app/ domains/reports/tests/web/
```

---

## Task 13: 보고서 상세에 추천 축과 이유

**Files:**
- Modify: `domains/reports/app/js/screens/report.js`
- Test: `domains/reports/tests/web/screens.test.mjs` (상세 테스트가 이미 거기 있다)

**Interfaces:**
- Consumes: `ctx.picks`
- Produces: `detailModel(id, ctx)` 의 반환에 `pick` 이 붙는다 — 추천됐으면 `{axis, why}`, 아니면 `null`

**왜 `detailModel` 에 넣나:** 이 화면은 순수 함수(`detailModel`)와 그리기(`render`)가 나뉘어 있고 테스트가 순수 함수 쪽을 잰다. 그 경계를 지킨다.

- [ ] **Step 1: 기존 상세 화면과 테스트를 읽는다**

```bash
sed -n 1,30p domains/reports/app/js/screens/report.js
grep -n "detailModel" domains/reports/tests/web/screens.test.mjs
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`screens.test.mjs` 의 상세 구역에 붙인다:

```javascript
test('추천된 보고서는 축과 이유를 들고 온다', () => {
  const ctx = { ...CTX, picks: { a: { pick: true, axis: '청년 고용', why: '경력 사다리 분해' } } };
  const model = detailModel('a', ctx);
  assert.equal(model.pick.axis, '청년 고용');
  assert.equal(model.pick.why, '경력 사다리 분해');
});

test('고르지 않은 보고서는 pick 이 없다', () => {
  const ctx = { ...CTX, picks: { a: { pick: false, axis: '', why: '무관' } } };
  assert.equal(detailModel('a', ctx).pick, null);
});

test('추천 파일이 없어도 상세가 죽지 않는다', () => {
  assert.equal(detailModel('a', CTX).pick, null);
});
```

- [ ] **Step 3: 실패를 확인한다**

Run: `node --test "domains/reports/tests/web/screens.test.mjs"`
Expected: FAIL — `Cannot read properties of undefined (reading 'axis')`

- [ ] **Step 4: 상세에 붙인다**

`report.js` 의 `detailModel` 반환에 한 줄을 더한다:

```javascript
  const p = (ctx.picks || {})[report.id];
  return {
    report,
    abstract: (body && body.abstract) || '',
    toc: (body && body.toc) || [],
    links,
    // 목록에서 이유를 보고 들어온 사람이 여기서 그걸 다시 못 보면 왜 여기
    // 있는지 잊는다.
    pick: p && p.pick ? { axis: p.axis, why: p.why } : null,
  };
```

`render` 의 제목 아래에 그린다:

```javascript
  const pickHtml = model.pick ? `
    <div class="pick-note">
      <span class="pick-note__axis">${esc(model.pick.axis)}</span>
      <span class="pick-note__why">${esc(model.pick.why)}</span>
    </div>` : '';
```

`app.css`:

```css
.pick-note { display: flex; gap: 8px; align-items: baseline; margin: 8px 0 14px; flex-wrap: wrap; }
.pick-note__axis { font-size: 0.8em; padding: 2px 8px; border-radius: 999px;
                   background: var(--chip-bg); color: var(--chip-fg); }
.pick-note__why { color: var(--muted); font-size: 0.9em; }
```

`--chip-bg`·`--chip-fg` 가 없으면 기존 칩 스타일이 쓰는 변수 이름을 `app.css` 에서 찾아 그걸 쓴다:

```bash
grep -n "chip" domains/reports/app/css/app.css | head
```

- [ ] **Step 5: 통과를 확인한다**

Run: `node --test "domains/reports/tests/web/*.test.mjs" && python -m pytest -q domains/reports`
Expected: PASS 전부

- [ ] **Step 6: 화면을 연다**

추천된 보고서 하나를 열어 축 칩과 이유가 보이는지, 좁은 폭에서 줄바꿈되는지 본다.

- [ ] **Step 7: 커밋**

```bash
git commit -m "$(cat <<'EOF'
feat(reports): 보고서 상세에 추천 축과 이유를 보인다

목록에서 이유를 보고 들어온 사람이 상세에서 그걸 다시 못 보면
왜 여기 있는지 잊는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/app/ domains/reports/tests/web/
```

---

## Task 14: 첫 전체 판정과 회차 점검

**Files:**
- Modify: `domains/reports/data/recommendations.json` (산출물)
- Modify: `domains/reports/pipeline/check_run.py` (필요하면)

**Interfaces:**
- Consumes: Task 9·10 의 전부

- [ ] **Step 1: BOK 수집이 얼마나 찼는지 본다**

```bash
python -X utf8 -c "
import json
last = json.load(open('domains/reports/data/last_run.json', encoding='utf-8'))
for bid, r in sorted(last.get('boards', {}).items()):
    if bid.startswith('bok-'):
        print(f\"{bid:15s} ok={r['ok']} 파싱={r['parsed']:4d} 추가={r['added']:3d} 하한도달={r['reached_cutoff']}\")
"
```

`reached_cutoff` 가 전부 `True` 여야 2021년까지 찬 것이다.

**첫 백필은 로컬에서 한 번에 받는다.** 회차당 게시판별 40건 상한은 게시판 예의를 위한
것이지 기술적 제약이 아니고, 워크플로에 맡기면 조사연구자료 403건이 11회차(=11일)
걸린다. 첫 백필은 한 번뿐이므로 상한을 올려 받는다:

```bash
python -m domains.reports.pipeline.collect --org bok --cap 500
```

710건을 요청 간 1초 지연으로 받으므로 20~40분 걸린다. **지연을 줄이지 않는다** —
`DELAY = 1.0` 은 남의 서버에 대한 예의다.

받는 중간에 실패하면 그때까지 받은 것은 raw 에 남아 있다. 다시 돌리면 이미 받은 것은
건너뛴다(`known` 집합). 게시판 하나만 다시 받으려면 `--board bok-research`.

- [ ] **Step 2: 전체 판정을 돌린다**

키가 필요하다. 약 $8 든다(2,243건 기준).

```bash
set -a && . ~/Desktop/고용데이터아카이브/.env && set +a
python -m domains.reports.pipeline.build
python -m domains.reports.pipeline.recommend
```

- [ ] **Step 3: 결과를 사람이 읽는다**

```bash
python -X utf8 -c "
import json, collections
c = json.load(open('domains/reports/data/recommendations.json', encoding='utf-8'))
r = {x['id']: x for x in json.load(open('domains/reports/data/reports.json', encoding='utf-8'))}
picked = {k: v for k, v in c.items() if v['pick']}
print('추천', len(picked), '/ 판정', len(c))
print('축별', dict(collections.Counter(v['axis'] for v in picked.values())))
print('기관별', dict(collections.Counter(r[k]['org'] for k in picked)))
print('제목만으로 판정', sum(1 for v in c.values() if v['basis'] == 'title'))
print()
for k, v in list(picked.items())[:30]:
    print(f\"[{v['axis']}] {r[k]['title'][:50]} — {v['why']}\")
"
```

**여기서 멈추고 사용자에게 보여준다.** 프로파일이 맞는지는 사람만 안다. 고칠 데가 있으면 `interests.md` 를 고치고 재판정한다(재수집 불필요, 약 $8 다시 든다 — `--limit` 으로 일부만 먼저 보는 게 낫다).

- [ ] **Step 4: 회차 점검이 BOK 게시판을 아는지 확인한다**

```bash
python -m domains.reports.pipeline.check_run
```

게시판 단위 실패 규칙은 둘뿐이다(요청 실패, 200인데 파싱 0건). **신규 0건은 통과다** — 주력산업 모니터링은 2025.2월 이후 멈췄고 국제경제이슈는 1건뿐이라 몇 달씩 조용한 게 정상이다. `check_run.py` 가 이를 이미 그렇게 다루는지 확인하고, 아니면 고친다.

- [ ] **Step 5: 커밋**

```bash
git commit -m "$(cat <<'EOF'
data(reports): 전량 아카이브 첫 판정

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- domains/reports/data/
```

---

## Task 15: 문서 갱신과 병합

**Files:**
- Modify: `README.md` (연구보고서 도메인 소개가 있으면)
- Modify: `domains/reports/data/_check.txt` (있으면 형식을 보고 갱신)

- [ ] **Step 1: 도메인 소개가 옛 취지를 말하고 있는지 본다**

```bash
grep -rn "거르\|고용 관련\|키워드" README.md domains/reports/data/_check.txt 2>/dev/null | head
```

"고용 관련 보고서를 모은다"는 설명이 있으면 "외부기관 보고서를 전량 모으고 추천으로 고른다"로 고친다. 기관 넷을 세는 곳이 있으면 다섯으로 고친다.

- [ ] **Step 2: 전체 테스트**

Run: `python -m pytest -q domains/reports`
Expected: PASS 전부

- [ ] **Step 3: 커밋하고 병합한다**

```bash
git commit -m "$(cat <<'EOF'
docs(reports): 전량 아카이브로 바뀐 취지를 반영한다

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KguFebZsFeNvQfhAtaj9wh
EOF
)" -- README.md domains/reports/data/_check.txt
```

병합은 **본체 폴더에서, 한 번에 한 세션만** 한다:

```bash
cd ~/Desktop/고용데이터아카이브
git pull
git merge --no-ff reports/full-archive -m "merge: 연구보고서 전량 아카이브와 추천 (reports/full-archive)"
python -m pytest -q domains/reports
git push
```

푸시한 뒤 워크트리를 정리한다:

```bash
git worktree remove ../연구보고서-전량아카이브
git branch -d reports/full-archive
```

---

## 자체 점검 결과

스펙 대비로 훑은 것과 고친 것:

- **스펙 §4(전량 수록)** → Task 4. `filter`·`employment` 제거, `matched` 유지, 기존 테스트 갱신까지 덮는다
- **스펙 §5(BOK 수집기)** → Task 1·2·3
- **스펙 §6(추천)** → Task 7(프로파일)·8(판정)·9(캐시)·10(워크플로)
- **스펙 §7(화면)** → Task 11(추천 탭)·12(홈 섹션·배지)·13(상세)
- **스펙 §3-5(중복 제거)** → Task 5
- **스펙 §3-7(크기 관측)** → Task 6
- **스펙 §8(검증)** → 각 Task 에 테스트 단계로 들어갔고, 워크플로 배선은 Task 10, 첫 판정 확인은 Task 14
- **스펙 §9(범위 밖)** → 계획에 넣지 않았다: `enrich.py` 의 BOK 확장, 임베딩 검토 도구, `abstracts.json` 분할, 수집 하한 이동

이름 일관성: `Board.body`(Task 1 정의 → 2·3 사용), `interests.load()/Profile.version`(7 → 8·9), `recommend.Verdict`(8 → 9), `ctx.picks`(11 → 12·13), `groupByAxis`/`topPicks`/`detailModel.pick` 은 각각 한 곳에서만 정의된다.

점검에서 고친 것 넷:

1. **Task 2** — 고정본 뜨기에서 목록 주소를 상세 주소인 양 적어 뒀다. `parse_list` 로 상세 주소를 먼저 얻는 절차로 고쳤다.
2. **Task 4** — `test_build.py:8-12` 의 두 게시판이 `filter=` 를 넘긴다. 필드를 없애면 pydantic 이 **조용히 무시**하므로 테스트가 거짓으로 통과한다. 인자를 지우는 단계를 넣었다.
3. **Task 8** — 묶음 분할 테스트가 가짜 응답 크기를 바깥 변수에서 읽는 엉성한 모양이었다. 프롬프트에서 항목 수를 세게 고쳤다.
4. **Task 11·12·13** — 화면 테스트를 pytest 로 쓰고 있었는데 이 저장소의 화면 테스트는 `node:test` `.mjs` 다(`pages.yml` 이 `domains/reports/tests/web/*.test.mjs` 를 돌린다). 셋 다 실제 형식으로 다시 썼고, Task 13 은 `detailModel` 이 이미 순수 함수/그리기로 나뉘어 있어 `pick` 을 모델 쪽에 넣도록 바꿨다.
