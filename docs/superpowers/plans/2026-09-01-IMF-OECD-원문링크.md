# IMF·OECD 원문 링크 교체 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** IMF·OECD 레코드의 "원문 보기"가 JSON 대신 실제 보고서를 열게 한다.

**Architecture:** 수치는 지금처럼 API 로 받는다. 바꾸는 것은 `source_url`·`landing_url` 이 가리키는 곳뿐이다. IMF 는 발표일에서 회차 주소를 조립하고, OECD 는 연재 목록을 긁어 권·호별 주소를 찾는다. 이미 저장된 56건은 일회성 마이그레이션으로 고친다.

**Tech Stack:** Python 3.12, curl_cffi(기존 `http.py`), pydantic v2, pytest

**Spec:** `docs/superpowers/specs/2026-09-01-IMF-OECD-원문링크-design.md`

## Global Constraints

- **`source_url`·`landing_url` 에 기계용 주소를 저장하지 않는다. 예외 없다.** 보고서 주소를 구하지 못하면 API 주소로 대신 채우지 않고 그 회차 수집을 실패시킨다.
- 수치 수집 방식과 `confidence: "verified"` 는 건드리지 않는다.
- `store.merge` 규칙을 바꾸지 않는다. 기존 레코드 소급은 일회성 마이그레이션으로 한다.
- 마이그레이션은 `value`·`id`·`revision`·`prev_value` 를 한 글자도 바꾸지 않는다.
- 모든 주석·문서·에러 메시지는 한국어, 기존 수집기 문체(설명적이고 *왜* 를 적는다).
- 새 의존성 없음. `domains/forecast/app/`, `domains/employment/`, `hub/`, `core/`, `tools/` 를 건드리지 않는다.
- 테스트는 네트워크를 타지 않는다. 목록 HTML 은 픽스처로 고정한다.
- 커밋 메시지는 `feat(forecast):` / `fix(forecast):` 형태의 한국어.

## 파일 구조

| 파일 | 책임 |
|---|---|
| `domains/forecast/pipeline/collectors/imf.py` (수정) | 발표일·라벨에서 WEO 회차 주소를 조립 |
| `domains/forecast/pipeline/collectors/oecd.py` (수정) | 연재 목록에서 권·호별 보고서 주소를 찾음 |
| `domains/forecast/pipeline/relink.py` (신규) | 기존 레코드의 링크만 고치는 일회성 마이그레이션 |
| `domains/forecast/tests/fixtures/oecd_serials.html` (신규) | 연재 목록 발췌 |
| `domains/forecast/tests/test_imf.py` · `test_oecd.py` · `test_relink.py` | 검증 |

---

## Task 1: IMF 회차 주소 조립

**Files:**
- Modify: `domains/forecast/pipeline/collectors/imf.py`
- Test: `domains/forecast/tests/test_imf.py`

**Interfaces:**
- Produces: `imf.report_url(label: str, published_at: date) -> str`

`imf.py` 를 먼저 읽는다. 회차 정보를 담은 곳이 **둘**이다 — 현행 DataMapper 용
`EDITIONS`(라벨 → 제목·발표일·마지막연도)와 보관 SDMX 용 `VINTAGES`(라벨 →
흐름ID·제목·발표일). 두 경로 모두 지금 API 주소를 `source_url` 에 넣고,
`LANDING_URL` 은 회차 구분이 없는 상수 하나다.

- [ ] **Step 1: 실패 테스트를 쓴다**

`domains/forecast/tests/test_imf.py` 에 이어 붙인다.

```python
from datetime import date


def test_report_url_builds_the_weo_issue_address():
    # 실제로 200 을 확인한 주소다(설계 3.2)
    assert imf.report_url("April 2026", date(2026, 4, 14)) == (
        "https://www.imf.org/en/publications/weo/issues/"
        "2026/04/14/world-economic-outlook-april-2026"
    )
    assert imf.report_url("October 2025", date(2025, 10, 14)) == (
        "https://www.imf.org/en/publications/weo/issues/"
        "2025/10/14/world-economic-outlook-october-2025"
    )


def test_report_url_marks_an_update_issue():
    # Update 회차는 슬러그에 -update- 가 들어간다
    assert imf.report_url("Update July 2026", date(2026, 7, 8)) == (
        "https://www.imf.org/en/publications/weo/issues/"
        "2026/07/08/world-economic-outlook-update-july-2026"
    )


def test_report_url_rejects_a_label_it_cannot_read():
    # 월 이름을 못 읽으면 조용히 이상한 주소를 만들지 않는다
    with pytest.raises(ValueError, match="회차 라벨"):
        imf.report_url("Spring 2026", date(2026, 4, 14))
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_imf.py -q -k report_url`
Expected: FAIL — `AttributeError: module ... has no attribute 'report_url'`

- [ ] **Step 3: 구현한다**

`imf.py` 에 이어 붙인다. `LANDING_URL` 상수는 지우지 않는다(Task 3 이 폴백으로
쓰지 않도록 확인한 뒤 마지막에 정리한다).

```python
WEO_ISSUE_BASE = "https://www.imf.org/en/publications/weo/issues"
_MONTHS = ("january", "february", "march", "april", "may", "june",
           "july", "august", "september", "october", "november", "december")


def report_url(label: str, published_at: date) -> str:
    """회차의 WEO 보고서 주소를 발표일에서 조립한다.

    IMF 는 회차 주소가 규칙적이라 목록을 긁을 필요가 없다. 라벨에 'Update' 가
    있으면 슬러그에 -update- 가 들어간다.

    주소가 실제로 열리는지 여기서 확인하지 않는다 — 매 수집마다 요청이 한 번 더
    붙고, 틀린 주소는 사람이 눌러 보면 즉시 드러난다. 회차를 추가할 때 그 주소를
    한 번 열어 보는 것으로 갈음한다.
    """
    lowered = label.lower()
    month = next((m for m in _MONTHS if m in lowered), None)
    if month is None:
        raise ValueError(f"회차 라벨에서 월을 읽지 못했다: {label!r}")
    kind = "world-economic-outlook-update" if "update" in lowered else "world-economic-outlook"
    return (f"{WEO_ISSUE_BASE}/{published_at:%Y/%m/%d}/"
            f"{kind}-{month}-{published_at:%Y}")
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_imf.py -q`
Expected: PASS

- [ ] **Step 5: 두 수집 경로가 이 주소를 쓰게 한다**

`parse()` 와 `parse_vintage()` 가 만드는 레코드의 `source_url`·`landing_url` 을
`report_url(...)` 결과로 바꾼다. `collect_vintage()` 가 `source_url` 을 SDMX 주소로
덮어쓰는 부분(`model_copy(update={"source_url": ...})`)을 지운다.

- [ ] **Step 6: 기계용 주소가 안 남았는지 테스트로 못 박는다**

```python
def test_no_record_points_at_a_machine_endpoint():
    # 설계 3.0 — 원문 보기가 JSON 을 띄우면 안 된다
    records = imf.parse("NGDP_RPCH", _SAMPLE_PAYLOAD, date(2026, 8, 31))
    assert records
    for r in records:
        for url in (r.source_url, r.landing_url):
            assert "api." not in url and "/api/" not in url and "sdmx" not in url
            assert url.startswith("https://www.imf.org/en/publications/weo/issues/")
```

`_SAMPLE_PAYLOAD` 는 이 파일에 이미 있는 픽스처를 쓴다. 없으면 기존 테스트가
쓰는 페이로드를 그대로 재사용한다.

- [ ] **Step 7: 전체 테스트**

Run: `python -m pytest domains/forecast -q`
Expected: PASS. 기존 IMF 테스트 중 `source_url` 이 API 주소임을 단언하던 것이
있으면 새 주소로 고친다 — 그 단언이 바로 이번에 바꾸는 동작이다.

- [ ] **Step 8: 커밋**

```bash
git add domains/forecast/pipeline/collectors/imf.py domains/forecast/tests/test_imf.py
git commit -m "feat(forecast): IMF 원문 링크를 WEO 회차 보고서로 바꾼다"
```

---

## Task 2: OECD 연재 목록에서 보고서 주소 찾기

**Files:**
- Modify: `domains/forecast/pipeline/collectors/oecd.py`
- Create: `domains/forecast/tests/fixtures/oecd_serials.html`
- Test: `domains/forecast/tests/test_oecd.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `oecd.volume_issue(edition: int) -> tuple[int, int]` — 회차 번호 → (권 연도, 호)
  - `oecd.parse_serials(page_html: str) -> dict[tuple[int, int], str]` — (권, 호) → 보고서 주소
  - `oecd.report_url(edition: int) -> str` — 네트워크

- [ ] **Step 1: 픽스처를 만든다**

`domains/forecast/tests/fixtures/oecd_serials.html` — 실제 연재 목록에서 딴
발췌다. 확인한 네 줄을 그대로 쓴다.

```html
<ul class="publication-list">
  <li><a href="/en/publications/oecd-economic-outlook-volume-2026-issue-1_2d1956f0-en.html">OECD Economic Outlook, Volume 2026 Issue 1</a></li>
  <li><a href="/en/publications/oecd-economic-outlook-volume-2025-issue-2_9f653ca1-en.html">OECD Economic Outlook, Volume 2025 Issue 2</a></li>
  <li><a href="/en/publications/oecd-economic-outlook-volume-2025-issue-1_83363382-en.html">OECD Economic Outlook, Volume 2025 Issue 1</a></li>
  <li><a href="/en/publications/oecd-economic-outlook-volume-2024-issue-2_d8814e8b-en.html">OECD Economic Outlook, Volume 2024 Issue 2</a></li>
</ul>
```

- [ ] **Step 2: 실패 테스트를 쓴다**

```python
SERIALS_HTML = (FIXTURES / "oecd_serials.html").read_text(encoding="utf-8")


def test_volume_issue_maps_the_edition_number():
    # 실제 대조로 확인한 대응이다(설계 3.3)
    assert oecd.volume_issue(116) == (2024, 2)
    assert oecd.volume_issue(117) == (2025, 1)
    assert oecd.volume_issue(118) == (2025, 2)
    assert oecd.volume_issue(119) == (2026, 1)
    assert oecd.volume_issue(120) == (2026, 2)


def test_parse_serials_reads_every_volume_issue():
    got = oecd.parse_serials(SERIALS_HTML)
    assert got[(2026, 1)] == (
        "https://www.oecd.org/en/publications/"
        "oecd-economic-outlook-volume-2026-issue-1_2d1956f0-en.html"
    )
    assert got[(2024, 2)].endswith("volume-2024-issue-2_d8814e8b-en.html")
    assert len(got) == 4


def test_parse_serials_raises_when_the_listing_has_none():
    with pytest.raises(ValueError, match="연재 목록"):
        oecd.parse_serials("<html><body>준비중</body></html>")
```

- [ ] **Step 3: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_oecd.py -q -k "volume_issue or serials"`
Expected: FAIL — `AttributeError`

- [ ] **Step 4: 구현한다**

```python
SERIALS_URL = f"{BASE}/en/publications/serials/oecd-economic-outlook_g1ghgh13.html"

# 회차 116 이 2024년 2호다. 이후 한 호씩 번갈아 올라간다.
_FIRST_EDITION = 116
_FIRST_VOLUME = (2024, 2)

_SERIAL_LINK = re.compile(
    r'href="(/en/publications/oecd-economic-outlook-volume-'
    r'(?P<year>\d{4})-issue-(?P<issue>\d)_[0-9a-f]+-en\.html)"')


def volume_issue(edition: int) -> tuple[int, int]:
    """회차 번호를 (권 연도, 호) 로 바꾼다.

    OECD 본편은 연 2회이고 회차 번호가 한 호마다 1씩 오른다. 번호에서 권·호를
    구할 수 있으므로 대응표를 따로 두지 않는다.
    """
    step = edition - _FIRST_EDITION
    year, issue = _FIRST_VOLUME
    total = (year * 2 + (issue - 1)) + step
    return total // 2, total % 2 + 1


def parse_serials(page_html: str) -> dict[tuple[int, int], str]:
    """연재 목록에서 (권, 호) -> 보고서 주소 를 만든다."""
    found = {
        (int(m.group("year")), int(m.group("issue"))): BASE + m.group(1)
        for m in _SERIAL_LINK.finditer(page_html)
    }
    if not found:
        raise ValueError("연재 목록에서 회차 링크를 찾지 못했다 — 서식이 바뀌었다")
    return found


def report_url(edition: int) -> str:
    """그 회차 보고서 주소를 준다. 목록에 없으면 실패시킨다.

    옛 회차 주소를 재활용하거나 기관 안내 페이지로 떨어뜨리지 않는다 — 둘 다
    독자를 다른 회차 문서로 데려간다(설계 4장).
    """
    key = volume_issue(edition)
    listing = parse_serials(http.get(SERIALS_URL).text)
    if key not in listing:
        raise ValueError(f"연재 목록에 EO {edition}(권 {key[0]} {key[1]}호)이 없다")
    return listing[key]
```

- [ ] **Step 5: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_oecd.py -q`
Expected: PASS

- [ ] **Step 6: 레코드가 이 주소를 쓰게 한다**

`_record(...)` 가 받는 `source_url` 과 `landing_url` 을 `report_url(edition)` 결과로
바꾼다. `parse()` 가 회차 번호를 이미 알고 있으므로 그 값을 넘긴다. `LANDING_URL`
상수를 레코드에 쓰지 않는다.

`report_url` 은 네트워크를 타므로 회차당 **한 번만** 부른다 — 레코드마다 부르면
한 회차에 수십 번 요청한다.

- [ ] **Step 7: 기계용 주소가 안 남았는지 테스트로 못 박는다**

```python
def test_no_record_points_at_a_machine_endpoint(monkeypatch):
    # 설계 3.0
    monkeypatch.setattr(oecd, "report_url",
                        lambda edition: "https://www.oecd.org/en/publications/x_1234abcd-en.html")
    records = oecd.parse(SAMPLE_CSV)
    assert records
    for r in records:
        for url in (r.source_url, r.landing_url):
            assert "sdmx" not in url and "/rest/data/" not in url
```

`SAMPLE_CSV` 는 이 파일에 이미 있는 픽스처를 쓴다.

- [ ] **Step 8: 전체 테스트**

Run: `python -m pytest domains/forecast -q`
Expected: PASS. 기존 OECD 테스트 중 `source_url` 이 SDMX 주소임을 단언하던 것은
새 동작에 맞춰 고친다.

- [ ] **Step 9: 커밋**

```bash
git add domains/forecast/pipeline/collectors/oecd.py domains/forecast/tests/test_oecd.py domains/forecast/tests/fixtures/oecd_serials.html
git commit -m "feat(forecast): OECD 원문 링크를 회차 보고서로 바꾼다"
```

---

## Task 3: 기존 56건 링크 마이그레이션

**Files:**
- Create: `domains/forecast/pipeline/relink.py`
- Test: `domains/forecast/tests/test_relink.py`

**Interfaces:**
- Consumes: `imf.report_url(label, published_at)`, `oecd.report_url(edition)`, `oecd.edition_number(report_title)`
- Produces: `relink.relink(records, resolvers) -> RelinkResult`

`store.merge` 는 값이 같으면 건너뛰므로 백필을 다시 돌려도 링크가 안 바뀐다.
그 규칙은 옳으니 건드리지 않고, 한 번 돌리는 도구로 푼다.

- [ ] **Step 1: 실패 테스트를 쓴다**

```python
from datetime import date, datetime

from domains.forecast.pipeline import relink
from domains.forecast.pipeline.models import ForecastRecord


def _rec(org, url, **kw):
    base = dict(
        id=f"{org.lower()}-2026-04-gdp_growth-2026", org=org, org_name_ko=org,
        report_title="IMF World Economic Outlook, April 2026",
        published_at=date(2026, 4, 14), target_year=2026, target_period="annual",
        indicator="gdp_growth", value=2.5, unit="%",
        source_url=url, landing_url=url, confidence="verified",
        collected_at=datetime(2026, 4, 15),
    )
    base.update(kw)
    return ForecastRecord(**base)


def test_relink_replaces_only_the_urls():
    rec = _rec("IMF", "https://www.imf.org/external/datamapper/api/v1/NGDP_RPCH/KOR")
    result = relink.relink([rec], {"IMF": lambda r: "https://www.imf.org/en/publications/weo/issues/x"})
    got = result.records[0]
    assert got.source_url == "https://www.imf.org/en/publications/weo/issues/x"
    assert got.landing_url == "https://www.imf.org/en/publications/weo/issues/x"
    # 수치와 식별자는 한 글자도 바뀌지 않는다
    assert (got.id, got.value, got.revision, got.prev_value) == (
        rec.id, rec.value, rec.revision, rec.prev_value)


def test_relink_leaves_other_orgs_alone():
    rec = _rec("BOK", "https://www.bok.or.kr/report.pdf", id="bok-1")
    result = relink.relink([rec], {"IMF": lambda r: "https://x"})
    assert result.records[0].source_url == "https://www.bok.or.kr/report.pdf"
    assert result.changed == 0


def test_relink_reports_records_it_could_not_resolve():
    rec = _rec("IMF", "https://www.imf.org/external/datamapper/api/v1/NGDP_RPCH/KOR")

    def failing(_):
        raise ValueError("회차를 찾지 못했다")

    result = relink.relink([rec], {"IMF": failing})
    assert result.changed == 0
    assert len(result.unresolved) == 1
    # 조용히 넘기지 않는다 — 원본은 그대로 남는다
    assert result.records[0].source_url == rec.source_url
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_relink.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named '...relink'`

- [ ] **Step 3: 구현한다**

```python
"""이미 저장된 레코드의 원문 링크만 고치는 일회성 도구.

store.merge 는 값이 같은 레코드를 건너뛴다 — 같은 값이면 같은 관측이고, 수집기가
돌 때마다 메타데이터가 흔들리면 안 되기 때문이다. 그 규칙은 옳다. 그래서 백필을
다시 돌려도 옛 레코드의 링크는 API 주소로 남는다.

이 도구가 그 구멍을 메운다. CI 에 넣지 않는다 — 사람이 한 번 돌린다.
"""
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Callable

from .models import ForecastRecord


@dataclass
class RelinkResult:
    records: list[ForecastRecord]
    changed: int = 0
    unresolved: list[tuple[str, str]] = field(default_factory=list)


def relink(records: list[ForecastRecord],
           resolvers: dict[str, Callable[[ForecastRecord], str]]) -> RelinkResult:
    """기관별 해결자로 원문 주소를 구해 링크만 바꾼다.

    수치·식별자·수정폭은 건드리지 않는다. 주소를 못 구한 레코드는 그대로 두고
    몇 건인지 남긴다 — 조용히 빠뜨리면 무엇이 안 고쳐졌는지 알 수 없다.
    """
    result = RelinkResult(records=[])
    cache: dict[tuple[str, str], str] = {}
    for rec in records:
        resolve = resolvers.get(rec.org)
        if resolve is None:
            result.records.append(rec)
            continue
        key = (rec.org, rec.report_title)
        try:
            url = cache[key] if key in cache else resolve(rec)
        except Exception as exc:
            result.unresolved.append((rec.id, f"{type(exc).__name__}: {exc}"))
            result.records.append(rec)
            continue
        cache[key] = url
        result.records.append(rec.model_copy(update={"source_url": url, "landing_url": url}))
        result.changed += 1
    return result
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest domains/forecast/tests/test_relink.py -q`
Expected: PASS

- [ ] **Step 5: 실행 진입점을 붙인다**

`relink.py` 끝에 `main()` 을 더한다. `forecasts.json` 을 읽어 IMF·OECD 해결자를
꽂고 돌린 뒤, **수치가 하나도 안 바뀌었는지 스스로 확인하고** 저장한다.

```python
def main(data_dir: Path = DATA_DIR) -> int:
    from .collectors import imf, oecd
    from . import store

    path = Path(data_dir) / "forecasts.json"
    before = store.load_forecasts(path)
    resolvers = {
        "IMF": lambda r: imf.report_url(_imf_label(r.report_title), r.published_at),
        "OECD": lambda r: oecd.report_url(oecd.edition_number(r.report_title)),
    }
    result = relink(before, resolvers)

    # 링크만 바꾸는 도구다. 수치가 한 건이라도 움직였으면 저장하지 않는다.
    same = {(r.id, r.value) for r in before} == {(r.id, r.value) for r in result.records}
    if not same:
        print("::error::수치가 바뀌었다 — 저장하지 않는다")
        return 1

    store.save_forecasts(path, result.records)
    print(f"링크 교체 {result.changed}건 / 못 찾음 {len(result.unresolved)}건")
    for rec_id, why in result.unresolved:
        print(f"  못 찾음 {rec_id}: {why}")
    return 0
```

`_imf_label` 도 같은 파일에 둔다. `oecd.edition_number(report_title)` 은
`oecd.py` 에 이미 있으니 그대로 쓴다.

```python
_IMF_LABEL = re.compile(r"(?:Update\s+)?(January|February|March|April|May|June|"
                        r"July|August|September|October|November|December)\s+(\d{4})",
                        re.IGNORECASE)


def _imf_label(report_title: str) -> str:
    """'IMF World Economic Outlook, April 2026' 에서 'April 2026' 을 뗀다.

    Update 회차는 제목에 Update 가 들어가므로 라벨에도 남긴다 — imf.report_url
    이 그 낱말로 슬러그를 가른다.
    """
    m = _IMF_LABEL.search(report_title)
    if m is None:
        raise ValueError(f"IMF 회차 제목에서 라벨을 읽지 못했다: {report_title!r}")
    prefix = "Update " if "update" in report_title.lower() else ""
    return f"{prefix}{m.group(1)} {m.group(2)}"
```

OECD Interim 레코드는 `org` 가 `OECD` 라 같은 해결자에 걸린다. `report_title` 에
`Interim` 이 있으면 건너뛰도록 해결자에서 거른다 — 이미 보고서를 가리키고 있다.

- [ ] **Step 6: 전체 테스트**

Run: `python -m pytest domains/forecast -q`
Expected: PASS

- [ ] **Step 7: 커밋**

```bash
git add domains/forecast/pipeline/relink.py domains/forecast/tests/test_relink.py
git commit -m "feat(forecast): 기존 레코드의 원문 링크를 고치는 일회성 도구"
```

---

## Task 4: 실제 실행과 확인

**Files:** 없음 (실행·검증만). 데이터 커밋은 컨트롤러가 한다.

- [ ] **Step 1: 놓친 IMF 회차가 있는지 본다**

설계 3.2 가 남긴 항목이다. 목록에 2026년 7월 WEO Update 가 있는데 아카이브 최신은
2026년 4월이다.

Run:
```bash
python -c "
import sys; sys.path.insert(0,'.')
from datetime import date
from domains.forecast.pipeline.collectors import imf
print(sorted(imf.EDITIONS), sorted(imf.VINTAGES))
print(imf.collect(date.today())[:1])
"
```

7월 Update 가 빠져 있다면 **그 사실만 보고한다.** 회차를 새로 추가하는 것은 이
계획의 범위가 아니다 — 별도 판단이 필요하다.

- [ ] **Step 2: 마이그레이션을 돌린다**

Run: `python -m domains.forecast.pipeline.relink`

Expected: `링크 교체 56건 / 못 찾음 0건` 안팎. 못 찾은 건이 있으면 그 목록을
보고하고 **멈춘다** — 원인을 보기 전에 진행하지 않는다.

- [ ] **Step 3: 결과를 대조한다**

Run:
```bash
python -c "
import json,sys; sys.stdout.reconfigure(encoding='utf-8')
rows=json.load(open('domains/forecast/data/forecasts.json',encoding='utf-8'))
bad=[r for r in rows if any(k in (r['source_url']+r['landing_url']) for k in ('api.','sdmx','/api/','/rest/data/'))]
print('기계용 주소가 남은 레코드:', len(bad))
for r in bad[:5]: print('  ', r['org'], r['source_url'][:80])
for org in ('IMF','OECD'):
    s={(r['report_title'], r['source_url']) for r in rows if r['org']==org}
    print(f'--- {org}')
    for t,u in sorted(s): print(f'   {t[:45]:45s} {u[:80]}')
"
```

Expected: **기계용 주소가 남은 레코드 0건.** 하나라도 남으면 멈추고 보고한다.

- [ ] **Step 4: 수치가 안 바뀌었는지 확인한다**

Run: `git diff --stat domains/forecast/data/forecasts.json`

그리고 값이 그대로인지 직접 본다.

```bash
python -c "
import json,subprocess,sys
old=json.loads(subprocess.run(['git','show','HEAD:domains/forecast/data/forecasts.json'],capture_output=True,text=True,encoding='utf-8').stdout)
new=json.load(open('domains/forecast/data/forecasts.json',encoding='utf-8'))
a={(r['id'],r['value']) for r in old}; b={(r['id'],r['value']) for r in new}
print('레코드 수', len(old), '->', len(new))
print('수치 동일:', a==b)
"
```

Expected: 레코드 수가 같고 `수치 동일: True`.

- [ ] **Step 5: 보고**

바뀐 링크 예시와 위 확인 결과를 보고한다. **데이터 파일은 커밋하지 않는다** —
컨트롤러가 확인 후 커밋한다.

---

## Task 5: 문서 갱신

**Files:**
- Modify: `README.md`
- Modify: `고용전망_아카이브_기획서.md`

- [ ] **Step 1: README 의 전망 수집기 표를 고친다**

IMF·OECD 행의 비고에 원문 링크가 보고서를 가리킨다는 것을 적는다. 표 아래
문단에 API 수집기도 원문은 보고서를 가리킨다는 한 줄을 더한다 — 지금은 PDF
수집기만 설명한다.

- [ ] **Step 2: 기획서 3.2 스키마 표에 규칙을 적는다**

`source_url` 행 설명에 "기계용(API) 주소를 넣지 않는다 — 사람이 원문을 확인하는
링크다"를 더한다.

- [ ] **Step 3: 확인**

Run: `grep -n "datamapper\|sdmx" README.md 고용전망_아카이브_기획서.md`
Expected: API 주소를 원문으로 소개하는 서술이 남아 있지 않다.

- [ ] **Step 4: 커밋**

```bash
git add README.md 고용전망_아카이브_기획서.md
git commit -m "docs: 원문 링크가 보고서를 가리킨다는 규칙을 적는다"
```
