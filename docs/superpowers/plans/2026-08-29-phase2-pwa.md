# Phase 2: PWA 웹앱 (홈·기관·비교·타임라인) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `data/forecasts.json`을 읽어 4개 화면(홈·기관·비교·타임라인)으로 보여주는 설치형 PWA를 만들고 GitHub Pages 배포 워크플로까지 붙인다.

**Architecture:** 빌드 스텝 없는 순수 정적 앱. `web/`에 ES 모듈 vanilla JS — `data.js`(순수 파생 로직, node --test로 TDD) + `app.js`(해시 라우터·데이터 로딩·앱 셸) + 화면별 렌더 모듈 4개. 데이터는 상대경로 `./data/*.json` fetch (배포 시 data/를 사이트에 복사). 확정된 디자인 목업(.dc.html 4장)을 저장소에 커밋해 **픽셀 스펙**으로 사용한다.

**Tech Stack:** Vanilla JS(ES modules), CSS custom properties(다크모드), node:test(로직 테스트, 의존성 0), Service Worker + Web App Manifest, GitHub Actions(actions/deploy-pages).

**Spec:** `고용전망_아카이브_화면기획서.md`(화면 동작의 정본) + `고용전망_아카이브_기획서.md` 5장 + **확정 목업** `docs/design/*.dc.html`(Task 1에서 커밋 — 색·간격·구성의 정본)

## Global Constraints

- **빌드 도구·npm 의존성 금지.** ES 모듈 vanilla JS만. 로직 테스트는 `node --test`(내장 러너)로 실행.
- **디자인 토큰 (목업 확정값, CSS 변수로 정의):** 배경 `#eef0f3`, 카드 `#ffffff`, 테두리 `#e2e5ea`, 본문 `#191d24`, 보조 `#667085`, 흐림 `#98a2b3`, 액센트 `#23508f`, 액센트연 `#e7edf6`, 상향 `#c73e3a`, 하향 `#2f6bd0`, 배지 검증 `#17714a`/`#e5f3ec`, 자동 `#9a6b15`/`#faf0dc`, 확인 `#2458a6`/`#e5edfa`. 폰트 IBM Plex Sans KR(구글 폰트) + `'Malgun Gothic', system-ui, sans-serif` 폴백, 숫자는 `font-variant-numeric: tabular-nums`.
- **수정폭 표기:** 상향 ▲(빨강 `#c73e3a`) / 하향 ▼(파랑 `#2f6bd0`) / 유지·최초 —(회색). 삼각형은 인라인 SVG(목업과 동일 polygon), 색만으로 구분 금지(접근성).
- **날짜 라벨 룰링(레저 확정):** 기관 `method`가 `api`인 레코드의 `published_at`은 수집일이므로 화면에 **"수집"으로 라벨**(예: "2026.08.29 수집"), 그 외는 "발표". 발표일로 오인시키는 표기 금지.
- **하단 고지 문구(전 화면, 원문 그대로):** "본 서비스는 개인이 제작한 비공식 참고자료이며, 각 기관 원문이 정본입니다."
- **터치 타깃 44px 이상**(탭·행·버튼), 모바일 390px 기준 + 데스크톱은 max-width 480px 중앙 배치.
- **다크모드:** `prefers-color-scheme: dark`에서 토큰만 재정의(배경 `#15181d`, 카드 `#1e232b`, 테두리 `#2c333d`, 본문 `#e6e9ee`, 보조 `#8b95a3`, 액센트연 `#1d2c42`; 배지·상향/하향 색상은 동일 유지하되 배지 배경은 각 색의 어두운 변형 `#123526`/`#3a2d10`/`#152b4a`).
- **현재 실데이터(OECD·IMF, 각 1회차)만으로 모든 화면이 성립해야 함** — 빈 지표·회차 1개·미제공 기관의 예외 처리는 화면기획서 1.6/2.8 그대로.
- 데이터 fetch는 전부 상대경로 `./data/forecasts.json`, `./data/orgs.json`, `./data/indicators.json`, `./data/schedule.json`.
- 사용자 문자열은 전부 `esc()`로 이스케이프 후 innerHTML 조립.
- 커밋 메시지 끝: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

## File Structure

```
web/
├─ index.html            # 앱 셸 (헤더/화면 컨테이너/탭바/고지)
├─ manifest.webmanifest
├─ sw.js                 # 서비스워커 (셸 캐시 + 데이터 stale-while-revalidate)
├─ icons/icon-192.png, icon-512.png
├─ css/styles.css        # 토큰 + 공통 컴포넌트 + 다크모드
└─ js/
   ├─ data.js            # 순수 파생 로직 (node --test 대상)
   ├─ app.js             # 부트/라우터/상태/셸
   └─ screens/home.js, org.js, compare.js, timeline.js
data/schedule.json        # 다음 발표 예정 (수동 관리)
docs/design/              # 확정 목업 4장 + canvas.json (픽셀 스펙)
tests/web/data.test.mjs   # node --test
.github/workflows/pages.yml
.claude/launch.json       # 로컬 미리보기 (python -m http.server)
```

---

### Task 1: 목업 커밋 + 웹 스캐폴딩

**Files:**
- Create: `docs/design/` (목업 5개 파일 복사), `web/index.html`, `web/css/styles.css`, `data/schedule.json`, `.claude/launch.json`

**Interfaces:**
- Produces: `web/index.html`의 DOM 계약 — `#screen`(화면 컨테이너), `#tabbar`(탭 4개, `data-route` 속성), `#headerDate`(갱신 시각), `#offlineBanner`(숨김 배너). 이후 모든 화면 태스크가 이 id에 의존.
- Produces: `css/styles.css`의 클래스 계약 — `.card`, `.badge`(`.badge--verified/--extracted/--reviewed`), `.num`, `.pill`(`.pill--active`), `.delta-up/.delta-down/.delta-flat`, `.band`(요약 밴드), `.notice`(고지), `.tab`(`.tab--active`)

- [ ] **Step 1: 목업을 저장소에 커밋**

`.superpowers/design/`의 `Main.dc.html, Org.dc.html, Compare.dc.html, Timeline.dc.html, canvas.json` 5개 파일을 `docs/design/`으로 복사한다. (이후 태스크의 픽셀 스펙 — 구현자는 이 마크업의 구조·인라인 스타일 값을 그대로 옮긴다)

- [ ] **Step 2: `data/schedule.json` 작성**

```json
[
  { "org": "BOK", "org_name_ko": "한국은행", "report": "경제전망", "date": "2026-11-26" }
]
```

- [ ] **Step 3: `web/index.html` 작성**

셸 구조 (목업 공통 요소를 그대로, 화면 내용만 비움):

```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#23508f">
  <title>고용전망</title>
  <link rel="manifest" href="./manifest.webmanifest">
  <link rel="icon" href="./icons/icon-192.png">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+KR:wght@400;500;600;700&display=swap">
  <link rel="stylesheet" href="./css/styles.css">
</head>
<body>
  <div class="app">
    <header class="header">
      <div class="header__title" id="headerTitle">고용전망</div>
      <div class="header__meta"><span class="num" id="headerDate"></span></div>
    </header>
    <div id="offlineBanner" class="offline-banner" hidden>오프라인 · 마지막으로 받은 데이터를 표시 중</div>
    <main id="screen" class="screen"></main>
    <div class="notice">본 서비스는 개인이 제작한 비공식 참고자료이며, 각 기관 원문이 정본입니다.</div>
    <nav class="tabbar" id="tabbar">
      <a class="tab" href="#/" data-route="home"><!-- 홈 SVG(목업 Main.dc.html 탭바와 동일) --><span>홈</span></a>
      <a class="tab" href="#/org" data-route="org"><span>기관</span></a>
      <a class="tab" href="#/compare" data-route="compare"><span>비교</span></a>
      <a class="tab" href="#/timeline" data-route="timeline"><span>타임라인</span></a>
    </nav>
  </div>
  <script type="module" src="./js/app.js"></script>
</body>
</html>
```

각 탭 `<a>` 안에 목업 탭바의 SVG 아이콘(홈/기관/비교/타임라인)을 그대로 넣는다. stroke 색은 하드코딩하지 말고 `stroke="currentColor"`로 바꿔 `.tab--active` 색상이 적용되게 한다.

- [ ] **Step 4: `web/css/styles.css` 작성**

Global Constraints의 토큰을 `:root` 변수로, 다크모드 재정의를 `@media (prefers-color-scheme: dark)`로. 목업의 인라인 스타일 값(카드 radius 10px, 패딩 12/14px, 헤더 구조, 탭바, 배지, pill, band, delta 색)을 클래스로 옮긴다. `.app`은 `max-width: 480px; margin: 0 auto; min-height: 100dvh; display: flex; flex-direction: column;`, `.screen`은 `flex: 1; overflow-y: auto;`. 터치 타깃: `.tab` 최소 높이 48px, 카드 행 최소 44px.

- [ ] **Step 5: `.claude/launch.json` 작성 (로컬 미리보기용)**

```json
{
  "version": "0.0.1",
  "configurations": [
    { "name": "pwa", "runtimeExecutable": "python", "runtimeArgs": ["-m", "http.server", "8642"], "port": 8642 }
  ]
}
```

주의: 서버 루트는 저장소 루트이므로 로컬 미리보기 URL은 `http://localhost:8642/web/`이고, 이때 `./data/...` fetch가 `web/data/`를 가리켜 404가 난다. 로컬에서도 동작하도록 **`app.js`의 데이터 경로는 `new URL('../data/…', import.meta.url)` 방식이 아니라** 단순 상대경로를 유지하되, Task 8 배포 스텝에서 data/를 사이트 루트의 `data/`로 복사하고, **로컬 검증 시에는 저장소 루트에서 서버를 띄우고 `web/` 경로로 접속하는 대신 `data` 심볼릭 복사 없이 `fetch('./data/…')`가 실패하면 `fetch('../data/…')`로 1회 폴백**하는 로더를 쓴다(Task 3 loadJson 코드에 포함됨).

- [ ] **Step 6: 정적 검증**

Run: `python -c "import pathlib; [print(p) for p in ['web/index.html','web/css/styles.css','data/schedule.json','docs/design/Main.dc.html'] if not pathlib.Path(p).exists()]"` → 출력 없음 (전부 존재)
Run: `node --check web/js/app.js 2>&1 || echo "app.js는 Task 3에서 생성 — OK"`

- [ ] **Step 7: 커밋**

```bash
git add docs/design web/index.html web/css data/schedule.json .claude/launch.json
git commit -m "feat: PWA 셸·디자인 토큰 + 확정 목업 커밋

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: 파생 로직 (`data.js`) — TDD

**Files:**
- Create: `web/js/data.js`
- Test: `tests/web/data.test.mjs`

**Interfaces:**
- Produces (모든 화면 태스크가 사용):
  - `latestRecords(records, {indicator, targetYear})` → org별 최신(published_at 최대) 레코드 배열, 발표일 내림차순 정렬
  - `summarize(latest)` → `{count, avg, max, min}` (avg 소수1자리 반올림, max/min은 레코드; latest 비면 null)
  - `seriesFor(records, {org, indicator, targetYear})` → published_at 오름차순 배열
  - `orgIndicators(records, org)` → 그 기관이 가진 indicator 코드 배열 (지표탭 노출용, 순서: emp_change, unemp_rate, gdp_growth, cpi, emp_rate, emp_rate_youth, labor_force 중 존재하는 것)
  - `compareSet(records, {indicator, targetYear, today, orgsMeta, filter})` → `[{rec, stale, monthLabel}]` 값 내림차순, `stale`= published_at이 today보다 90일 초과 과거, `filter`: 'all'|'domestic'|'intl' (orgsMeta의 `org` 필드가 IMF/OECD/ADB면 intl)
  - `timelineGroups(records)` → `[{month: 'YYYY.MM', events: [{org, org_name_ko, published_at, report_title, items: [rec…]}]}]` 최신월 먼저, 이벤트는 (org, published_at) 그룹, items는 지표 순
  - `fmtValue(rec)` → `'12.3만명'` | `'2.1%'` (unit 필드 사용, emp_change는 만명)
  - `fmtDelta(rec)` → `{dir: 'up'|'down'|'flat', text: '+3만명'|'-0.2%p'|'—'}` (revision null → flat/'—'; %지표는 '%p' 접미)
  - `dateLabel(rec, orgsMeta)` → `'2026.08.29 수집'`(method api) | `'2026.08.28 발표'`
  - `isNew(rec, today)` → published_at이 7일 이내
  - `esc(s)` → HTML 이스케이프
- Consumes: 없음 (순수 모듈, fetch 금지)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/web/data.test.mjs`

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import {
  latestRecords, summarize, seriesFor, orgIndicators, compareSet,
  timelineGroups, fmtValue, fmtDelta, dateLabel, isNew, esc,
} from '../../web/js/data.js';

const ORGS = [
  { org: 'OECD', name_ko: 'OECD', method: 'api', track: 'A' },
  { org: 'BOK', name_ko: '한국은행', method: 'pdf', track: 'A' },
];

function rec(over = {}) {
  return {
    id: 'oecd-2026-08-gdp_growth-2027', org: 'OECD', org_name_ko: 'OECD',
    report_title: 'Economic Outlook 119', published_at: '2026-08-29',
    target_year: 2027, target_period: 'annual', indicator: 'gdp_growth',
    value: 1.9, unit: '%', prev_value: null, revision: null,
    rationale: '', rationale_tags: [], source_url: 'https://x/a',
    source_page: null, landing_url: 'https://x', confidence: 'verified',
    collected_at: '2026-08-29T16:00:00+09:00', ...over,
  };
}

test('latestRecords picks newest per org, sorted by date desc', () => {
  const rs = [
    rec({ org: 'BOK', published_at: '2026-05-28', value: 1.8, id: 'a' }),
    rec({ org: 'BOK', published_at: '2026-08-28', value: 2.1, id: 'b' }),
    rec({ published_at: '2026-08-29', id: 'c' }),
  ];
  const latest = latestRecords(rs, { indicator: 'gdp_growth', targetYear: 2027 });
  assert.deepEqual(latest.map((r) => r.id), ['c', 'b']);
});

test('summarize computes count/avg/max/min', () => {
  const latest = [rec({ value: 2.1, org: 'BOK' }), rec({ value: 1.9 })];
  const s = summarize(latest);
  assert.equal(s.count, 2);
  assert.equal(s.avg, 2.0);
  assert.equal(s.max.value, 2.1);
  assert.equal(s.min.value, 1.9);
  assert.equal(summarize([]), null);
});

test('seriesFor sorts ascending', () => {
  const rs = [rec({ published_at: '2026-08-29', id: 'b' }), rec({ published_at: '2026-02-10', id: 'a' })];
  assert.deepEqual(seriesFor(rs, { org: 'OECD', indicator: 'gdp_growth', targetYear: 2027 }).map((r) => r.id), ['a', 'b']);
});

test('orgIndicators returns ordered present indicators', () => {
  const rs = [rec(), rec({ indicator: 'emp_change', value: 12.3, unit: '만명', id: 'x' })];
  assert.deepEqual(orgIndicators(rs, 'OECD'), ['emp_change', 'gdp_growth']);
});

test('compareSet sorts desc, flags stale >90d, filters intl', () => {
  const rs = [
    rec({ org: 'BOK', published_at: '2026-08-28', value: 2.1 }),
    rec({ published_at: '2026-04-01', value: 2.4 }),
  ];
  const set = compareSet(rs, { indicator: 'gdp_growth', targetYear: 2027, today: '2026-08-29', orgsMeta: ORGS, filter: 'all' });
  assert.deepEqual(set.map((e) => e.rec.value), [2.4, 2.1]);
  assert.equal(set[0].stale, true);
  assert.equal(set[1].stale, false);
  assert.equal(set[0].monthLabel, '04.01');
  const intl = compareSet(rs, { indicator: 'gdp_growth', targetYear: 2027, today: '2026-08-29', orgsMeta: ORGS, filter: 'intl' });
  assert.deepEqual(intl.map((e) => e.rec.org), ['OECD']);
});

test('timelineGroups groups by month then org+date', () => {
  const rs = [
    rec({ id: 'a', indicator: 'gdp_growth' }),
    rec({ id: 'b', indicator: 'emp_change', value: 12.3, unit: '만명' }),
    rec({ id: 'c', org: 'BOK', published_at: '2026-05-28' }),
  ];
  const g = timelineGroups(rs);
  assert.deepEqual(g.map((m) => m.month), ['2026.08', '2026.05']);
  assert.equal(g[0].events.length, 1);
  assert.equal(g[0].events[0].items.length, 2);
});

test('fmtValue / fmtDelta formats', () => {
  assert.equal(fmtValue(rec({ value: 12.3, unit: '만명', indicator: 'emp_change' })), '12.3만명');
  assert.equal(fmtValue(rec()), '1.9%');
  assert.deepEqual(fmtDelta(rec()), { dir: 'flat', text: '—' });
  assert.deepEqual(fmtDelta(rec({ revision: 0.2 })), { dir: 'up', text: '+0.2%p' });
  assert.deepEqual(fmtDelta(rec({ revision: -3, unit: '만명', indicator: 'emp_change' })), { dir: 'down', text: '-3만명' });
});

test('dateLabel: api org → 수집, else 발표', () => {
  assert.equal(dateLabel(rec(), ORGS), '2026.08.29 수집');
  assert.equal(dateLabel(rec({ org: 'BOK', published_at: '2026-08-28' }), ORGS), '2026.08.28 발표');
});

test('isNew within 7 days', () => {
  assert.equal(isNew(rec({ published_at: '2026-08-25' }), '2026-08-29'), true);
  assert.equal(isNew(rec({ published_at: '2026-08-10' }), '2026-08-29'), false);
});

test('esc escapes html', () => {
  assert.equal(esc('<b>&"'), '&lt;b&gt;&amp;&quot;');
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/web/data.test.mjs`
Expected: FAIL (Cannot find module data.js)

- [ ] **Step 3: `web/js/data.js` 구현**

테스트를 통과하는 순수 함수들. 핵심 규칙: 날짜 비교는 ISO 문자열 비교(동일 포맷이므로 안전), `INDICATOR_ORDER = ['emp_change','unemp_rate','gdp_growth','cpi','emp_rate','emp_rate_youth','labor_force']`, `INTL_ORGS = new Set(['IMF','OECD','ADB'])`, stale 판정은 `(Date.UTC(today) - Date.UTC(published_at)) > 90일`, `fmtDelta`의 %지표 접미는 `'%p'`·만명 지표는 `'만명'`, avg는 `Math.round(x*10)/10`. `monthLabel`은 `'MM.DD'`.

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/web/data.test.mjs`
Expected: PASS (10 tests)

- [ ] **Step 5: 커밋**

```bash
git add web/js/data.js tests/web/data.test.mjs
git commit -m "feat: 화면 파생 로직 data.js (TDD)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: 앱 셸 (`app.js`) — 라우터·데이터 로딩·상태

**Files:**
- Create: `web/js/app.js`
- Modify: 없음

**Interfaces:**
- Consumes: Task 1 DOM 계약, Task 2 `data.js`
- Produces (화면 모듈 계약): 각 화면 모듈은 `export function render(el, ctx)` — `el`은 `#screen`, `ctx`는:
  ```js
  {
    records,          // ForecastRecord[]
    orgs,             // orgs.json 배열
    indicators,       // indicators.json 배열 (code→meta 맵도 ctx.indicatorMeta로 제공)
    schedule,         // schedule.json 배열
    today,            // 'YYYY-MM-DD' (로컬 KST 기준)
    state,            // { indicator, year, compare: {indicator, year, filter} }
    navigate(hash),   // location.hash 변경
    rerender(),       // 현재 화면 다시 그리기 (상태 변경 후 호출)
  }
  ```
- 라우트: `#/` → home, `#/org/<ORG>` → org(파라미터 `ctx.params.org`), `#/org`(파라미터 없음) → 데이터 있는 첫 기관으로 리다이렉트, `#/compare`, `#/timeline`

- [ ] **Step 1: `web/js/app.js` 구현**

```js
import { render as home } from './screens/home.js';
import { render as org } from './screens/org.js';
import { render as compare } from './screens/compare.js';
import { render as timeline } from './screens/timeline.js';

async function loadJson(path) {
  // 배포(사이트 루트에 data/ 복사)에선 ./data/…, 로컬(저장소 루트 서빙)에선 ../data/… 폴백
  for (const base of ['./', '../']) {
    try {
      const res = await fetch(base + path, { cache: 'no-cache' });
      if (res.ok) return await res.json();
    } catch { /* 다음 후보 */ }
  }
  return null;
}
```

부트: 4개 JSON 병렬 로드(`data/forecasts.json` 등) → 하나라도 null이면 `#offlineBanner` 표시 + 캐시된 응답으로 재시도(서비스워커가 처리) → `ctx` 구성 → `hashchange` 리스너 + 최초 `route()`. `#headerDate`는 forecasts의 `collected_at` 최댓값을 `'MM.DD 갱신'`으로. 탭바는 라우트에 따라 `.tab--active` 토글. 상태 기본값: `{ indicator: 'emp_change', year: 2027, compare: { indicator: 'gdp_growth', year: 2027, filter: 'all' } }` (연도 기본 = 다음 연도 = `today`의 연+1을 계산하되 데이터에 없으면 있는 최대 연도).

home 진입 시 보던 지표를 org 화면이 이어받도록 `state.indicator`는 공유 상태로 둔다(화면기획서 2.2).

- [ ] **Step 2: 문법 검증 + 수동 로드 확인**

Run: `node --check web/js/app.js && node --check web/js/data.js`
화면 모듈이 아직 없으므로 import 에러 방지용 최소 스텁 4개(`export function render(el){ el.textContent = '…'; }`)를 `web/js/screens/`에 생성해 둔다 (다음 태스크들이 실구현으로 교체).

Run: `python -m http.server 8642` 백그라운드 → `python -c "import urllib.request; print(urllib.request.urlopen('http://localhost:8642/web/index.html').status)"` → 200 확인 후 서버 종료.

- [ ] **Step 3: 커밋**

```bash
git add web/js/app.js web/js/screens
git commit -m "feat: 앱 셸 — 해시 라우터, 데이터 로딩, 공유 상태

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 홈 화면 (`screens/home.js`)

**Files:**
- Modify: `web/js/screens/home.js` (스텁 → 실구현)

**픽셀 스펙:** `docs/design/Main.dc.html` — 지표 pill 행, 연도 스위치(‹ ›), 요약 밴드, 기관 카드(기관명/배지/큰 수치/수정폭/날짜/근거 1줄), 미제공 기관 접힘 행, 탭바 위 고지.

**동작 (화면기획서 1장):**
- 지표 pill: `emp_change(취업자 증감)/unemp_rate(실업률)/gdp_growth(성장률)/cpi(물가)` 4개, 클릭 시 `state.indicator` 변경 + `rerender()`. 활성 pill `.pill--active`.
- 연도 스위치: ‹/› 버튼으로 데이터에 존재하는 연도 순환(현재 2026↔2027), `state.year`.
- 카드 목록: `latestRecords()` 결과. 수치는 `fmtValue`, 수정폭 `fmtDelta`(dir별 삼각형 SVG + 색), 날짜 `dateLabel`(수집/발표 라벨 준수), 근거는 `rationale || (method가 api면 '<report_title> · API 수집')` 말줄임 1줄, 배지는 confidence별(검증/자동/확인).
- 요약 밴드: `summarize()` — `'N개 기관 평균 X · 발표시점 상이'` + `'최고 x (기관) · 최저 y (기관)'`. 레코드 1개면 평균 대신 `'1개 기관 수집'`.
- 미제공: orgs.json 전체 중 이 지표 레코드가 없는 기관명을 회색 접힘 행에 나열.
- 카드 클릭 → `navigate('#/org/' + org)`.
- 데이터 0건: `'아직 수집된 전망이 없습니다'` + 마지막 수집 시각.

- [ ] **Step 1: 구현** (위 동작 + 목업 마크업 구조 이식, esc() 필수)
- [ ] **Step 2: 검증** — `node --check web/js/screens/home.js`; `node --test tests/web/data.test.mjs` 여전히 통과
- [ ] **Step 3: 커밋** `feat: 홈 화면 — 지표탭·연도스위치·요약밴드·기관 카드` (+트레일러)

---

### Task 5: 기관 화면 (`screens/org.js`)

**Files:**
- Modify: `web/js/screens/org.js`

**픽셀 스펙:** `docs/design/Org.dc.html` — 뒤로가기 헤더(기관명 + 최근/다음), 지표 요약 카드(2×2 그리드+스파크라인), 연도+지표 pill, SVG 라인 차트, 회차 행(펼침 상태 포함).

**동작 (화면기획서 2장):**
- `ctx.params.org`의 기관. 뒤로가기 → `history.back()` 폴백 `#/`.
- 헤더 서브라인: 최근 = 해당 기관 레코드 최신 날짜(dateLabel), 다음 = schedule.json에 해당 org 있으면 `'다음 MM.DD 예정'`.
- 지표 요약 카드: `orgIndicators()`의 각 지표 최신값 + `fmtDelta` 방향 삼각형 + 스파크라인(seriesFor 값들을 40×14 SVG polyline으로 — 회차 2개 미만이면 평평한 선). 항목 클릭 → 해당 지표로 전환.
- 지표 pill: 그 기관이 제공하는 지표만 (`orgIndicators`), `state.indicator`가 미제공이면 첫 지표로.
- 차트: `seriesFor()` 회차 2개 이상일 때만 표시. 330×110 SVG, 값 범위를 y 16~88에 선형 매핑, 마지막 점 강조 + 값 라벨, x축 `'YY.MM'`.
- 회차 목록: 최신 위. 행 = 발표연월/`fmtValue`/`fmtDelta`/근거 1줄. 행 클릭 → 그 자리 펼침(여러 행 동시 가능): 근거 전문, `rationale_tags` 칩, 배지, 원문 링크(`source_url` 새탭, `source_page` 있으면 `'원문 보기 (p.N)'`; 링크 없으면 `'원문 링크 확인 필요'`; `landing_url`은 `'기관 자료실'` 보조 링크).
- 회차 1개뿐: 차트 숨김, 단일 카드 표시(화면기획서 2.8).

- [ ] **Step 1: 구현**
- [ ] **Step 2: 검증** — `node --check`, 기존 테스트 통과
- [ ] **Step 3: 커밋** `feat: 기관 화면 — 요약카드·회차 차트·펼침 이력` (+트레일러)

---

### Task 6: 비교 화면 (`screens/compare.js`)

**Files:**
- Modify: `web/js/screens/compare.js`

**픽셀 스펙:** `docs/design/Compare.dc.html` — 제목, 지표/연도 셀렉터, 전체/국내/국제 칩, 가로 막대(기관/막대/값/발표월), 격차·평균 밴드, 경고 배너, 표로 보기 버튼.

**동작 (화면기획서 3장):**
- 지표/연도 셀렉터: `<select>`(7개 지표 전부/존재 연도), `state.compare` 갱신 + rerender.
- 막대: `compareSet()` — 최대값 대비 비율 너비, `stale`이면 행 `opacity: .45`, 값 옆 발표월(`monthLabel`) 병기. 막대(행) 클릭 → 기관 화면.
- 밴드: `'격차 X · 평균 Y'`(단위 포함, %지표는 `%p` 격차) + `'N개 기관'`. 2개 미만이면 격차 생략.
- 경고 배너 상시: `'발표 시점이 상이하므로 단순 비교에 유의하세요. 3개월 이상 지난 전망은 흐리게 표시됩니다.'`
- **표로 보기**: 클릭 시 탭 구분 텍스트 표(`기관\t값\t발표일`, dateLabel 규칙 적용)를 `navigator.clipboard.writeText`로 복사, 성공 시 버튼 문구를 1.5초간 `'복사됨 ✓'`… ✓ 금지(이모지·딩벳) → `'복사됨'`으로 변경. clipboard 실패 시 `<textarea>` 표시 폴백.

- [ ] **Step 1: 구현**
- [ ] **Step 2: 검증** — `node --check`, 기존 테스트 통과
- [ ] **Step 3: 커밋** `feat: 비교 화면 — 기관 막대·발표월 병기·표 복사` (+트레일러)

---

### Task 7: 타임라인 화면 + PWA (매니페스트·아이콘·서비스워커)

**Files:**
- Modify: `web/js/screens/timeline.js`
- Create: `web/manifest.webmanifest`, `web/sw.js`, `web/icons/icon-192.png`, `web/icons/icon-512.png`, `tools/make_icons.py`

**픽셀 스펙:** `docs/design/Timeline.dc.html`

**동작 (화면기획서 4장 + 5장):**
- 상단 배너: schedule.json에서 오늘 이후 가장 가까운 일정 → `'다음 발표 예정 — 한국은행 경제전망 11.26'`. 없으면 숨김.
- `timelineGroups()`: 월 라벨 + 이벤트 카드(기관·report_title / 날짜·`'N개 지표 갱신'`·confidence 라벨 / 지표 요약 1줄 = 각 item `지표명 fmtValue` + 방향 삼각형). `isNew()`면 NEW 배지(빨강 pill). 카드 클릭 → 기관 화면.
- **manifest.webmanifest**: `{"name":"고용전망 아카이브","short_name":"고용전망","start_url":"./","display":"standalone","background_color":"#eef0f3","theme_color":"#23508f","icons":[{"src":"./icons/icon-192.png","sizes":"192x192","type":"image/png"},{"src":"./icons/icon-512.png","sizes":"512x512","type":"image/png","purpose":"any maskable"}]}`
- **아이콘**: `tools/make_icons.py` — Pillow(`pip install pillow` ad hoc, requirements.txt 추가 금지)로 512×512: `#23508f` 배경 라운드 사각형 + 흰색 막대 3개(높이 다르게) + 우상향 꺾은선. 512와 192 저장. 스크립트와 산출 PNG 모두 커밋.
- **sw.js**: 설치 시 앱 셸(`index.html, css, js 6개 파일, manifest, icons`) 프리캐시(`CACHE = 'forecast-v1'`), fetch 핸들러: 셸 자원은 cache-first, `data/*.json`은 network-first→실패 시 캐시(stale 허용) → 그것도 없으면 오류. `activate`에서 구캐시 정리. `app.js` 부트에 `navigator.serviceWorker?.register('./sw.js')` 추가(로컬 http에서는 조용히 실패해도 무방).

- [ ] **Step 1: 타임라인 구현**
- [ ] **Step 2: 아이콘 생성** — `pip install pillow` → `python tools/make_icons.py` → 두 PNG 생성 확인
- [ ] **Step 3: manifest·sw 작성 + app.js에 SW 등록 1줄 추가**
- [ ] **Step 4: 검증** — `node --check web/js/screens/timeline.js web/sw.js`; `python -c "import json; json.load(open('web/manifest.webmanifest'))"` → 통과; `node --test` 통과
- [ ] **Step 5: 커밋** `feat: 타임라인 화면 + PWA (매니페스트·아이콘·오프라인 캐시)` (+트레일러)

---

### Task 8: GitHub Pages 배포 워크플로 + 통합 검증

**Files:**
- Create: `.github/workflows/pages.yml`
- Modify: `README.md` (실행 방법에 웹앱 항목 추가)

- [ ] **Step 1: `pages.yml` 작성**

```yaml
name: pages

on:
  push:
    branches: [main]
    paths: ["web/**", "data/**", ".github/workflows/pages.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Build site
        run: |
          mkdir -p _site
          cp -r web/. _site/
          mkdir -p _site/data
          cp data/*.json _site/data/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - uses: actions/deploy-pages@v4
        id: deployment
```

(매일 16시 수집 커밋이 `data/**`를 바꾸면 자동 재배포됨)

- [ ] **Step 2: README에 추가** — "웹앱: `web/` (GitHub Pages 자동 배포, 로컬은 `python -m http.server` 후 `/web/` 접속)" 한 단락.

- [ ] **Step 3: 전체 테스트 + YAML 검증**

Run: `node --test tests/web/data.test.mjs && python -m pytest -q`
Run: `python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/pages.yml').read_text(encoding='utf-8')); print('yaml ok')"`

- [ ] **Step 4: 커밋** `ci: GitHub Pages 배포 워크플로` (+트레일러)

- [ ] **Step 5: (컨트롤러 수행) 브라우저 통합 검증**

로컬 서버(`.claude/launch.json`의 pwa)로 4개 화면을 실제 렌더링해 목업과 대조: 각 화면 스크린샷 → 토큰·레이아웃·라벨(수집/발표)·빈 상태 확인, 콘솔 에러 0건 확인. 발견된 결함은 픽스 라운드로 처리.

---

## 의도적 축소 (화면기획서 대비, Phase 2 이후 후속)

- 5.1 "홈 화면에 추가" 유도 배너 — 브라우저 기본 설치 UI로 갈음
- 1.4 정렬 옵션(수치순/기관명순) — 발표일 최신순 고정
- 1.5 당겨서 새로고침·지표 탭 스와이프 — 버튼/탭 클릭으로 갈음
- 1.2 지표 "더보기"(고용률 등 3종) — 홈은 4개 탭 고정, 비교 화면 셀렉터에서는 7종 전부 선택 가능

## 이 플랜 범위 밖 (Phase 3+)

- 국내기관 PDF 파서(한은·KDI…) — 붙는 순간 홈·기관 화면이 자동으로 채워짐
- 텔레그램 봇, 과거 이력 소급
- OECD 재확인 회차 감지(report_title 변화), published_at 실제 발표일 소급 — 레저 룰링 참조
